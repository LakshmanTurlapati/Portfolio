---
phase: "12-persistent-voice-overlay"
plan: "04"
type: execute
wave: 4
depends_on:
  - "12-01"
  - "12-02"
  - "12-03"
files_modified: []
autonomous: false
requirements:
  - OVLY-01
  - OVLY-02
  - OVLY-03
  - OVLY-04

must_haves:
  truths:
    - "Voice activated on home page, navigating to portfolio keeps VoicePanel visible in fixed overlay"
    - "Voice state (listening/thinking/speaking) does not reset when route changes"
    - "Closing voice mode (X button) works on both home and non-home pages"
    - "On non-home page: 'switch to text' navigates to home and opens ChatPopup"
    - "Ask Parz button is functional on home page; pressing it activates voice"
    - "VoicePanel disappears from fixed overlay when navigating back to home page"
  artifacts:
    - path: "src/providers/voice-session-provider.tsx"
      provides: "Layout-level voice session context"
      exports:
        - VoiceSessionProvider
        - useVoiceSession
    - path: "src/components/voice-overlay.tsx"
      provides: "Fixed-position VoicePanel for non-home pages"
      exports:
        - VoiceOverlay
    - path: "src/app/layout.tsx"
      provides: "Updated provider nesting"
      contains: "VoiceSessionProvider"
    - path: "src/app/page.tsx"
      provides: "Home page consuming voice from context"
      contains: "useVoiceSession"
  key_links:
    - from: "voice-session-provider.tsx"
      to: "voice-overlay.tsx"
      via: "useVoiceSession context"
      pattern: "voiceActive && pathname !== '/'"
---

<objective>
Human visual verification of all Phase 12 requirements — confirm voice session persists across navigation and all four OVLY requirements are satisfied end-to-end.

Purpose: The architecture lift cannot be verified by TypeScript alone. Voice persistence across navigation requires a running app and deliberate test flows that cross page boundaries.

Output: Human confirmation that OVLY-01 through OVLY-04 all pass. If any flow fails, describe the exact failure for targeted debugging.
</objective>

<execution_context>
@/Users/lakshman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lakshman/Documents/Misc/P V3/.planning/PROJECT.md
@/Users/lakshman/Documents/Misc/P V3/.planning/ROADMAP.md
@/Users/lakshman/Documents/Misc/P V3/.planning/STATE.md
@/Users/lakshman/Documents/Misc/P V3/.planning/phases/12-persistent-voice-overlay/12-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start dev server and confirm clean build</name>

  <read_first>
    - src/providers/voice-session-provider.tsx (verify file is complete)
    - src/components/voice-overlay.tsx (verify file is complete)
    - src/app/layout.tsx (verify VoiceSessionProvider + VoiceOverlay are present)
    - src/app/page.tsx (verify useVoiceController is absent, useVoiceSession is present)
  </read_first>

  <files></files>

  <action>
Run the development server and confirm no build errors:

```bash
cd "/Users/lakshman/Documents/Misc/P V3" && npm run dev
```

Wait for "Ready" in the terminal output. If any compilation errors appear, fix them before proceeding to the checkpoint.

Also run a final TypeScript check:
```bash
npx tsc --noEmit --project tsconfig.json 2>&1
```

If TypeScript reports errors, fix them now (do not proceed to checkpoint with TS errors).

Confirm these conditions are met before the checkpoint:
1. `npm run dev` starts without errors
2. `npx tsc --noEmit` exits 0
3. The browser loads `http://localhost:3000` without a blank screen or React error boundary
  </action>

  <verify>
    <automated>cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit --project tsconfig.json 2>&1; echo "EXIT: $?"</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0 (output: "EXIT: 0")
    - `npm run dev` starts without "Failed to compile" in output
    - `grep -rn "useVoiceController" src/app/page.tsx` returns 0 matches (final safety check)
  </acceptance_criteria>

  <done>
    Dev server is running, TypeScript is clean, home page loads in browser.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Plans 01-03 lifted the voice session from page.tsx to layout level via VoiceSessionProvider. A new VoiceOverlay component renders the VoicePanel as a fixed capsule on non-home pages when voice is active. page.tsx now consumes voice state from context instead of owning it. The 'parz:open-text-chat' CustomEvent bridges the "switch to text" flow across the provider/page boundary.
  </what-built>

  <how-to-verify>
    Open http://localhost:3000 in Chrome (required — Web Speech API).

    **Test Flow 1: OVLY-02 — Ask Parz on home page**
    1. Home page loads with particle background
    2. Desktop navbar shows Ask Parz button with green status dot
    3. Click Ask Parz — VoicePanel appears in navbar with GSAP Flip morph (navbar expands to voice-active size)
    4. PASS: VoicePanel visible in navbar, state shows "Ready" or "Listening"

    **Test Flow 2: OVLY-01 — Voice persists across navigation**
    5. While voice is active (VoicePanel visible in navbar), click Portfolio in the navbar
    6. Circular reveal transition fires to portfolio page
    7. PASS: VoicePanel appears as a fixed capsule at the TOP CENTER of the portfolio page (not inside the navbar area — there is no navbar on portfolio). It should show the current voice state (listening/thinking/speaking).
    8. If VoicePanel disappears entirely → voice session is NOT persisting (check VoiceOverlay rendering condition)

    **Test Flow 3: OVLY-04 — VoiceBus state survives navigation**
    9. On portfolio page, if voice was listening before navigation, it should still be listening (or have completed its cycle)
    10. Speak a word — the transcript should appear in the VoicePanel caption area
    11. PASS: Voice still captures speech on portfolio page (state machine not reset)

    **Test Flow 4: OVLY-01 (return to home) — VoicePanel moves back to navbar**
    12. While voice is active on portfolio, click the back arrow to return to home
    13. PASS: Fixed overlay disappears, VoicePanel is back inside the navbar (voice-active state, no morph re-animation needed)

    **Test Flow 5: OVLY-03 — Switch to text from non-home page**
    14. With voice active on portfolio page, click the chat bubble icon in the VoicePanel (switch to text button)
    15. PASS: Navigates to home page AND ChatPopup opens automatically (both should happen)
    16. If ChatPopup does NOT open: the 400ms delay in openTextChat may be too short. Note this as a timing issue.

    **Test Flow 6: Close voice mode on non-home page**
    17. Activate voice on home, navigate to portfolio
    18. Click the X button in the VoicePanel (close voice mode)
    19. PASS: Fixed overlay disappears, voice mode is off, navigating back to home shows normal navbar (not voice-active)
  </how-to-verify>

  <resume-signal>
    Type one of:
    - "approved" — all 6 test flows pass
    - "partial: [describe which flows passed and which failed]" — some flows pass
    - "failed: [describe what went wrong]" — critical failure, needs debugging
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Verification | Human observer confirms behavior; no new code in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-07 | Repudiation | Voice session state across navigation | accept | Verification flow is the mitigation — human confirms state persists without reset |
</threat_model>

<verification>
Human confirms all 6 test flows in the checkpoint pass. Phase 12 is complete when "approved" is received.
</verification>

<success_criteria>
All four OVLY requirements verified by human:
- OVLY-01: Voice panel visible on portfolio/about/chat while session is active
- OVLY-02: Ask Parz activates voice from home page navbar
- OVLY-03: "Switch to text" on non-home page navigates to home and opens ChatPopup
- OVLY-04: VoiceBus state machine (listening/thinking/speaking) does not reset on route change
</success_criteria>

<output>
After completion, create `.planning/phases/12-persistent-voice-overlay/12-04-SUMMARY.md`
</output>
