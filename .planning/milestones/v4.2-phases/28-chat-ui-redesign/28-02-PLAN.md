---
phase: 28-chat-ui-redesign
plan: 02
type: execute
wave: 2
depends_on: [28-01]
files_modified:
  - src/components/chat-popup.tsx
autonomous: true
requirements:
  - CHAT-UI-01
must_haves:
  truths:
    - "User sees the persona name 'Parz' rendered in Instrument Serif italic at 22px in the header."
    - "User sees the optional 'Lakshman's digital twin' subtitle rendered in Lato 12px at opacity 0.55 directly under the persona name."
    - "User sees assistant message bubbles with a transparent secondary tint, 1px subtle border, and tail bottom-left (border-radius 16/16/16/4)."
    - "User sees user message bubbles filled with var(--color-text), text in var(--color-bg), Lato 15px medium weight, tail bottom-right (border-radius 16/16/4/16), max-width 85%."
    - "User sees suggestion chips with full-pill shape (border-radius 999px), Lato 13px medium with letter-spacing 0.005em, and a hover scale of 1.04 (down from previous 1.05)."
    - "User sees suggestion chip strip wrap on desktop (justify-content center) and scroll horizontally on mobile (flex-wrap nowrap, overflow-x auto)."
    - "User sees a 44x44 send button (raised from 36x36) with filled accent when input has content and outlined at 0.30 opacity when empty."
    - "User sees the input field with border-radius 24px, padding 12px 56px 12px 20px, secondary-token background, and a 1px input-border outline."
    - "User on error sees the input wrapper gain a red 1px border (rgba(239,68,68,0.45)) that clears on the next keystroke (onChange)."
    - "User keeps Phase 26 iOS attrs (inputMode, enterKeyHint, autoComplete, onFocus scrollIntoView 300ms) and the input row's safe-area padding intact."
    - "User keeps every existing useEffect, useCallback, useChat destructure, and tool-call dispatch branch byte-identical."
  artifacts:
    - path: "src/components/chat-popup.tsx"
      provides: "Typography contract per UI-SPEC §3 (Instrument Serif persona, Lato 400/500 only)"
      contains: "var(--font-instrument-serif)"
    - path: "src/components/chat-popup.tsx"
      provides: "Color contract per UI-SPEC §4 (monochrome + accent reserved to 5 places)"
      contains: "rgba(239, 68, 68, 0.45)"
    - path: "src/components/chat-popup.tsx"
      provides: "44x44 send button with filled/outlined dual state"
      contains: "width: '44px'"
    - path: "src/components/chat-popup.tsx"
      provides: "Pill-shaped suggestion chips with mobile horizontal scroll"
      contains: "borderRadius: '999px'"
  key_links:
    - from: "src/components/chat-popup.tsx (header h2)"
      to: "var(--font-instrument-serif) defined in src/app/layout.tsx"
      via: "fontFamily inline style"
      pattern: "fontFamily: 'var\\(--font-instrument-serif\\)'"
    - from: "src/components/chat-popup.tsx (input wrapper)"
      to: "currentError state"
      via: "conditional borderColor on input wrapper"
      pattern: "currentError \\? 'rgba\\(239"
---

<objective>
Apply the UI-SPEC §3 (Typography), §4 (Color), §5.4 (Message bubbles), §5.5 (Empty state), §5.6 (Loading indicator polish), §5.7 (Error display), §5.8 (Suggestion chips), §5.9 (Input row), and §5.10 (Error border) treatments to the popup. Build on Plan 01's geometry. This plan is purely visual — no motion changes (Plan 03), no a11y semantics beyond what's already in place from Plan 01.

Purpose: Make the popup look like the UI-SPEC pixel-for-pixel against monochrome + accent palette without disturbing behavior.

Output: src/components/chat-popup.tsx with full visual treatment matching UI-SPEC §3-§5; bubble max-width raised from 80% to 85%; send button raised from 36 to 44; suggestion chips converted to pill-shape with mobile horizontal scroll; input gets dual-state send button and error border.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-chat-ui-redesign/28-CONTEXT.md
@.planning/phases/28-chat-ui-redesign/28-UI-SPEC.md
@.planning/phases/28-chat-ui-redesign/28-01-SUMMARY.md

@src/components/chat-popup.tsx
@src/app/globals.css
@src/app/layout.tsx

<interfaces>
<!-- Typography variables registered in src/app/layout.tsx (verified by UI-SPEC §1): -->
<!-- - var(--font-lato): Lato (100/300/400/700/900) -->
<!-- - var(--font-instrument-serif): Instrument Serif (400 italic + roman) -->
<!-- - JetBrains Mono is NOT registered. Do not use mono. -->

<!-- Theme tokens from src/app/globals.css (verified): -->
<!-- - var(--color-text): #000 light / #fff dark -->
<!-- - var(--color-bg): #FFFFFF light / #000000 dark -->

<!-- Phase 26 / MOB-02 attrs on the input that MUST survive (from chat-popup.tsx): -->
<!--   type="text" inputMode="text" enterKeyHint="send" autoComplete="off" -->
<!--   onFocus={() => setTimeout(() => inputRef.current?.scrollIntoView(...), 300)} -->
<!--   parent paddingBottom: max(16px, env(safe-area-inset-bottom)) -->

<!-- existing PARZ_ERRORS / loadingMessages / smallQuestions / bigQuestions arrays — preserve verbatim -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Apply typography + color to header, empty-state, message bubbles, loading bubble, and error bubble (UI-SPEC §3, §4, §5.4-5.7)</name>
  <files>src/components/chat-popup.tsx</files>
  <read_first>
    - src/components/chat-popup.tsx (state after Plan 01)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §3 (Typography table)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §4 (Color table + accent rules)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §5.4-5.7 (bubble dimensions)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §6 (Copywriting contract)
    - src/app/layout.tsx (confirm --font-instrument-serif and --font-lato are registered)
  </read_first>
  <behavior>
    - Test 1: Header h2 (`id="chat-popup-heading"`) has style `fontFamily: 'var(--font-instrument-serif), serif'`, `fontStyle: 'italic'`, `fontSize: '22px'`, `fontWeight: 400`, `lineHeight: 1.2`, `color: var(--color-text)`.
    - Test 2: Header has a subtitle `<span>` immediately after the h2, text content `Lakshman's digital twin`, with `fontFamily: 'var(--font-lato), sans-serif'`, `fontSize: '12px'`, `fontWeight: 400`, `lineHeight: 1.3`, `opacity: 0.55`, `color: var(--color-text)`.
    - Test 3: Empty-state copy reads `Hey! I'm Parz — Lakshman's digital twin. Ask me anything about his work or projects.` (note em-dash, not en-dash; this matches UI-SPEC §6's correction). Padding 32px 16px 16px (s-7/s-4/s-4). Lato 14/400/1.6, opacity 0.7.
    - Test 4: Assistant message bubbles use `backgroundColor: <secondary tint>` (light: rgba(0,0,0,0.04); dark: rgba(255,255,255,0.06)), `border: 1px solid <subtle border>` (light: rgba(0,0,0,0.08); dark: rgba(255,255,255,0.08)), `color: var(--color-text)`, font Lato 15/400/1.5, padding 10px 14px, maxWidth 85%, borderRadius 16/16/16/4 with tail bottom-left.
    - Test 5: User message bubbles use `backgroundColor: var(--color-text)`, `color: var(--color-bg)`, font Lato 15/500/1.5, no border, padding 10px 14px, maxWidth 85%, borderRadius 16/16/4/16 with tail bottom-right.
    - Test 6: Loading bubble uses the same border + secondary tint as assistant bubbles, padding 10px 14px. The 3-dot wave keeps its dimensions (7x7, gap 4px) and animation `dot-wave-popup 1.4s ease-in-out ${i * 0.2}s infinite` — UNCHANGED. Status text remains the existing `popup-shimmer-text` className with fontSize 12px.
    - Test 7: Error bubble uses `backgroundColor: rgba(239, 68, 68, 0.10)` and `border: 1px solid rgba(239, 68, 68, 0.30)`, color var(--color-text), font Lato 14/400/1.5, maxWidth 85%, borderRadius 16/16/16/4 (assistant tail).
    - Test 8: All bubble max-widths are `85%` (was `80%`).
    - Test 9: NO JetBrains Mono anywhere in the file.
    - Test 10: NO hardcoded `#000` or `#fff` outside of (a) the popup card background `#fafaf7 / #1a1a1c` from Plan 01 and (b) any rgba(0,0,0,X) / rgba(255,255,255,X) tint values explicitly listed in UI-SPEC §4.
  </behavior>
  <action>
Continue editing `src/components/chat-popup.tsx`. Apply visuals.

1. **Header `<h2>` and subtitle:**
   ```tsx
   <div style={{ display: 'flex', flexDirection: 'column', gap: '2px' }}>
     <h2
       id="chat-popup-heading"
       style={{
         margin: 0,
         fontFamily: 'var(--font-instrument-serif), serif',
         fontStyle: 'italic',
         fontSize: '22px',
         fontWeight: 400,
         lineHeight: 1.2,
         color: 'var(--color-text)',
       }}
     >
       Parz
     </h2>
     <span
       style={{
         fontFamily: 'var(--font-lato), sans-serif',
         fontSize: '12px',
         fontWeight: 400,
         lineHeight: 1.3,
         color: 'var(--color-text)',
         opacity: 0.55,
       }}
     >
       Lakshman&apos;s digital twin
     </span>
   </div>
   ```

2. **Empty-state greeting `<div>`:** replace its style block with:
   ```ts
   {
     padding: '32px 16px 16px',
     color: 'var(--color-text)',
     opacity: 0.7,
     fontFamily: 'var(--font-lato), sans-serif',
     fontSize: '14px',
     fontWeight: 400,
     lineHeight: 1.6,
   }
   ```
   And replace its text content from `Hey! I'm Parz — Lakshman's digital twin. Ask me anything about his work or projects.` (preserve verbatim — this matches UI-SPEC §6 already since the existing v4.1 copy uses an em-dash via `&mdash;` / em-dash glyph in source). **Confirm by grep that the existing copy uses `—` (em-dash, U+2014). If the existing copy uses `–` (en-dash, U+2013), replace with em-dash per UI-SPEC §6.**

3. **Message bubble inner `<div>`:** replace its style block with:
   ```ts
   {
     maxWidth: '85%',
     padding: '10px 14px',
     fontFamily: 'var(--font-lato), sans-serif',
     fontSize: '15px',
     fontWeight: isUser ? 500 : 400,
     lineHeight: 1.5,
     wordBreak: 'break-word',
     color: isUser ? 'var(--color-bg)' : 'var(--color-text)',
     backgroundColor: isUser
       ? 'var(--color-text)'
       : isDark
         ? 'rgba(255,255,255,0.06)'
         : 'rgba(0,0,0,0.04)',
     border: isUser
       ? 'none'
       : `1px solid ${isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.08)'}`,
     borderRadius: '16px',
     borderBottomRightRadius: isUser ? '4px' : '16px',
     borderBottomLeftRadius: isUser ? '16px' : '4px',
   }
   ```

4. **Loading bubble inner `<div>`:** replace its style block with:
   ```ts
   {
     padding: '10px 14px',
     border: `1px solid ${isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.08)'}`,
     backgroundColor: isDark ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.04)',
     borderRadius: '16px',
     borderBottomLeftRadius: '4px',
   }
   ```
   **Do NOT touch the inner three-dot div (animation: `dot-wave-popup 1.4s ease-in-out ${i * 0.2}s infinite`) or the `popup-shimmer-text` paragraph.**

5. **Error bubble inner `<div>`:** replace its style block with:
   ```ts
   {
     maxWidth: '85%',
     padding: '10px 14px',
     fontFamily: 'var(--font-lato), sans-serif',
     fontSize: '14px',
     fontWeight: 400,
     lineHeight: 1.5,
     backgroundColor: 'rgba(239, 68, 68, 0.10)',
     border: '1px solid rgba(239, 68, 68, 0.30)',
     color: 'var(--color-text)',
     borderRadius: '16px',
     borderBottomLeftRadius: '4px',
   }
   ```

6. **Close button hover state:** add inline `onMouseEnter` / `onMouseLeave` handlers to bump opacity from 0.7 to 1.0 with a 200ms transition. Add `transition: 'opacity 200ms ease, background-color 200ms ease'` to the style. Hover background: `isDark ? 'rgba(255,255,255,0.05)' : 'rgba(0,0,0,0.05)'`.

7. **DO NOT TOUCH:**
   - The empty-state predicate `messages.length === 0 && !isLoading`.
   - The message map's `key={message.id}` or the wrapper `<div style={{ display: 'flex', justifyContent: isUser ? 'flex-end' : 'flex-start', marginBottom: '12px' }}>`.
   - The 3-dot wave animation (Plan 03 may add reduced-motion fallback).
   - The `popup-shimmer-text` className.
   - The error display predicate `currentError &&`.
   - The `<div ref={messagesEndRef} />` sentinel.
   - The `RenderLinkedText` component.
   - The `sanitizeText` call.
   - Any useEffect, useCallback, useState, useRef, useChat, useSiteControl.
   - The Phase 26 input attrs and onFocus handler.

References:
- UI-SPEC §3 typography rules (Lato 400/500 only; Instrument Serif italic for persona only)
- UI-SPEC §4 color (accent reserved; secondary tint = rgba(0,0,0,0.04) light / rgba(255,255,255,0.06) dark; subtle border = 0.08 alpha)
- UI-SPEC §5.4 bubble dimensions (max-width 85%, padding 10/14, radius 16/16/16/4)
- UI-SPEC §5.5 empty state (padding 32/16/16, Lato 14/400/1.6, opacity 0.7)
- UI-SPEC §5.6 loading bubble (same shape as assistant)
- UI-SPEC §5.7 error bubble (red 0.10 bg, 0.30 border)
- UI-SPEC §6 copy (em-dash for empty state; subtitle "Lakshman's digital twin")
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "var(--font-instrument-serif)" src/components/chat-popup.tsx && grep -q "fontStyle: 'italic'" src/components/chat-popup.tsx && grep -q "fontSize: '22px'" src/components/chat-popup.tsx && grep -q "Lakshman" src/components/chat-popup.tsx && grep -q "maxWidth: '85%'" src/components/chat-popup.tsx && grep -q "fontWeight: isUser ? 500 : 400" src/components/chat-popup.tsx && grep -q "rgba(0,0,0,0.04)" src/components/chat-popup.tsx && grep -q "rgba(255,255,255,0.06)" src/components/chat-popup.tsx && grep -q "rgba(0,0,0,0.08)" src/components/chat-popup.tsx && grep -q "rgba(239, 68, 68, 0.10)" src/components/chat-popup.tsx && grep -q "rgba(239, 68, 68, 0.30)" src/components/chat-popup.tsx && grep -q "borderBottomLeftRadius" src/components/chat-popup.tsx && grep -q "popup-shimmer-text" src/components/chat-popup.tsx && grep -q "dot-wave-popup" src/components/chat-popup.tsx && grep -q "messagesEndRef" src/components/chat-popup.tsx && grep -q "inputMode=\"text\"" src/components/chat-popup.tsx && ! grep -q "JetBrains" src/components/chat-popup.tsx && ! grep -q "maxWidth: '80%'" src/components/chat-popup.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - Header h2 + subtitle render with Instrument Serif italic + Lato 12 opacity 0.55.
    - Bubbles use 85% max-width, secondary tint for assistant, var(--color-text) fill for user, subtle border for assistant.
    - Loading + error bubbles use the right tints.
    - No JetBrains Mono.
    - All Phase 25/26 inheritance still present.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Apply suggestion chip pill treatment + input row + 44x44 send button + error border on input wrapper (UI-SPEC §5.8, §5.9, §5.10)</name>
  <files>src/components/chat-popup.tsx</files>
  <read_first>
    - src/components/chat-popup.tsx (state after Task 1)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §5.8 (Suggestion chips strip)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §5.9 (Input row)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §5.10 (Error border)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §4 (focus ring + accent reservation)
  </read_first>
  <behavior>
    - Test 1: Suggestion strip container uses `display: flex; gap: 8px; padding: 8px 16px; flexShrink: 0` and switches between `flexWrap: 'nowrap'; overflowX: 'auto'; WebkitOverflowScrolling: 'touch'; scrollbarWidth: 'none'` on mobile and `flexWrap: 'wrap'; justifyContent: 'center'` on desktop, gated by the existing `isDesktop` flag.
    - Test 2: Each suggestion chip button uses padding `8px 16px`, borderRadius `999px`, fontFamily Lato sans, fontSize 13px, fontWeight 500, letterSpacing `0.005em`, border 1px solid `<chip border>` (rgba(0,0,0,0.15) light / rgba(255,255,255,0.20) dark), background secondary tint (rgba(0,0,0,0.04) light / rgba(255,255,255,0.06) dark), color var(--color-text), transition `transform 200ms ease, border-color 200ms ease`.
    - Test 3: Suggestion chip hover scale is `1.04` (was 1.05) and active scale is `0.98`.
    - Test 4: Input field uses borderRadius `24px`, padding `12px 56px 12px 20px`, fontFamily Lato sans, fontSize 14px, fontWeight 400, lineHeight 1.5, background secondary tint, color var(--color-text), border 1px solid input-border (rgba(0,0,0,0.10) light / rgba(255,255,255,0.12) dark).
    - Test 5: Input field disabled state uses `opacity: 0.6; cursor: 'not-allowed'`.
    - Test 6: Input WRAPPER (the `<div style={{ position: 'relative' }}>` that contains the input + send button) gains a conditional border style: `border: 1px solid <token>; borderRadius: 24px; transition: border-color 200ms ease-in-out;`. Border color uses `rgba(239, 68, 68, 0.45)` when `currentError !== null`, else `transparent`. **Note:** the input field itself keeps its own border too — the wrapper's red ring sits OUTSIDE the input's border per UI-SPEC §5.10.
    - Test 7: Input onChange handler is augmented to clear `currentError` on first keystroke (when `inputValue.length === 0` transitions to >= 1). Logic: `setInputValue(e.target.value); if (e.target.value.length > 0 && currentError) setCurrentError(null);`.
    - Test 8: Send button is `width: 44px; height: 44px; borderRadius: 999px; right: 6px; top: 50%; transform: translateY(-50%)`. Enabled state: `backgroundColor: var(--color-text); color: var(--color-bg); opacity: 1; cursor: pointer; border: none`. Disabled state: `backgroundColor: 'transparent'; color: var(--color-text); opacity: 0.30; cursor: not-allowed; border: 1px solid <subtle border>`.
    - Test 9: Send button transition is `transform 150ms ease, background-color 200ms ease, opacity 200ms ease`. Hover (enabled): `transform: translateY(-50%) scale(1.04)`. Active (enabled): `transform: translateY(-50%) scale(0.94)`. Implement hover via onMouseEnter/onMouseLeave + active via onMouseDown/onMouseUp inline state OR a CSS class via the scoped `<style>` block. **Recommendation:** use a small piece of local state `const [sendHover, setSendHover] = useState(false)` to keep the change inline and easy to audit.
    - Test 10: Send button icon size is 16 (was 14) per UI-SPEC §5.9.
    - Test 11: Input row container `<div>` outer style still uses `padding: '8px 16px'` plus `paddingBottom: 'max(16px, env(safe-area-inset-bottom))'` (UI-SPEC §5.9 + Phase 26 / MOB-02 — verbatim).
    - Test 12: All four iOS attrs and the onFocus 300ms scrollIntoView handler are byte-identical.
  </behavior>
  <action>
Continue editing `src/components/chat-popup.tsx`.

1. **Suggestion strip container `<div>`:** replace its style block with:
   ```ts
   {
     display: 'flex',
     gap: '8px',
     padding: '8px 16px',
     flexShrink: 0,
     ...(isDesktop
       ? { flexWrap: 'wrap' as const, justifyContent: 'center' }
       : {
           flexWrap: 'nowrap' as const,
           overflowX: 'auto' as const,
           WebkitOverflowScrolling: 'touch' as const,
           scrollbarWidth: 'none' as const,
         }),
   }
   ```

2. **Suggestion chip buttons (both):** replace each style block with:
   ```ts
   {
     padding: '8px 16px',
     borderRadius: '999px',
     fontFamily: 'var(--font-lato), sans-serif',
     fontSize: '13px',
     fontWeight: 500,
     letterSpacing: '0.005em',
     lineHeight: 1.3,
     cursor: 'pointer',
     border: `1px solid ${isDark ? 'rgba(255,255,255,0.20)' : 'rgba(0,0,0,0.15)'}`,
     color: 'var(--color-text)',
     backgroundColor: isDark ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.04)',
     transition: 'transform 200ms ease, border-color 200ms ease',
     whiteSpace: 'nowrap' as const,
     flexShrink: 0,
   }
   ```
   Update onMouseEnter to `(e) => (e.currentTarget.style.transform = 'scale(1.04)')` (was 1.05).
   Add onMouseDown to `(e) => (e.currentTarget.style.transform = 'scale(0.98)')` and onMouseUp back to `'scale(1.04)'` if hovered, else `'scale(1)'`. **Simpler:** keep onMouseDown/Up inline with `'scale(0.98)'` and `'scale(1)'` resp., trading a tiny hover-during-click flicker for code simplicity. Either approach is acceptable.
   Keep onClick={() => handleSuggestionClick(suggestions.small)} / .big — verbatim.

3. **Input wrapper `<div style={{ position: 'relative' }}>`:** change its style to:
   ```ts
   {
     position: 'relative',
     borderRadius: '24px',
     border: `1px solid ${currentError ? 'rgba(239, 68, 68, 0.45)' : 'transparent'}`,
     transition: 'border-color 200ms ease-in-out',
   }
   ```

4. **Input field `<input>`:** replace its style block with:
   ```ts
   {
     width: '100%',
     borderRadius: '24px',
     padding: '12px 56px 12px 20px',
     fontFamily: 'var(--font-lato), sans-serif',
     fontSize: '14px',
     fontWeight: 400,
     lineHeight: 1.5,
     outline: 'none',
     backgroundColor: isDark ? 'rgba(255,255,255,0.06)' : 'rgba(0,0,0,0.04)',
     color: 'var(--color-text)',
     backdropFilter: 'blur(10px)',
     border: `1px solid ${isDark ? 'rgba(255,255,255,0.12)' : 'rgba(0,0,0,0.10)'}`,
     boxSizing: 'border-box' as const,
     opacity: isLoading ? 0.6 : 1,
     cursor: isLoading ? 'not-allowed' : 'text',
   }
   ```
   **DO NOT TOUCH** the existing input attributes: `ref={inputRef} type="text" inputMode="text" enterKeyHint="send" autoComplete="off" value={inputValue} onKeyDown={handleKeyDown} onFocus={() => setTimeout(...)} placeholder="Talk to my persona!" disabled={isLoading}`.
   **CHANGE the onChange handler** to:
   ```tsx
   onChange={(e) => {
     setInputValue(e.target.value);
     if (e.target.value.length > 0 && currentError) setCurrentError(null);
   }}
   ```

5. **Send button `<button>`:** replace its style block with:
   ```ts
   {
     position: 'absolute' as const,
     right: '6px',
     top: '50%',
     transform: sendHover && inputValue.trim() && !isLoading
       ? 'translateY(-50%) scale(1.04)'
       : 'translateY(-50%)',
     width: '44px',
     height: '44px',
     borderRadius: '999px',
     display: 'flex',
     alignItems: 'center',
     justifyContent: 'center',
     cursor: inputValue.trim() && !isLoading ? 'pointer' : 'not-allowed',
     backgroundColor: inputValue.trim() && !isLoading ? 'var(--color-text)' : 'transparent',
     color: inputValue.trim() && !isLoading ? 'var(--color-bg)' : 'var(--color-text)',
     opacity: inputValue.trim() && !isLoading ? 1 : 0.30,
     border: inputValue.trim() && !isLoading
       ? 'none'
       : `1px solid ${isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.08)'}`,
     transition: 'transform 150ms ease, background-color 200ms ease, opacity 200ms ease, color 200ms ease',
   }
   ```
   Add a piece of state at the top of the component (alongside the others): `const [sendHover, setSendHover] = useState(false);`
   Add to the button: `onMouseEnter={() => setSendHover(true)} onMouseLeave={() => setSendHover(false)}`.
   For the active scale, add `onMouseDown={(e) => { if (inputValue.trim() && !isLoading) e.currentTarget.style.transform = 'translateY(-50%) scale(0.94)'; }}` and `onMouseUp={(e) => { if (inputValue.trim() && !isLoading) e.currentTarget.style.transform = sendHover ? 'translateY(-50%) scale(1.04)' : 'translateY(-50%)'; }}`.
   Bump icon size: `<FaArrowUp size={16} />` (was 14).
   Keep `onClick={handleSend}`, `disabled={!inputValue.trim() || isLoading}`, `aria-label="Send message"`.

6. **Input row container `<div>` (outer):** confirm its style is exactly:
   ```ts
   {
     padding: '8px 16px',
     paddingBottom: 'max(16px, env(safe-area-inset-bottom))',
     flexShrink: 0,
   }
   ```
   The existing v4.1 padding string `'8px 16px 16px'` is wrong with the override below it; replace with the shorthand `'8px 16px'` and let `paddingBottom` take effect. **DO NOT remove `paddingBottom: 'max(16px, env(safe-area-inset-bottom))'`** — that's Phase 26.

7. **DO NOT TOUCH:**
   - showSuggestions predicate
   - useState for suggestions (small + big random pick)
   - handleSuggestionClick body
   - handleSend body
   - handleKeyDown body
   - All useEffect bodies
   - useChat destructure, siteControlChatTransport, useSiteControl

After saving, grep for the iOS attrs, useChat call, and tool-call dispatch markers to confirm survival.

References:
- UI-SPEC §5.8 chip dimensions and mobile-vs-desktop strip
- UI-SPEC §5.9 input field + send button (44x44, dual state, transitions)
- UI-SPEC §5.10 error border on wrapper (clears on first keystroke)
- UI-SPEC §4 accent reserved to user bubble + send (filled when input has text) + persona name + focus ring + chip border on hover
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "borderRadius: '999px'" src/components/chat-popup.tsx && grep -q "letterSpacing: '0.005em'" src/components/chat-popup.tsx && grep -q "width: '44px'" src/components/chat-popup.tsx && grep -q "height: '44px'" src/components/chat-popup.tsx && grep -q "FaArrowUp size={16}" src/components/chat-popup.tsx && grep -q "padding: '12px 56px 12px 20px'" src/components/chat-popup.tsx && grep -q "rgba(239, 68, 68, 0.45)" src/components/chat-popup.tsx && grep -q "currentError && setCurrentError(null)" src/components/chat-popup.tsx && grep -q "sendHover" src/components/chat-popup.tsx && grep -q "scale(1.04)" src/components/chat-popup.tsx && grep -q "scale(0.94)" src/components/chat-popup.tsx && grep -q "max(16px, env(safe-area-inset-bottom))" src/components/chat-popup.tsx && grep -q "inputMode=\"text\"" src/components/chat-popup.tsx && grep -q "enterKeyHint=\"send\"" src/components/chat-popup.tsx && grep -q "autoComplete=\"off\"" src/components/chat-popup.tsx && grep -q "scrollIntoView({ block: 'center', behavior: 'smooth' })" src/components/chat-popup.tsx && grep -q "handleSuggestionClick" src/components/chat-popup.tsx && grep -q "siteControlChatTransport" src/components/chat-popup.tsx && grep -q "handledToolCallsRef" src/components/chat-popup.tsx && ! grep -q "scale(1.05)" src/components/chat-popup.tsx && ! grep -q "width: '36px'" src/components/chat-popup.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - Suggestion chips are pill-shaped, Lato 13/500/0.005em, hover 1.04, mobile horizontal scroll vs desktop wrap.
    - Send button is 44x44 with filled/outlined dual state and 1.04/0.94 hover/active scale.
    - Input wrapper has the conditional red error border that clears on first keystroke.
    - Phase 26 iOS attrs and safe-area padding intact.
    - All useChat / useEffect / handler logic byte-identical.
  </done>
</task>

</tasks>

<verification>
After both tasks land, the popup matches UI-SPEC §3, §4, §5.4-§5.10 visually:
- Persona name in Instrument Serif italic 22, subtitle in Lato 12 0.55 opacity
- Bubbles in Lato 15, user-filled accent / assistant secondary tint with subtle border, 85% max-width, tail bottom-right / bottom-left
- Empty-state in Lato 14 opacity 0.7
- Suggestion chips full-pill, Lato 13/500/0.005em, hover 1.04, mobile strip / desktop wrap
- Input field with new dimensions, error border on wrapper that clears on first keystroke
- Send button 44x44, filled accent when armed, outlined at 0.30 opacity when empty

Manual smoke at end of plan:
1. Open popup empty → see greeting + 2 chips + 44x44 outlined send button.
2. Click chip → message sends, suggestion strip disappears (predicate: suggestionClicked=true).
3. Type → send button fills accent, chip-strip already gone.
4. Send → response appears in assistant bubble with subtle border.
5. Toggle theme → all token-bound colors invert.
6. Force an error (offline) → red wrapper border + Parz error bubble.
7. Type a character → red border fades to default 200ms.
8. (iOS) Focus input → keyboard pops, scrollIntoView fires after 300ms.
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → DOM (input value) | Typed input rendered into user bubble verbatim |
| Server → DOM (assistant text) | Streamed assistant text passed through sanitizeText then RenderLinkedText |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-04 | Tampering | RenderLinkedText link href | accept | linkifyText / RenderLinkedText is unchanged from v4.1; URL handling not in scope this phase. |
| T-28-05 | Information disclosure | Error bubble shows technical error.message | mitigate | currentError is set from `getRandomItem(PARZ_ERRORS)` — never from `error.message`. Preserved verbatim from v4.1. |
| T-28-06 | Repudiation | Suggestion chip click sends message identical to a typed message | accept | sendMessage path is unchanged; server-side logging is unaffected. |
</threat_model>

<success_criteria>
- Persona name uses var(--font-instrument-serif) italic 22.
- Subtitle "Lakshman's digital twin" present at Lato 12 / 0.55 opacity.
- Empty-state copy uses em-dash (verified by grep for the exact text).
- All bubbles use 85% max-width with the correct tail radius.
- Loading bubble retains the dot-wave animation byte-identical.
- Error bubble uses rgba(239,68,68,0.10) bg + 0.30 alpha border.
- Suggestion chips are pill-shaped with hover scale 1.04.
- Mobile chip strip scrolls horizontally; desktop wraps and centers.
- Send button is 44x44 and uses the dual fill/outline state.
- Input wrapper border turns red on error and clears on first keystroke.
- All Phase 26 iOS attrs survive verbatim.
- All useChat / useEffect / handler logic survives grep.
- No JetBrains Mono anywhere in the file.
</success_criteria>

<output>
After completion, create `.planning/phases/28-chat-ui-redesign/28-02-SUMMARY.md` documenting:
- Visual treatment grep evidence
- Behavior preservation grep evidence (Phase 25 + Phase 26 inheritance)
- Any deviations from UI-SPEC §3-§5 (none expected)
- Hand-off note to Plan 03: motion is still v4.1 (popupIn keyframe removed in Plan 01; no entry animation right now); a11y semantics beyond role="dialog" still missing.
</output>
