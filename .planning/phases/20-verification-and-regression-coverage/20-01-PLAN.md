---
phase: 20
plan: 01
title: Verification Harness and Contract Tests
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.ts
  - playwright.config.ts
  - src/providers/site-control-provider.tsx
  - tests/parz-contracts.test.ts
  - tests/project-resolution.test.ts
  - e2e/parz-site-control.spec.ts
autonomous: true
requirements: [EVAL-01, EVAL-02, EVAL-03, EVAL-04, EVAL-05]
---

<objective>
Add deterministic local verification for v4.1 Parz persona/content/safety/project-control behavior with Vitest and Playwright, without live API or secret dependencies.
</objective>

<tasks>
<task id="1" type="execute">
<read_first>
- package.json
- src/data/public-profile.ts
- src/data/system-prompt.ts
- src/data/bio.ts
- src/data/experience.ts
- src/data/projects.ts
- src/providers/site-control-provider.tsx
- src/components/fsb-control-overlay.tsx
</read_first>
<action>
Add `test`, `test:watch`, and `test:e2e` scripts. Add Vitest and Playwright configuration for local TypeScript tests and E2E tests. Do not add postinstall browser-install side effects.
</action>
<acceptance_criteria>
- `package.json` contains `"test": "vitest run"`.
- `package.json` contains `"test:watch": "vitest"`.
- `package.json` contains `"test:e2e": "playwright test"`.
- `vitest.config.ts` exists.
- `playwright.config.ts` exists.
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- src/data/public-profile.ts
- src/data/system-prompt.ts
- src/data/bio.ts
- src/data/experience.ts
- src/data/projects.ts
</read_first>
<action>
Create Vitest tests that assert Parz prompt/persona facts, guardrails, source parity, and project resolver behavior. Exact locked values include `AI Enablement Engineer`, `InfiniteChoice`, `Voyza`, `https://gitfly.ai`, `https://www.full-selfbrowsing.com`, aliases `FSB`, `Full Self Browsing`, `GitFly`, `Review Gate`, `T2S`, and `Parz-AI`.
</action>
<acceptance_criteria>
- `tests/parz-contracts.test.ts` imports `publicProfile`, `systemPrompt`, `bioSegments`, `experienceData`, and `PROJECT_DETAILS`.
- `tests/project-resolution.test.ts` imports `resolveProject`, `getProjectBrowserTarget`, and `isApprovedProjectUrl`.
- `npm test` exits 0.
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- src/providers/site-control-provider.tsx
- src/components/fsb-control-overlay.tsx
- src/components/iframe-viewer.tsx
</read_first>
<action>
Expose a non-production browser test hook from `SiteControlProvider` for E2E tests and add Playwright coverage that calls site-control actions directly. The hook must not be installed when `process.env.NODE_ENV === 'production'`.
</action>
<acceptance_criteria>
- `src/providers/site-control-provider.tsx` contains `__parzSiteControl`.
- `src/providers/site-control-provider.tsx` contains `process.env.NODE_ENV === 'production'` guard logic for the test hook.
- `e2e/parz-site-control.spec.ts` asserts `powered by FSB`.
- `e2e/parz-site-control.spec.ts` asserts `gitfly.ai` after opening GitFly.
</acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm test`.
- Run `npm run lint`.
- Run `npm run test:e2e` if Playwright browsers are installed; otherwise record the browser-install blocker and command.
</verification>

<success_criteria>
- EVAL-01 through EVAL-04 are covered by passing Vitest tests.
- EVAL-05 is covered by a Playwright spec that can run locally without API keys.
- No production API keys, live model calls, or production URLs are required.
</success_criteria>
