---
phase: 19-fsb-inspired-control-overlay
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/site-control-provider.tsx
  - src/components/fsb-control-overlay.tsx
  - src/app/globals.css
autonomous: true
requirements: [FSB-01, FSB-02, FSB-03]
user_setup: []
must_haves:
  truths:
    - Parz-triggered site-control actions show a monochrome FSB-inspired overlay and bottom-left badge while control is active.
    - Manual user clicks, route changes, and general chat/voice thinking do not trigger the overlay.
    - Overlay visuals are pointer-safe and do not prevent closing the inbuilt browser, using voice controls, navigation, or scrolling.
    - Overlay clears cleanly after success, error, or blocked control results.
  artifacts:
    - src/components/fsb-control-overlay.tsx exists.
    - src/providers/site-control-provider.tsx renders FsbControlOverlay and wraps provider control methods.
    - src/app/globals.css contains FSB overlay animation and reduced-motion CSS.
  key_links:
    - FsbControlOverlay uses exact badge copy `powered by FSB`.
    - FsbControlOverlay exposes the screen-reader status `Parz is controlling the site.`.
---

<objective>
Add the FSB-inspired visible control overlay around existing Parz site-control actions.

Purpose: Users should be able to tell when Parz is actively operating the portfolio shell without losing access to browser, voice, nav, or scroll controls.
Output: A theme-aware monochrome overlay component, global CSS treatment, and `SiteControlProvider` lifecycle wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/19-fsb-inspired-control-overlay/19-CONTEXT.md
@.planning/phases/19-fsb-inspired-control-overlay/19-UI-SPEC.md
@.planning/phases/19-fsb-inspired-control-overlay/19-RESEARCH.md
@src/providers/site-control-provider.tsx
@src/components/voice-glow.tsx
@src/components/iframe-viewer.tsx
@src/app/globals.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the FSB control overlay component</name>
  <files>src/components/fsb-control-overlay.tsx</files>
  <read_first>src/components/voice-glow.tsx, src/components/voice-overlay.tsx, src/app/globals.css, .planning/phases/19-fsb-inspired-control-overlay/19-UI-SPEC.md</read_first>
  <action>Create `src/components/fsb-control-overlay.tsx` as a client component exporting `function FsbControlOverlay({ active }: { active: boolean })`. Use `useTheme` and `useMounted`. Return `null` when not mounted or not active. When active, render a fixed full-viewport root with `aria-hidden="true"`, inline CSS custom property `--fsb-overlay-rgb` set to `255,255,255` in dark mode and `0,0,0` in light mode, and class names including `pointer-events-none`, `fixed`, `inset-0`, and `fsb-control-overlay`. Render decorative child elements for scan/grid/corner/target marks using classes `fsb-control-grid`, `fsb-control-corners`, and `fsb-control-target`. Render a bottom-left badge with exact visible text `powered by FSB`. Render a separate `span` with class `sr-only`, `role="status"`, `aria-live="polite"`, and exact text `Parz is controlling the site.`.</action>
  <verify>Grep `src/components/fsb-control-overlay.tsx` for `export function FsbControlOverlay`, `powered by FSB`, `Parz is controlling the site.`, `pointer-events-none`, and `--fsb-overlay-rgb`.</verify>
  <acceptance_criteria>
    - `src/components/fsb-control-overlay.tsx` contains `export function FsbControlOverlay`.
    - `src/components/fsb-control-overlay.tsx` contains `powered by FSB`.
    - `src/components/fsb-control-overlay.tsx` contains `Parz is controlling the site.`.
    - `src/components/fsb-control-overlay.tsx` contains `pointer-events-none`.
    - `src/components/fsb-control-overlay.tsx` contains `--fsb-overlay-rgb`.
  </acceptance_criteria>
  <done>Overlay component renders only while active and includes required badge/accessibility status.</done>
</task>

<task type="auto">
  <name>Task 2: Wrap site-control methods with overlay lifecycle</name>
  <files>src/providers/site-control-provider.tsx</files>
  <read_first>src/providers/site-control-provider.tsx, src/components/fsb-control-overlay.tsx, src/components/iframe-viewer.tsx</read_first>
  <action>Import `FsbControlOverlay` from `@/components/fsb-control-overlay`. Add `const [controlOverlayActive, setControlOverlayActive] = useState(false);` and `const overlayHideTimerRef = useRef<ReturnType<typeof window.setTimeout> | null>(null);`. Add a helper named `runWithControlOverlay` that accepts a synchronous `() => ControlResult`, clears any pending hide timer, sets overlay active before running the callback, runs the callback, then schedules `setControlOverlayActive(false)` after 900ms in a `finally` block and returns the original `ControlResult`. Wrap the bodies of `navigate`, `openProject`, `scrollTo`, `closeBrowser`, `openCurrentProjectExternal`, and `unsupportedIframeControl` with `runWithControlOverlay`. Render `<FsbControlOverlay active={controlOverlayActive} />` inside `SiteControlProvider` after `{children}` and before `{browser && (...)}`. Add cleanup so any pending hide timer is cleared when the provider unmounts.</action>
  <verify>Grep `src/providers/site-control-provider.tsx` for `FsbControlOverlay`, `controlOverlayActive`, `runWithControlOverlay`, `overlayHideTimerRef`, and `<FsbControlOverlay active={controlOverlayActive} />`.</verify>
  <acceptance_criteria>
    - `src/providers/site-control-provider.tsx` contains `import { FsbControlOverlay } from '@/components/fsb-control-overlay';`.
    - `src/providers/site-control-provider.tsx` contains `controlOverlayActive`.
    - `src/providers/site-control-provider.tsx` contains `runWithControlOverlay`.
    - `src/providers/site-control-provider.tsx` contains `overlayHideTimerRef`.
    - `src/providers/site-control-provider.tsx` contains `<FsbControlOverlay active={controlOverlayActive} />`.
  </acceptance_criteria>
  <done>All Parz site-control provider actions trigger and clear the overlay through one shared lifecycle helper.</done>
</task>

<task type="auto">
  <name>Task 3: Add monochrome overlay CSS</name>
  <files>src/app/globals.css</files>
  <read_first>src/app/globals.css, .planning/phases/19-fsb-inspired-control-overlay/19-UI-SPEC.md</read_first>
  <action>Add a Phase 19 CSS section to `src/app/globals.css`. Define `.fsb-control-overlay` with z-index below the `IframeViewer` (`z-index: 70`), `mix-blend-mode: normal`, and no background that blocks content. Define `.fsb-control-grid` using multiple `linear-gradient` backgrounds based on `rgba(var(--fsb-overlay-rgb), ...)` to create subtle scan/grid lines. Define `.fsb-control-corners::before` and `.fsb-control-corners::after` for monochrome corner marks. Define `.fsb-control-target` for a small centered target/crosshair motif. Define `.fsb-control-badge` for fixed bottom-left badge placement, uppercase tracking, 10px font size, rounded border, and exact pointer-safe visual treatment. Add `@keyframes fsbControlScan` and animation usage for scan movement. Inside the existing `@media (prefers-reduced-motion: reduce)` block or a new one, disable `.fsb-control-grid` animation with `animation: none !important;`.</action>
  <verify>Grep `src/app/globals.css` for `fsb-control-overlay`, `fsb-control-grid`, `fsb-control-badge`, `fsbControlScan`, and `prefers-reduced-motion`.</verify>
  <acceptance_criteria>
    - `src/app/globals.css` contains `.fsb-control-overlay`.
    - `src/app/globals.css` contains `.fsb-control-grid`.
    - `src/app/globals.css` contains `.fsb-control-badge`.
    - `src/app/globals.css` contains `@keyframes fsbControlScan`.
    - `src/app/globals.css` contains `.fsb-control-grid {` inside a `prefers-reduced-motion` rule with `animation: none !important;`.
  </acceptance_criteria>
  <done>Overlay visuals match the UI-SPEC and respect reduced motion.</done>
</task>

</tasks>

<verification>
- [ ] `src/components/fsb-control-overlay.tsx` exists and includes exact badge and screen-reader copy.
- [ ] `src/providers/site-control-provider.tsx` wraps all Parz control actions with the overlay lifecycle helper.
- [ ] `src/app/globals.css` contains monochrome overlay, badge, scan animation, and reduced-motion CSS.
- [ ] `npm run lint` passes.
- [ ] `npm run build` passes.
</verification>

<success_criteria>
- FSB-01: Overlay appears for Parz control actions.
- FSB-02: Bottom-left `powered by FSB` badge appears during control actions.
- FSB-03: Overlay is `pointer-events-none` and does not block browser, voice, nav, or scroll controls.
</success_criteria>

<output>
After completion, create `.planning/phases/19-fsb-inspired-control-overlay/19-01-SUMMARY.md`.
</output>
