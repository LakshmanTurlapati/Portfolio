---
phase: 26-mobile-ux-pass
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/particle-background.tsx
autonomous: false
requirements:
  - MOB-01
must_haves:
  truths:
    - "User on a mobile device (<768px) sees ~45 particles instead of 90"
    - "User on tablet/desktop (>=768px) continues to see 90 particles unchanged"
    - "User resizing the browser across the 768px boundary triggers a clean reinit (no orphaned canvases, no console errors)"
    - "User with VoiceBus level > 0.01 still sees the breathing rAF loop modulate opacity/line distance smoothly with the reduced count"
  artifacts:
    - path: "src/components/particle-background.tsx"
      provides: "Mobile-aware particle initialization"
      contains: "useMediaQuery('(max-width: 768px)')"
      contains_alt: "isMobile ? 45 : 90"
  key_links:
    - from: "src/components/particle-background.tsx"
      to: "src/hooks/use-media-query.ts"
      via: "useMediaQuery import + subscription"
      pattern: "useMediaQuery\\(['\"]\\(max-width: 768px\\)['\"]\\)"
    - from: "particle-background useEffect deps"
      to: "isMobile boolean"
      via: "added dependency forces reinit on breakpoint cross"
      pattern: "\\[isDark, mounted, isMobile\\]"
---

<objective>
Reduce particle count on mobile (<768px) from 90 to 45 to eliminate visible jank during the VoiceBus breathing rAF loop, per MOB-01 and the locked decisions in 26-CONTEXT.md and 26-UI-SPEC.md.

Purpose: Mobile devices struggle with the 90-particle config; halving the count (per CONTEXT.md D-MOB-01: "45, mid-point of 40-50 target") restores smoothness without changing palette, density area, line distance, or move speed.
Output: Updated `src/components/particle-background.tsx` that subscribes to a 768px matchMedia query via the existing `useMediaQuery` hook and switches `number.value` between 90 (>=768px) and 45 (<768px), with the existing teardown sequence handling reinit on breakpoint cross.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-mobile-ux-pass/26-CONTEXT.md
@.planning/phases/26-mobile-ux-pass/26-UI-SPEC.md
@src/components/particle-background.tsx
@src/hooks/use-media-query.ts

<interfaces>
<!-- Existing hook contract that this plan consumes unchanged -->

From src/hooks/use-media-query.ts:
```typescript
export function useMediaQuery(query: string): boolean
```
Behavior: SSR-safe (returns false on server / before mount), subscribes via `media.addEventListener('change', listener)`, cleans up on unmount.

From src/components/particle-background.tsx (current shape, lines 50-203):
- `'use client'` component, exports `ParticleBackground()`
- Uses `useMounted()` and `useTheme()` -> `isDark`
- Single `useEffect` keyed on `[isDark, mounted]` that:
  1. Calls `ensureParticlesScript()` then `init()`
  2. Inside init: tears down stale `pJSDom` instances, removes old canvas, then calls `window.particlesJS('pf-particles', { particles: { number: { value: 90, density: { enable: true, value_area: 900 } }, ... } })`
  3. Wires the VoiceBus breathing rAF loop via `waitForInst()` after particlesJS is initialized
  4. Cleanup cancels `__vmTick` rAF on unmount
- Hardcoded `number.value: 90` lives at line 84.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add mobile particle count switch via useMediaQuery</name>
  <files>src/components/particle-background.tsx</files>
  <read_first>
    - src/components/particle-background.tsx (full file — understand current useEffect deps, teardown sequence at lines 71-80, particlesJS config at lines 82-106, breathing rAF at lines 108-194)
    - src/hooks/use-media-query.ts (full file — confirm SSR-safe contract: returns false before mount)
    - .planning/phases/26-mobile-ux-pass/26-CONTEXT.md (decisions section — confirm 45 mobile / 90 desktop / 768px breakpoint locked)
    - .planning/phases/26-mobile-ux-pass/26-UI-SPEC.md (Interaction & Motion Contract → MOB-01 table — confirm value_area unchanged at 900, line distance unchanged at 150, move speed unchanged at 1.2, no fps cap, reinit required on breakpoint cross)
  </read_first>
  <action>
    Modify `src/components/particle-background.tsx` exactly as follows:

    1. Add import at top of file (alongside the existing `useMounted` import on line 5):
       ```typescript
       import { useMediaQuery } from '@/hooks/use-media-query';
       ```

    2. Inside `ParticleBackground()` function, immediately after the existing `const isDark = mounted && resolvedTheme === 'dark';` line (currently line 54), add:
       ```typescript
       const isMobile = useMediaQuery('(max-width: 768px)');
       ```

    3. Change the `number.value` literal on line 84 (`number: { value: 90, density: { enable: true, value_area: 900 } }`) to use the locked mobile/desktop split:
       ```typescript
       number: { value: isMobile ? 45 : 90, density: { enable: true, value_area: 900 } },
       ```
       Per CONTEXT.md D-MOB-01: 45 on mobile, 90 on tablet+desktop. `value_area: 900` MUST stay 900 unchanged (UI-SPEC: "Density value_area: unchanged (900)").

    4. Update the `useEffect` dependency array at line 203 from `[isDark, mounted]` to `[isDark, mounted, isMobile]`. This forces the existing teardown-and-reinit cycle (lines 73-80: destroy pJSDom instances → clear array → remove canvas → call particlesJS again) to fire when the user crosses the 768px boundary, exactly as required by UI-SPEC ("Reinitialization on breakpoint cross: YES").

    DO NOT change:
    - `density.value_area` (locked at 900)
    - `line_linked.distance` (locked at 150)
    - `move.speed` (locked at 1.2)
    - The breathing rAF loop logic (lines 111-194) — UI-SPEC: "no fps cap, no throttle"
    - The palette object or any color logic
    - The teardown order (destroy → clear → remove canvas → call particlesJS)

    DO NOT introduce:
    - `window.innerWidth` polling or `resize` event listeners (forbidden by UI-SPEC: "Do NOT use innerWidth polling, do NOT use useEffect with resize listener directly")
    - VisualViewport usage
    - Any new fps cap or throttle on the breathing loop

    Reference UI-SPEC MOB-01 table for the full locked contract.
  </action>
  <verify>
    <automated>cd "/Users/lakshmanturlapati/Documents/Codes/Projekt A/Portfolio V2/Code/portfolio_v2" && grep -n "useMediaQuery" src/components/particle-background.tsx && grep -n "isMobile ? 45 : 90" src/components/particle-background.tsx && grep -n "\[isDark, mounted, isMobile\]" src/components/particle-background.tsx && npx tsc --noEmit -p .</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "useMediaQuery('(max-width: 768px)')" src/components/particle-background.tsx` returns >= 1
    - `grep -c "isMobile ? 45 : 90" src/components/particle-background.tsx` returns 1
    - `grep -c "value_area: 900" src/components/particle-background.tsx` returns 1 (proves density unchanged)
    - `grep -c "distance: 150" src/components/particle-background.tsx` returns 1 (proves line distance unchanged)
    - `grep -c "speed: 1.2" src/components/particle-background.tsx` returns 1 (proves move speed unchanged)
    - `grep -E "\\[isDark, mounted, isMobile\\]" src/components/particle-background.tsx` matches exactly once on the useEffect deps line
    - `grep -c "innerWidth" src/components/particle-background.tsx` returns 0 (forbidden detection method)
    - `grep -c "addEventListener\\(['\\\"]resize" src/components/particle-background.tsx` returns 0 (forbidden detection method)
    - `npx tsc --noEmit` exits 0 (no type errors after adding `isMobile` to deps)
  </acceptance_criteria>
  <done>
    `particle-background.tsx` imports `useMediaQuery`, derives `isMobile` from `'(max-width: 768px)'`, sets `number.value` to `isMobile ? 45 : 90`, and includes `isMobile` in the useEffect dependency array. All other particles config (density, line distance, move speed, breathing rAF loop, teardown sequence) preserved byte-for-byte. TypeScript compiles cleanly.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual mobile particle smoothness verification</name>
  <files>(no source files modified — this task is a manual human verification gate; references the files modified by prior auto tasks in this plan)</files>
  <action>This is a checkpoint task. The executor presents the &lt;what-built&gt; summary and &lt;how-to-verify&gt; steps to the user, then halts and waits for the &lt;resume-signal&gt;. The executor MUST NOT proceed to subsequent plans until the user types the resume signal. See &lt;how-to-verify&gt; below for the exact manual steps the user must run.</action>
  <verify>
    <automated>echo "MISSING — checkpoint is a human verification gate; manual steps in &lt;how-to-verify&gt; are the verification. Resume only when user types the signal in &lt;resume-signal&gt;."</automated>
  </verify>
  <done>User has executed the &lt;how-to-verify&gt; steps and typed the &lt;resume-signal&gt; ("approved" or a description of regressions). If regressions are reported, executor returns to the failing auto task and re-implements before re-checkpointing.</done>
<what-built>
    `ParticleBackground` now subscribes to `(max-width: 768px)` and emits 45 particles on mobile vs. 90 on tablet/desktop. Reinit fires on breakpoint cross.
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev` (do NOT auto-start; ask user to start it).
    2. Open `http://localhost:3000/` in a desktop browser. Verify ~90 particles render (count is approximate due to particles.js density). No console errors.
    3. Open DevTools → Toggle device toolbar (Cmd/Ctrl+Shift+M) → select iPhone SE (375px). Hard reload. Verify particle density is visibly sparser (~45). No console errors. No orphaned `<canvas>` elements (inspect `#pf-particles`).
    4. With device toolbar still open, drag the responsive width slider from 1200px down past 768px to 700px. Verify the canvas reinitializes (a brief flicker is acceptable) and particle count drops. No console errors.
    5. Drag back from 700px to 1200px. Verify count returns to ~90.
    6. On mobile viewport, click the voice button (or trigger any VoiceBus.level > 0 path). Verify the breathing rAF loop still modulates opacity/line distance smoothly with 45 particles — no stutter, no console errors.
    7. Switch theme (dark ↔ light) on mobile viewport. Verify reinit still works at the reduced count.
  </how-to-verify>
  <resume-signal>Type "approved" if mobile renders ~45 particles smoothly and desktop preserves 90, or describe any visual/console regressions.</resume-signal>
</task>

</tasks>

<verification>
- `grep` checks in Task 1 acceptance criteria all pass.
- TypeScript compiles cleanly.
- Manual checkpoint (Task 2) confirms mobile/desktop particle counts and smooth breathing rAF.
</verification>

<success_criteria>
- User on iPhone SE / iPhone 14 Pro sees 45 particles render smoothly (MOB-01 success criterion #1 from ROADMAP).
- User on iPad portrait (768px) and desktop (>=1024px) sees 90 particles unchanged.
- Resizing across 768px triggers clean reinit with no orphaned canvases or console errors.
- VoiceBus breathing loop continues to modulate opacity/line distance smoothly at the reduced count.
</success_criteria>

<output>
After completion, create `.planning/phases/26-mobile-ux-pass/26-01-SUMMARY.md` documenting:
- Files modified (`src/components/particle-background.tsx`)
- The 3 changes (import, `isMobile` derivation, count split, deps array)
- Manual verification result from Task 2
- Confirmation that no forbidden patterns (innerWidth polling, resize listener, fps cap) were introduced
</output>
