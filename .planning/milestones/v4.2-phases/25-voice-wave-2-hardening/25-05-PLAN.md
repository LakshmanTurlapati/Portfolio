---
phase: 25-voice-wave-2-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 25-04
files_modified:
  - src/lib/voice-controller.ts
autonomous: true
requirements:
  - VOICE-07
tags:
  - voice
  - timeout
  - safari

must_haves:
  truths:
    - "The SpeechSynthesis fallback path inside streamTTS arms a worst-case timeout of `Math.max(1000, text.length * 50)` ms capped at 30000 ms"
    - "On guard fire: synth.cancel() is called and finishSynth() is invoked so the speak() Promise resolves and the queue drains"
    - "u.onend and u.onerror clear the guard timer BEFORE calling finishSynth to avoid double-finalize"
    - "synthGuardRef is cleared inside cancelAllAudio so a stop-during-fallback-speak doesn't leave a stale timer"
  artifacts:
    - path: "src/lib/voice-controller.ts"
      provides: "synthGuardRef + clearSynthGuard() + worst-case timeout in synth fallback"
      contains: "synthGuardRef"
  key_links:
    - from: "streamTTS .catch synth fallback (line 326+)"
      to: "synthGuardRef timer + finishSynth"
      via: "guard armed after speechUtteranceRef.current = u; cleared by onend/onerror; on fire calls synth.cancel() + finishSynth()"
      pattern: "synthGuardRef"
    - from: "cancelAllAudio"
      to: "clearSynthGuard()"
      via: "guard cleanup composes with existing audio cancel path"
      pattern: "clearSynthGuard"
---

<objective>
Add a worst-case timeout guard to the SpeechSynthesis fallback path inside `streamTTS` (src/lib/voice-controller.ts:326-364). The guard duration is `Math.min(30000, Math.max(1000, text.length * 50))` ms. On fire: call `synth.cancel()` then `finishSynth()` so the queue drains and the speak() Promise resolves. Clear the guard inside `u.onend` and `u.onerror` BEFORE the existing `finishSynth` fires (wrap finishSynth so onend/onerror clear the timer first). Add `clearSynthGuard()` to `cancelAllAudio` so an external cancel mid-fallback doesn't leave a stale timer. Closes VOICE-07.

Purpose: Safari (and any browser where SpeechSynthesis silently no-ops) can fail to fire `onend` / `onerror`, leaving the speak() Promise hanging forever. The text-length-aware timeout (~50ms/char + 1s floor, capped at 30s) ensures bounded resolution.

Output: src/lib/voice-controller.ts gains a `synthGuardRef` ref, a `clearSynthGuard()` helper, the guard arm inside the synth fallback, wrapped onend/onerror handlers that clear the guard, and a clearSynthGuard call inside cancelAllAudio.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/25-voice-wave-2-hardening/25-CONTEXT.md
@.planning/phases/25-voice-wave-2-hardening/25-RESEARCH.md
@src/lib/voice-controller.ts
@.planning/phases/25-voice-wave-2-hardening/25-04-SUMMARY.md

<interfaces>
Existing synth fallback inside streamTTS .catch (lines 326-364):

```
.catch((err) => {
  if (ac.signal.aborted || (err instanceof Error && err.name === 'AbortError')) return;
  console.warn('[VoiceController] streamTTS failed, falling back to SpeechSynthesis:', err);
  const synth = window.speechSynthesis;
  if (!synth) {
    window.VoiceBus.setState('idle');
    speakingRef.current = false;
    if (speakResolverRef.current === resolve) speakResolverRef.current = null;
    speakAbortRef.current = null;
    resolve();
    return;
  }
  try { synth.cancel(); } catch {}
  const u = new SpeechSynthesisUtterance(text);
  u.rate = 1.05;
  u.pitch = 1.0;
  u.volume = 1.0;
  speechUtteranceRef.current = u;
  window.VoiceBus.setState('speaking');
  window.VoiceBus.attachTTSFake(u);
  const finishSynth = () => {
    if (speechUtteranceRef.current === u) {
      window.VoiceBus.setState('idle');
      speakingRef.current = false;
      speechUtteranceRef.current = null;
      speakAbortRef.current = null;
      if (speakResolverRef.current === resolve) speakResolverRef.current = null;
    }
    resolve();
  };
  u.onend = finishSynth;
  u.onerror = finishSynth;
  speakingRef.current = true;
  synth.speak(u);
});
```

Identity-check pattern: finishSynth has `if (speechUtteranceRef.current === u)` -- the second call no-ops on state mutation. This is the safety net.

Plan 04 added sessionGuardRef + clearSessionGuard. This plan adds synthGuardRef + clearSynthGuard alongside.

cancelAllAudio (after Plan 04) starts with `clearSessionGuard()`. This plan adds `clearSynthGuard()` immediately after.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add synthGuardRef + clearSynthGuard helper, integrate into cancelAllAudio</name>
  <files>src/lib/voice-controller.ts</files>
  <read_first>
    - src/lib/voice-controller.ts ref declarations area (post-Plan-04, sessionGuardRef now exists alongside the others)
    - src/lib/voice-controller.ts cancelAllAudio body (post-Plan-04, starts with clearSessionGuard())
    - .planning/phases/25-voice-wave-2-hardening/25-RESEARCH.md (Pitfall 3 "SpeechSynthesis onend/onerror race with guard timer", Pitfall 6 "Removing cancelAllAudio() from inside synth fallback's onend", "Synth guard inline with existing finishSynth" code example)
    - .planning/phases/25-voice-wave-2-hardening/25-CONTEXT.md (VOICE-07 timeout formula: Math.max(1000, text.length * 50), 30000 cap)
  </read_first>
  <action>
    Step A: Add `synthGuardRef` declaration immediately AFTER `sessionGuardRef` (added in Plan 04). The declaration block becomes:

    ```
      // VOICE-06: guard timer that fires if Scribe accepts the connection but never
      // emits SESSION_STARTED. Cleared in SESSION_STARTED / AUTH_ERROR / ERROR /
      // outer catch / cancelAllAudio. On fire: close Scribe, fall back to Web Speech.
      const sessionGuardRef = useRef<ReturnType<typeof setTimeout> | null>(null);

      // VOICE-07: worst-case timeout for the SpeechSynthesis fallback path. Some
      // browsers (notably Safari with disabled synth) silently no-op synth.speak()
      // and never fire onend/onerror. Duration is text-length-aware (50ms/char,
      // 1s floor, 30s cap). Cleared in onend/onerror and from cancelAllAudio.
      const synthGuardRef = useRef<ReturnType<typeof setTimeout> | null>(null);
    ```

    Step B: Add a `clearSynthGuard` helper inside `useVoiceController` immediately AFTER `clearSessionGuard` (added in Plan 04, just above cancelAllAudio):

    ```
      // VOICE-07: clear the SpeechSynthesis fallback worst-case timer if armed.
      // Idempotent -- safe to call from onend/onerror, the guard's own fire path,
      // and cancelAllAudio.
      const clearSynthGuard = () => {
        if (synthGuardRef.current !== null) {
          clearTimeout(synthGuardRef.current);
          synthGuardRef.current = null;
        }
      };
    ```

    Step C: INSIDE `cancelAllAudio`, immediately AFTER the `clearSessionGuard();` call (added in Plan 04) and BEFORE the existing `try { speakAbortRef.current?.abort(); } catch {}` line, add:

    ```
        // VOICE-07: clear synth fallback guard so a stop-during-speak doesn't
        // leave a stale timer firing into a torn-down speak() Promise.
        clearSynthGuard();
    ```

    Result -- the head of cancelAllAudio after this task should look like:

    ```
      const cancelAllAudio = useCallback(() => {
        // VOICE-06: clear Scribe stall guard so a stop-while-connecting doesn't
        // leave a 5s timer firing into a torn-down session.
        clearSessionGuard();

        // VOICE-07: clear synth fallback guard so a stop-during-speak doesn't
        // leave a stale timer firing into a torn-down speak() Promise.
        clearSynthGuard();

        // Abort any in-flight /api/tts fetch so a stale .then doesn't create another source
        try { speakAbortRef.current?.abort(); } catch {}
        // ... rest unchanged ...
    ```

    Deps array stays `[]`.

    Do NOT modify the synth fallback itself in this task -- Task 2 covers that.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint -- src/lib/voice-controller.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "const synthGuardRef = useRef<ReturnType<typeof setTimeout> | null>(null);" src/lib/voice-controller.ts` returns exactly one match
    - `grep -n "const clearSynthGuard = () =>" src/lib/voice-controller.ts` returns exactly one match
    - `grep -c "clearSynthGuard();" src/lib/voice-controller.ts` returns at least 1 match (cancelAllAudio site, with more added in Task 2)
    - synthGuardRef declared AFTER sessionGuardRef: `awk '/const sessionGuardRef/{s=NR} /const synthGuardRef/{y=NR} END{exit !(s<y)}' src/lib/voice-controller.ts` exits 0
    - clearSynthGuard defined AFTER clearSessionGuard: `awk '/const clearSessionGuard = /{s=NR} /const clearSynthGuard = /{y=NR} END{exit !(s<y)}' src/lib/voice-controller.ts` exits 0
    - In cancelAllAudio, clearSynthGuard is called AFTER clearSessionGuard but BEFORE the speakAbortRef abort: requires both `clearSessionGuard();` and `clearSynthGuard();` calls present in that order before `speakAbortRef.current?.abort()`. Verify: `awk '/const cancelAllAudio = useCallback/{flag=1} flag && /clearSessionGuard\(\)/{ses=NR} flag && /clearSynthGuard\(\)/{syn=NR} flag && /speakAbortRef.current\?.abort/{abr=NR; exit} END{exit !(ses<syn && syn<abr)}' src/lib/voice-controller.ts` exits 0
    - `grep -c "VOICE-07" src/lib/voice-controller.ts` returns at least 3 (declaration comment, helper comment, cancelAllAudio inline comment)
    - `npm run lint -- src/lib/voice-controller.ts` exits 0
  </acceptance_criteria>
  <done>
    synthGuardRef declared after sessionGuardRef; clearSynthGuard helper defined after clearSessionGuard; cancelAllAudio calls both clearSessionGuard then clearSynthGuard before existing audio cleanup; lint clean.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Arm worst-case timeout in synth fallback and wrap onend/onerror</name>
  <files>src/lib/voice-controller.ts</files>
  <read_first>
    - src/lib/voice-controller.ts streamTTS .catch synth fallback body (lines 326-364)
    - .planning/phases/25-voice-wave-2-hardening/25-RESEARCH.md (Pitfall 3 onend/onerror race -- exact wrap pattern, "Synth guard inline with existing finishSynth" code example, Pitfall 6 -- do NOT use cancelAllAudio inside onend)
    - .planning/phases/25-voice-wave-2-hardening/25-CONTEXT.md (formula: Math.max(1000, text.length * 50), capped at 30000)
  </read_first>
  <action>
    Modify the synth fallback inside `streamTTS .catch` (lines 326-364 of src/lib/voice-controller.ts). Replace the existing handler-assignment lines (currently `u.onend = finishSynth;` and `u.onerror = finishSynth;` at lines 360-361) with:

    1. Arm the guard timer immediately AFTER `speakingRef.current = true;` (line 362) and BEFORE `synth.speak(u);` (line 363). Insert the guard arm so the timer is armed BEFORE speech starts:

    ```
          // VOICE-07: worst-case timeout for synth.speak. Some browsers silently
          // no-op without firing onend/onerror -- this guard ensures finishSynth
          // is called and the speak() Promise resolves. Cleared by onend/onerror.
          const guardMs = Math.min(30000, Math.max(1000, text.length * 50));
          synthGuardRef.current = setTimeout(() => {
            synthGuardRef.current = null;
            try { synth.cancel(); } catch {}
            finishSynth(); // identity-checked finalizer -- safe even if onend already fired
          }, guardMs);
    ```

    2. Replace the bare `u.onend = finishSynth;` and `u.onerror = finishSynth;` at lines 360-361 with wrapped versions that clear the guard FIRST, then delegate to finishSynth. Place these BEFORE the new guard arm above (so onend/onerror are wired before the timer is armed) -- final order in the .catch body becomes:

    ```
          speechUtteranceRef.current = u;
          window.VoiceBus.setState('speaking');
          window.VoiceBus.attachTTSFake(u);
          const finishSynth = () => {
            if (speechUtteranceRef.current === u) {
              window.VoiceBus.setState('idle');
              speakingRef.current = false;
              speechUtteranceRef.current = null;
              speakAbortRef.current = null;
              if (speakResolverRef.current === resolve) speakResolverRef.current = null;
            }
            resolve();
          };
          // VOICE-07: wrap onend/onerror to clear the worst-case guard before
          // calling finishSynth. Identity check inside finishSynth is the final
          // safety net if both fire (RESEARCH.md Pitfall 3).
          const wrappedFinishSynth = () => {
            clearSynthGuard();
            finishSynth();
          };
          u.onend = wrappedFinishSynth;
          u.onerror = wrappedFinishSynth;
          speakingRef.current = true;
          // VOICE-07: worst-case timeout (50ms/char, 1s floor, 30s cap).
          const guardMs = Math.min(30000, Math.max(1000, text.length * 50));
          synthGuardRef.current = setTimeout(() => {
            synthGuardRef.current = null;
            try { synth.cancel(); } catch {}
            finishSynth();
          }, guardMs);
          synth.speak(u);
    ```

    Do NOT call `cancelAllAudio()` from inside the guard fire path or onend/onerror -- that's a sledgehammer per RESEARCH.md Pitfall 6 (it would also abort an unrelated in-flight TTS fetch). Just clearSynthGuard + finishSynth.

    Do NOT modify the no-synth early-return branch (lines 333-340) -- that path resolves immediately without ever speaking, no guard needed.

    Do NOT modify any state outside this synth-fallback block.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run lint -- src/lib/voice-controller.ts && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "Math.min(30000, Math.max(1000, text.length \\* 50))" src/lib/voice-controller.ts` returns exactly one match -- formula EXACTLY matches CONTEXT.md decision
    - `grep -n "const wrappedFinishSynth = () =>" src/lib/voice-controller.ts` returns exactly one match
    - `grep -n "u.onend = wrappedFinishSynth;" src/lib/voice-controller.ts` returns exactly one match
    - `grep -n "u.onerror = wrappedFinishSynth;" src/lib/voice-controller.ts` returns exactly one match
    - The bare assignments are gone: `grep -n "u.onend = finishSynth;" src/lib/voice-controller.ts` returns ZERO matches; `grep -n "u.onerror = finishSynth;" src/lib/voice-controller.ts` returns ZERO matches
    - The guard arm calls synth.cancel() before finishSynth: `awk '/synthGuardRef.current = setTimeout/{flag=1; next} flag && /synth.cancel\(\)/{c=NR} flag && /finishSynth\(\);/{f=NR} flag && /\}, guardMs\);/{exit} END{exit !(c<f)}' src/lib/voice-controller.ts` exits 0
    - cancelAllAudio is NOT called inside the synth fallback: `awk '/streamTTS = useCallback/{flag=1} flag && /cancelAllAudio/{print NR; flag=2; exit}' src/lib/voice-controller.ts` -- the only cancelAllAudio reference inside streamTTS is the existing entry-point call at line 263 (BEFORE the .catch); search inside the .catch block specifically with `awk '/falling back to SpeechSynthesis/{flag=1} flag && /\}\);$/{print NR; flag=0; exit} flag && /cancelAllAudio/{print "FOUND"; exit}' src/lib/voice-controller.ts` should not print "FOUND"
    - `clearSynthGuard()` total count: `grep -c "clearSynthGuard();" src/lib/voice-controller.ts` returns exactly 2 (one in cancelAllAudio from Task 1, one inside wrappedFinishSynth)
    - The 50ms/char and 30s cap are documented in a comment near the formula: `grep -n "50ms/char\\|50ms / char" src/lib/voice-controller.ts` returns at least one match
    - `npm run lint -- src/lib/voice-controller.ts` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    Synth fallback has worst-case timeout matching the exact CONTEXT.md formula; onend/onerror wrapped to clear guard first; cancelAllAudio not called from inside fallback; identity-check safety net preserved; build + lint clean.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none new) | Internal timeout that bounds an existing browser failure mode; no new external surface |

## STRIDE Threat Register

threats: []

(No new threats. The timeout REDUCES a denial-of-service-on-self risk where the user's voice mode could hang indefinitely on Safari.)
</threat_model>

<verification>
Manual smoke (post-execute, optional): start dev (`npm run dev`), open voice mode. To force the synth fallback path, monkey-patch `fetch` in DevTools to fail for `/api/tts` (`fetch = (url) => url.includes('/api/tts') ? Promise.reject(new Error('forced')) : originalFetch(url)`). Ask Parz a short question -- the fallback should kick in, speak via system TTS, and `voiceState` should return to `idle` within `text.length * 50` ms after speech ends. To force the worst-case guard fire, also patch `SpeechSynthesisUtterance.prototype.onend` to be a no-op so synth's onend never propagates -- after at most 30s the guard should fire and idle the state.

Automated: `npm run build` + `npm run lint`.
</verification>

<success_criteria>
- [ ] synthGuardRef declared with other guard refs
- [ ] clearSynthGuard helper defined
- [ ] cancelAllAudio calls clearSessionGuard then clearSynthGuard before existing audio cleanup
- [ ] Worst-case timeout formula exact: `Math.min(30000, Math.max(1000, text.length * 50))`
- [ ] Guard arm calls synth.cancel() then finishSynth()
- [ ] u.onend and u.onerror wrapped to clear guard first (wrappedFinishSynth)
- [ ] cancelAllAudio is NOT called from inside the synth fallback
- [ ] `npm run lint` and `npm run build` both succeed
</success_criteria>

<output>
After completion, create `.planning/phases/25-voice-wave-2-hardening/25-05-SUMMARY.md` describing what was implemented and the file:line of the guard arm + each clear site.
</output>
