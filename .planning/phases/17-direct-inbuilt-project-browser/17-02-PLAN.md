---
phase: 17
plan: 02
title: Portfolio direct browser opening and tool copy alignment
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - src/app/portfolio/page.tsx
  - src/app/api/chat/route.ts
autonomous: true
requirements:
  - BROW-01
  - BROW-02
  - BROW-04
  - BROW-05
  - SAFE-06
must_haves:
  - Project card clicks open the inbuilt browser directly
  - ProjectDetail is removed from the primary portfolio path
  - Voice/project tool opening on portfolio uses the same direct browser path
---

<objective>
Wire portfolio project cards and portfolio-local `openProject` tool callbacks to the canonical resolver and inbuilt browser, removing the right-side detail panel from the primary project experience.
</objective>

<tasks>
<task id="01" type="execute">
  <title>Replace portfolio detail-panel path with direct browser opening</title>
  <read_first>
    - .planning/phases/17-direct-inbuilt-project-browser/17-01-PLAN.md
    - src/app/portfolio/page.tsx
    - src/data/projects.ts
    - src/components/iframe-viewer.tsx
  </read_first>
  <files>
    - src/app/portfolio/page.tsx
  </files>
  <action>
    Update `src/app/portfolio/page.tsx` so `openProject(project)` uses `getProjectBrowserTarget(project)` and sets `viewer` directly.

    Remove `selectedProject` state, remove the `ProjectDetail` import, and remove the `ProjectDetail` render block. The right-side project detail panel must not appear from manual card clicks.
  </action>
  <acceptance_criteria>
    - `src/app/portfolio/page.tsx` does not import `ProjectDetail`.
    - `src/app/portfolio/page.tsx` does not render `<ProjectDetail`.
    - Manual `PortfolioCard` `onOpen` calls set `IframeViewer` state directly.
    - The portfolio grid/card layout classes remain visually unchanged.
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Update portfolio openProject tool callback</title>
  <read_first>
    - src/app/portfolio/page.tsx
    - src/data/projects.ts
    - src/lib/voice-commands.ts
  </read_first>
  <files>
    - src/app/portfolio/page.tsx
  </files>
  <action>
    Update the registered `openProject` voice callback so the provided `slug` resolves through `resolveProject(slug)` and opens the direct browser target via the same `openProject(project)` function.

    If no project or target is found, do not open anything arbitrary. A no-op with a console warning is acceptable for Phase 17 because user-visible global fallback messaging belongs to Phase 18's site-control layer.
  </action>
  <acceptance_criteria>
    - The portfolio `openProject` callback calls `resolveProject`.
    - The callback does not call `setSelectedProject`.
    - The callback opens the direct browser target for aliases such as `Parz-AI`, `FSB`, and `GitFly` when the portfolio page is mounted.
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Align chat tool guidance with approved project browser behavior</title>
  <read_first>
    - src/app/api/chat/route.ts
    - .planning/REQUIREMENTS.md
    - .planning/phases/17-direct-inbuilt-project-browser/17-CONTEXT.md
  </read_first>
  <files>
    - src/app/api/chat/route.ts
  </files>
  <action>
    Update the voice tool instructions and `openProject` tool description so they describe opening an approved project browser target rather than opening a project detail view or arbitrary URL.

    Do not implement Phase 18 global site control in this plan; keep wording compatible with later direct any-page opening.
  </action>
  <acceptance_criteria>
    - `src/app/api/chat/route.ts` no longer describes `openProject` as opening a project detail view.
    - `src/app/api/chat/route.ts` tells the model to use approved project names/aliases, not arbitrary URLs.
    - `openLink` remains separate and does not become the project-opening path.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run lint`.
- Confirm `src/app/portfolio/page.tsx` has no `ProjectDetail` import or render path.
- Confirm `src/app/portfolio/page.tsx` uses `resolveProject` and `getProjectBrowserTarget`.
- Confirm `src/app/api/chat/route.ts` tool wording references approved project targets.
</verification>

<success_criteria>
- User clicking a project card opens the inbuilt browser directly.
- Portfolio-local Parz/voice project opening uses canonical project aliases and direct browser targets.
- The right-side detail panel is no longer part of the primary project experience.
- Project opening remains allowlist/local-record based.
</success_criteria>

<must_haves>
- Covers BROW-01, BROW-02, BROW-04, BROW-05, and SAFE-06.
- Preserves current portfolio visual design.
</must_haves>
