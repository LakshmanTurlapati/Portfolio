---
phase: 09-chat-about-and-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/chat/page.tsx
  - src/components/chat-popup.tsx
  - src/data/system-prompt.ts
autonomous: true
requirements:
  - CHAT-01
  - CHAT-02
  - CHAT-03

must_haves:
  truths:
    - "When the AI API returns an error, the user sees a casual Parz-voice message, not a raw error string"
    - "Both chat/page.tsx and chat-popup.tsx use the same friendly error array"
    - "Suggestion chips (1 small + 1 big) appear at conversation start and disappear after 2 user messages in both chat surfaces"
    - "Rotating loading messages cycle every 3 seconds in both chat surfaces"
    - "Parz can answer questions about all 21 portfolio projects — DATA_STORE names match projects.ts names"
  artifacts:
    - path: "src/app/chat/page.tsx"
      provides: "Error display with Parz-persona random message, verified chips and loading"
    - path: "src/components/chat-popup.tsx"
      provides: "Same error treatment as chat/page.tsx"
    - path: "src/data/system-prompt.ts"
      provides: "DATA_STORE with accurate project coverage matching all 21 projects in projects.ts"
  key_links:
    - from: "src/app/chat/page.tsx"
      to: "PARZ_ERRORS array"
      via: "getRandomItem(PARZ_ERRORS) on error render"
    - from: "src/components/chat-popup.tsx"
      to: "PARZ_ERRORS array"
      via: "same getRandomItem pattern"
    - from: "src/data/system-prompt.ts"
      to: "src/data/projects.ts"
      via: "project name cross-reference audit"
---

<objective>
Replace raw `error.message` display in both chat surfaces with a random Parz-persona friendly error message. Verify suggestion chips and loading messages work correctly in both. Audit DATA_STORE project names against projects.ts and fix any mismatches so Parz can answer questions about all 21 projects.

Purpose: Complete CHAT-01 (full DATA_STORE), CHAT-02 (friendly errors + loading), and CHAT-03 (suggestion chips) per D-01 through D-07.
Output: Updated chat/page.tsx, chat-popup.tsx, and system-prompt.ts.
</objective>

<execution_context>
@/Users/lakshman/Documents/Misc/P V3/.planning/phases/09-chat-about-and-polish/09-CONTEXT.md
</execution_context>

<context>
@/Users/lakshman/Documents/Misc/P V3/.planning/ROADMAP.md
@/Users/lakshman/Documents/Misc/P V3/.planning/phases/09-chat-about-and-polish/09-CONTEXT.md
@/Users/lakshman/Documents/Misc/P V3/src/app/chat/page.tsx
@/Users/lakshman/Documents/Misc/P V3/src/components/chat-popup.tsx
@/Users/lakshman/Documents/Misc/P V3/src/data/system-prompt.ts
@/Users/lakshman/Documents/Misc/P V3/src/data/projects.ts

<interfaces>
<!-- Key contracts the executor needs. Read before implementing. -->

From src/app/chat/page.tsx (lines 81-85):
```typescript
const { messages, sendMessage, status, error } = useChat({ onError: () => {} });
```
`error` is of type `Error | undefined`. Currently displayed as `{error.message}` (lines 250-252).

From src/components/chat-popup.tsx (lines 82-86):
```typescript
const { messages, sendMessage, status, error } = useChat({ onError: () => {} });
```
Same pattern. Currently displayed as `{error.message}` (lines 362-364).

Existing helper already in both files:
```typescript
function getRandomItem<T>(arr: T[]): T {
  return arr[Math.floor(Math.random() * arr.length)];
}
```

From src/data/projects.ts — the 21 canonical project names (must match DATA_STORE):
"Review Gate", "Blockchain Smartcontracts", "Smart Fabric using IOT", "Portfolio",
"Financial Inclusion", "LinkedIn Auto Connect", "Service Portal", "X-Read",
"Heartline", "Lucent", "Parz-AI", "awsxUTD-Hackathon", "T2S CLI", "Star-Trail-Flutter",
"awsxutd", "Open-API", "ArtScii", "FSB", "Asteroids Game", "ProKeys", "SmolLM Flutter"

From src/data/system-prompt.ts — DATA_STORE project name coverage (requires cross-reference):
- "Review-Gate (V2)" → portfolio name is "Review Gate" (name mismatch — Parz may not match)
- "t2s-cli" → portfolio name is "T2S CLI" (mismatch)
- "Smart Fabric using IOT" → "Smart Fabric using IOT" (match)
- "Parz-AI" → "Parz-AI" (match)
- "SmolLM Flutter" → "SmolLM Flutter" (match)
- Missing by DATA_STORE category: "Asteroids Multiplayer" vs "Asteroids Game" (mismatch)
- "LinkedIn Auto Connect" → "linkedin-autoconnect-extension" (mismatch)
- "open-api" → "Open-API" (mismatch)
- "awsxutd" → "awsxutd" (likely match)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Parz-persona error messages and fix error display in both chat surfaces (D-01, D-02, D-03)</name>
  <read_first>
    - src/app/chat/page.tsx — lines 240-253 (error display block)
    - src/components/chat-popup.tsx — lines 349-367 (error display block)
  </read_first>
  <files>src/app/chat/page.tsx, src/components/chat-popup.tsx</files>
  <action>
    In BOTH files (chat/page.tsx and chat-popup.tsx), replace the raw `{error.message}` display with a randomly selected Parz-persona message.

    Step 1 — Add error messages array near the top of EACH file (after the loadingMessages array, before the getRandomItem helper):

    ```typescript
    // Parz-persona error messages (random, never show technical error.message)
    const PARZ_ERRORS = [
      "Ah, my brain glitched for a sec. Try again?",
      "Server's taking a nap. Give it another shot.",
      "Something's off on my end, hit me again.",
      "Whoops, lost my train of thought. One more time?",
      "Got a hiccup on my side. Try that again.",
      "Ran into a wall there. Mind asking again?",
      "My gears jammed. One more try should do it.",
    ];
    ```

    Step 2 — In each component, add a state variable to hold the selected error message:
    ```typescript
    const [currentError, setCurrentError] = useState<string | null>(null);
    ```

    Step 3 — When `error` changes to a truthy value, pick a random message. Add a useEffect in each component:
    ```typescript
    useEffect(() => {
      if (error) {
        setCurrentError(getRandomItem(PARZ_ERRORS));
      } else {
        setCurrentError(null);
      }
    }, [error]);
    ```

    Step 4 — Replace the error render block in each file:
    - chat/page.tsx: Replace `<p className="text-sm">{error.message}</p>` with `<p className="text-sm">{currentError}</p>` and change the condition from `{error && (` to `{currentError && (`
    - chat-popup.tsx: Same pattern — replace `{error.message}` with `{currentError}` and guard on `currentError` instead of `error`

    No other changes. The red error bubble styling stays identical.
  </action>
  <verify>
    TypeScript compile: `cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit 2>&1 | head -20`
  </verify>
  <acceptance_criteria>
    - PARZ_ERRORS array with 7 entries appears in both chat/page.tsx and chat-popup.tsx
    - `currentError` state is declared in both component functions
    - useEffect watching `[error]` sets currentError from PARZ_ERRORS in both files
    - Error display renders `{currentError}` not `{error.message}` in both files
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Both chat surfaces show a random Parz-voice error message instead of a raw technical error string on API failure</done>
</task>

<task type="auto">
  <name>Task 2: Audit DATA_STORE project names against projects.ts and fix mismatches (D-04, D-05)</name>
  <read_first>
    - src/data/system-prompt.ts — full file (213 lines), focus on "projects" section (lines 117-172)
    - src/data/projects.ts — pinnedProjects and shuffleableProjects arrays (lines 47-72) for the 21 canonical names
  </read_first>
  <files>src/data/system-prompt.ts</files>
  <action>
    Cross-reference each project name in projects.ts against the DATA_STORE in system-prompt.ts. The goal is that when a user asks Parz about any portfolio project by its displayed name, the DATA_STORE entry is recognizable.

    Known mismatches to fix (edit the DATA_STORE JSON string in system-prompt.ts):

    1. "Review-Gate (V2)" → change `"name": "Review-Gate (V2)"` to `"name": "Review Gate"` (and keep V2 detail in description)
    2. "t2s-cli" → change `"name": "t2s-cli"` to `"name": "T2S CLI"`
    3. "Asteroids Multiplayer" → change `"name": "Asteroids Multiplayer"` to `"name": "Asteroids Game"`
    4. "linkedin-autoconnect-extension" → change to `"name": "LinkedIn Auto Connect"`
    5. "open-api" → change to `"name": "Open-API"`
    6. "DCTE-Script (X-Read)" → change to `"name": "X-Read"`
    7. "awsxUTD-Hackathon" → verify the name matches exactly; the portfolio uses "awsxUTD-Hackathon"

    After fixing names, verify the DATA_STORE contains all 21 project names by doing a mental cross-reference:
    - Review Gate (was Review-Gate V2) ✓
    - Blockchain Smartcontracts ✓ (check if present as-is)
    - Smart Fabric using IOT ✓
    - Portfolio ✓
    - Financial Inclusion ✓
    - LinkedIn Auto Connect (was linkedin-autoconnect-extension) ✓
    - Service Portal ✓
    - X-Read (was DCTE-Script X-Read) ✓
    - Heartline ✓
    - Lucent ✓
    - Parz-AI ✓
    - awsxUTD-Hackathon ✓
    - T2S CLI (was t2s-cli) ✓
    - Star-Trail-Flutter ✓
    - awsxutd ✓
    - Open-API (was open-api) ✓
    - ArtScii ✓
    - FSB ✓
    - Asteroids Game (was Asteroids Multiplayer) ✓
    - ProKeys ✓
    - SmolLM Flutter ✓

    If any of the 21 names is not yet present in the DATA_STORE at all, add a minimal entry to the appropriate category:
    ```json
    {"name": "PROJECT_NAME", "url": "GITHUB_URL", "description": "BRIEF_DESCRIPTION"}
    ```
    (Match the GitHub URLs from projects.ts links.)

    Also verify D-05: confirm these DATA_STORE top-level keys exist: bio/biography, education, professionalExperience, projects, interestsAndPersonality (hobbies), philosophyAndWorkEthic. They already exist — just confirm no key is missing.
  </action>
  <verify>
    TypeScript compile: `cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit 2>&1 | head -20`
    Name check: `grep -o '"name": "[^"]*"' "/Users/lakshman/Documents/Misc/P V3/src/data/system-prompt.ts" | sort`
  </verify>
  <acceptance_criteria>
    - All 21 project names in the DATA_STORE match their displayed names in projects.ts exactly
    - No "Review-Gate (V2)", "t2s-cli", "Asteroids Multiplayer", "DCTE-Script (X-Read)", "linkedin-autoconnect-extension", or "open-api" entries remain in the DATA_STORE
    - TypeScript compiles without errors
    - The DATA_STORE JSON remains valid (no syntax errors in the template literal)
  </acceptance_criteria>
  <done>Parz can answer questions about all 21 portfolio projects using their displayed names</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → /api/chat | User messages reach the AI API route; errors flow back to client |
| system-prompt.ts → API route | Server-side only; never reaches client bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Information Disclosure | Error display (chat/page.tsx, chat-popup.tsx) | mitigate | Replace raw error.message with opaque Parz-persona strings — no stack traces, API messages, or internal details leak to UI (D-01 to D-03) |
| T-09-02 | Tampering | system-prompt.ts DATA_STORE | accept | Server-side only file; not in client bundle; no user input touches it |
| T-09-03 | Information Disclosure | system-prompt.ts loaded in API route | accept | Route is server-side only; API key already protected via env var in existing /api/chat route |
</threat_model>

<verification>
After execution:
1. `cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit` — must produce no errors
2. `grep -c "PARZ_ERRORS" "/Users/lakshman/Documents/Misc/P V3/src/app/chat/page.tsx"` — must return >= 2
3. `grep -c "PARZ_ERRORS" "/Users/lakshman/Documents/Misc/P V3/src/components/chat-popup.tsx"` — must return >= 2
4. `grep "error\.message" "/Users/lakshman/Documents/Misc/P V3/src/app/chat/page.tsx"` — must return nothing (no more raw error.message in JSX)
5. `grep "error\.message" "/Users/lakshman/Documents/Misc/P V3/src/components/chat-popup.tsx"` — must return nothing
6. `grep -o '"name": "[^"]*"' "/Users/lakshman/Documents/Misc/P V3/src/data/system-prompt.ts"` — inspect list confirms all 21 project names present
</verification>

<success_criteria>
- Both chat surfaces display Parz-persona error messages, never raw technical errors
- DATA_STORE project names match exactly with projects.ts displayed names (all 21 covered)
- Suggestion chips (1 small + 1 big) appear and disappear after 2 messages — verified by code inspection
- Loading messages cycle every 3s — verified by code inspection (already implemented)
- TypeScript build is clean
</success_criteria>

<output>
After completion, create `/Users/lakshman/Documents/Misc/P V3/.planning/phases/09-chat-about-and-polish/09-01-SUMMARY.md` with:
- What was changed in each file
- The 7 Parz error messages added
- Which project name mismatches were fixed
- TypeScript compile result
</output>
