---
phase: 18-global-parz-site-control
plan: 03
type: execute
wave: 3
depends_on: ["01", "02"]
files_modified:
  - src/app/api/chat/route.ts
  - src/lib/voice-controller.ts
  - src/providers/voice-session-provider.tsx
  - src/app/chat/page.tsx
  - src/components/chat-popup.tsx
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03, CTRL-04, CTRL-05]
user_setup: []
must_haves:
  truths:
    - Voice Parz can call global navigation, project opening, section scrolling, browser close, external-open, and limitation actions.
    - Text Parz can receive and dispatch the same site-control tool calls from chat page and popup.
    - Unsupported third-party iframe control requests receive honest limitation copy.
  artifacts:
    - src/app/api/chat/route.ts exposes site-control tools when requested.
    - src/lib/voice-controller.ts handles browser shell tools without routing project opens through portfolio.
    - src/app/chat/page.tsx and src/components/chat-popup.tsx pass site-control mode and dispatch tool parts.
  key_links:
    - VoiceSessionProvider registers global control callbacks from useSiteControl.
    - Chat clients use useSiteControl for returned tool calls.
---

<objective>
Connect Parz text and voice tool calls to the global control surface.

Purpose: Users can ask Parz from home, portfolio, about, or chat to control the portfolio shell with the same safe behavior.
Output: shared API tool schema updates, voice dispatch updates, and text chat dispatch wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-global-parz-site-control/18-CONTEXT.md
@.planning/phases/18-global-parz-site-control/18-UI-SPEC.md
@src/providers/site-control-provider.tsx
@src/providers/voice-session-provider.tsx
@src/lib/voice-controller.ts
@src/app/api/chat/route.ts
@src/app/chat/page.tsx
@src/components/chat-popup.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose safe site-control tools from chat API</name>
  <files>src/app/api/chat/route.ts</files>
  <read_first>src/app/api/chat/route.ts, .planning/phases/18-global-parz-site-control/18-UI-SPEC.md</read_first>
  <action>Rename the existing tool instruction string to a site-control instruction string usable by both text and voice. Add tools `closeBrowser`, `openCurrentProjectExternal`, and `unsupportedIframeControl`. Add instruction text that says: project opening must use approved aliases only; close/open-external only control the portfolio browser shell; arbitrary third-party iframe control is unsupported and must use the limitation response. Parse request body as `{ messages, isVoice, enableSiteControl }`; append instructions and pass tools when `isVoice || enableSiteControl` is true.</action>
  <verify>Grep `src/app/api/chat/route.ts` for `enableSiteControl`, `closeBrowser`, `openCurrentProjectExternal`, and `unsupportedIframeControl`.</verify>
  <acceptance_criteria>
    - `src/app/api/chat/route.ts` contains `enableSiteControl`.
    - `src/app/api/chat/route.ts` contains `closeBrowser`.
    - `src/app/api/chat/route.ts` contains `openCurrentProjectExternal`.
    - `src/app/api/chat/route.ts` contains `unsupportedIframeControl`.
    - `src/app/api/chat/route.ts` contains `I can control the portfolio shell`.
  </acceptance_criteria>
  <done>API exposes the complete safe site-control tool contract for text and voice modes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire voice tools to global control</name>
  <files>src/lib/voice-controller.ts, src/providers/voice-session-provider.tsx</files>
  <read_first>src/lib/voice-controller.ts, src/providers/voice-session-provider.tsx, src/providers/site-control-provider.tsx</read_first>
  <action>Extend `ToolCallbacks` with `closeBrowser?: () => ControlResult`, `openCurrentProjectExternal?: () => ControlResult`, and `unsupportedIframeControl?: () => ControlResult`. Update `dispatchToolCall` to emit executing/success/error for these callbacks. In the AI tool-call loop, remove the `goPage('portfolio')` and `waitForPage('portfolio')` sequence for `openProject`; call `dispatchToolCall('openProject', { slug: name })` directly. Add cases for `closeBrowser`, `openCurrentProjectExternal`, and `unsupportedIframeControl`. In `VoiceSessionProvider`, import `useSiteControl`, read its functions, and install them into `toolCallbacksRef.current` together with `toggleTheme` and `openLink`.</action>
  <verify>Grep voice files for direct openProject dispatch and new shell tool callback names.</verify>
  <acceptance_criteria>
    - `src/lib/voice-controller.ts` contains `closeBrowser?:`.
    - `src/lib/voice-controller.ts` contains `openCurrentProjectExternal?:`.
    - `src/lib/voice-controller.ts` contains `unsupportedIframeControl?:`.
    - `src/lib/voice-controller.ts` does not contain `Navigate to portfolio first, then open project`.
    - `src/providers/voice-session-provider.tsx` contains `useSiteControl`.
  </acceptance_criteria>
  <done>Voice tool calls use global control without a portfolio detour.</done>
</task>

<task type="auto">
  <name>Task 3: Dispatch text chat site-control tool calls</name>
  <files>src/app/chat/page.tsx, src/components/chat-popup.tsx</files>
  <read_first>src/app/chat/page.tsx, src/components/chat-popup.tsx, src/providers/site-control-provider.tsx</read_first>
  <action>In both chat clients, import `useSiteControl`, call `useChat({ body: { enableSiteControl: true }, onError })`, and add an effect that scans assistant `message.parts` for AI SDK tool invocation parts. Handle part types that include tool names for `navigate`, `openProject`, `scrollTo`, `closeBrowser`, `openCurrentProjectExternal`, and `unsupportedIframeControl`. Dispatch each completed invocation once using a `useRef<Set<string>>` of handled IDs. Use defensive casts so unsupported part shapes are ignored without throwing. Do not redesign chat UI.</action>
  <verify>Grep both chat files for `enableSiteControl`, `useSiteControl`, and `handledToolCallsRef`.</verify>
  <acceptance_criteria>
    - `src/app/chat/page.tsx` contains `enableSiteControl: true`.
    - `src/app/chat/page.tsx` contains `handledToolCallsRef`.
    - `src/components/chat-popup.tsx` contains `enableSiteControl: true`.
    - `src/components/chat-popup.tsx` contains `handledToolCallsRef`.
    - Both files contain `unsupportedIframeControl`.
  </acceptance_criteria>
  <done>Text chat page and popup can execute returned site-control tool calls through the global provider.</done>
</task>

</tasks>

<verification>
- [ ] API exposes the complete safe tool contract.
- [ ] Voice openProject no longer navigates to portfolio first.
- [ ] Text chat and popup dispatch site-control tool calls.
- [ ] `npm run lint` passes.
</verification>

<success_criteria>
- CTRL-01 through CTRL-05 are covered by text and voice Parz control paths.
- Unsupported third-party iframe control is honest and non-mutating.
</success_criteria>

<output>
After completion, create `.planning/phases/18-global-parz-site-control/18-03-SUMMARY.md`.
</output>
