---
phase: 18-global-parz-site-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/providers/site-control-provider.tsx
  - src/app/layout.tsx
autonomous: true
requirements: [CTRL-01, CTRL-04, CTRL-05]
user_setup: []
must_haves:
  truths:
    - Approved project aliases can open a global inbuilt browser from any route without navigating to portfolio.
    - Browser shell close and external-open actions operate only on the active approved browser target.
    - Unsupported third-party iframe control returns an honest limitation instead of mutating state.
  artifacts:
    - src/providers/site-control-provider.tsx exists.
    - src/app/layout.tsx imports and renders SiteControlProvider.
  key_links:
    - SiteControlProvider uses resolveProject and getProjectBrowserTarget from src/data/projects.ts.
    - SiteControlProvider renders IframeViewer from src/components/iframe-viewer.tsx.
---

<objective>
Create the global site-control provider and browser shell state.

Purpose: Parz needs one portfolio-shell control surface available from home, portfolio, about, chat, and voice without a portfolio-page detour.
Output: `SiteControlProvider`, `useSiteControl`, global `IframeViewer` rendering, and layout integration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-global-parz-site-control/18-CONTEXT.md
@.planning/phases/18-global-parz-site-control/18-UI-SPEC.md
@src/data/projects.ts
@src/components/iframe-viewer.tsx
@src/providers/transition-provider.tsx
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add global site-control provider</name>
  <files>src/providers/site-control-provider.tsx</files>
  <read_first>src/data/projects.ts, src/components/iframe-viewer.tsx, src/providers/transition-provider.tsx, src/providers/theme-provider.tsx</read_first>
  <action>Create `src/providers/site-control-provider.tsx` as a client component. Export `type ControlPage = 'home' | 'portfolio' | 'about'`, `type ControlSection = 'about' | 'experience' | 'academics'`, `interface ControlResult { ok: boolean; message: string }`, `SiteControlProvider`, and `useSiteControl`. The provider must import `resolveProject`, `getProjectBrowserTarget`, `IframeViewer`, `useTransition`, `usePathname`, and `useTheme`. Implement `navigate(page)`, `openProject(input)`, `scrollTo(section)`, `closeBrowser()`, `openCurrentProjectExternal()`, `unsupportedIframeControl()`, and `registerAboutScroller(scroller)`. Track active browser state as `{ url: string; label: string; projectName: string } | null`. `openProject` must resolve only local project records and return `{ ok: false, message: "I couldn't find an approved project target for that." }` if no approved target exists. `unsupportedIframeControl` must return `{ ok: false, message: "I can control the portfolio shell, but I can't operate arbitrary controls inside a third-party iframe." }`.</action>
  <verify>Grep `src/providers/site-control-provider.tsx` for `resolveProject`, `getProjectBrowserTarget`, `IframeViewer`, `unsupportedIframeControl`, and `registerAboutScroller`.</verify>
  <acceptance_criteria>
    - `src/providers/site-control-provider.tsx` contains `export function SiteControlProvider`.
    - `src/providers/site-control-provider.tsx` contains `export function useSiteControl`.
    - `src/providers/site-control-provider.tsx` contains `resolveProject`.
    - `src/providers/site-control-provider.tsx` contains `getProjectBrowserTarget`.
    - `src/providers/site-control-provider.tsx` contains `I can control the portfolio shell, but I can't operate arbitrary controls inside a third-party iframe.`
  </acceptance_criteria>
  <done>Global provider exposes all required control functions and renders the browser shell from approved state.</done>
</task>

<task type="auto">
  <name>Task 2: Mount provider in app layout</name>
  <files>src/app/layout.tsx</files>
  <read_first>src/app/layout.tsx, src/providers/site-control-provider.tsx</read_first>
  <action>Import `SiteControlProvider` from `@/providers/site-control-provider` in `src/app/layout.tsx`. Wrap `{children}`, `<VoiceOverlay />`, and `<VoiceGlow />` inside `<SiteControlProvider>` within the existing `<VoiceSessionProvider>` so all routes and persistent voice UI can access global control. Do not alter font, metadata, theme, transition, or voice bus configuration.</action>
  <verify>Run `npm run lint` after implementation.</verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` contains `import { SiteControlProvider } from '@/providers/site-control-provider';`.
    - `src/app/layout.tsx` contains `<SiteControlProvider>`.
    - `src/app/layout.tsx` contains `</SiteControlProvider>`.
  </acceptance_criteria>
  <done>Root layout mounts the global site-control provider around route content and persistent voice UI.</done>
</task>

</tasks>

<verification>
- [ ] `src/providers/site-control-provider.tsx` exists and exports `SiteControlProvider` and `useSiteControl`.
- [ ] `src/app/layout.tsx` wraps content in `SiteControlProvider`.
- [ ] `npm run lint` passes.
</verification>

<success_criteria>
- All tasks completed.
- CTRL-01, CTRL-04, and CTRL-05 foundation is present.
- No arbitrary model-generated project URL can open through the global browser shell.
</success_criteria>

<output>
After completion, create `.planning/phases/18-global-parz-site-control/18-01-SUMMARY.md`.
</output>
