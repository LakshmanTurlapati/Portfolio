---
phase: 18-global-parz-site-control
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified:
  - src/app/about/page.tsx
  - src/app/portfolio/page.tsx
autonomous: true
requirements: [CTRL-01, CTRL-02, CTRL-03]
user_setup: []
must_haves:
  truths:
    - Global section scroll can reach About, Experience, and Academics from another page.
    - Portfolio manual project opening still uses the inbuilt browser direct path.
    - Existing page-local voice callback wiring no longer conflicts with global control.
  artifacts:
    - src/app/about/page.tsx registers the about scroller with SiteControlProvider.
    - src/app/portfolio/page.tsx no longer needs portfolio-only openProject registration for global voice control.
  key_links:
    - About page uses `registerAboutScroller` from `useSiteControl`.
    - Section aliases map education to academics.
---

<objective>
Wire page-level navigation and section scroll behavior to the global control surface.

Purpose: Parz must navigate precisely and scroll to about-page sections even when the request starts outside the about page.
Output: about-page global scroll registration and cleanup of obsolete page-local tool callbacks.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-global-parz-site-control/18-CONTEXT.md
@src/providers/site-control-provider.tsx
@src/app/about/page.tsx
@src/app/portfolio/page.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register about-page section scroller globally</name>
  <files>src/app/about/page.tsx</files>
  <read_first>src/app/about/page.tsx, src/providers/site-control-provider.tsx</read_first>
  <action>Import `useSiteControl` in `src/app/about/page.tsx`. Replace page-local `useVoiceSession` `registerToolCallbacks({ scrollTo })` registration with `registerAboutScroller((section) => scrollToSection(section))`. Preserve the current alias handling in the provider by supporting only section ids `about`, `experience`, and `academics` in the callback. Keep `VoiceBus.emit('page-ready', 'about')` unchanged.</action>
  <verify>Grep `src/app/about/page.tsx` for `useSiteControl` and `registerAboutScroller`.</verify>
  <acceptance_criteria>
    - `src/app/about/page.tsx` contains `useSiteControl`.
    - `src/app/about/page.tsx` contains `registerAboutScroller`.
    - `src/app/about/page.tsx` does not contain `registerToolCallbacks({`.
    - `src/app/about/page.tsx` still contains `VoiceBus.emit('page-ready', 'about')`.
  </acceptance_criteria>
  <done>About page exposes its internal scroll container through the global control provider.</done>
</task>

<task type="auto">
  <name>Task 2: Keep portfolio direct browser behavior page-local only</name>
  <files>src/app/portfolio/page.tsx</files>
  <read_first>src/app/portfolio/page.tsx, src/providers/site-control-provider.tsx, src/data/projects.ts</read_first>
  <action>Remove portfolio-page `useVoiceSession` registration for `openProject` if it only exists to serve voice callbacks. Preserve manual card `onOpen={openProject}` behavior and direct `IframeViewer` rendering on the portfolio page. Do not reintroduce `ProjectDetail` or right-side detail-panel behavior.</action>
  <verify>Grep `src/app/portfolio/page.tsx` for absence of `registerToolCallbacks` and presence of `onOpen={openProject}` and `IframeViewer`.</verify>
  <acceptance_criteria>
    - `src/app/portfolio/page.tsx` does not contain `registerToolCallbacks`.
    - `src/app/portfolio/page.tsx` contains `onOpen={openProject}`.
    - `src/app/portfolio/page.tsx` contains `<IframeViewer`.
  </acceptance_criteria>
  <done>Portfolio page remains direct-browser for manual cards and no longer owns global project-opening callbacks.</done>
</task>

</tasks>

<verification>
- [ ] About page registers the global section scroller.
- [ ] Portfolio page keeps direct browser card behavior.
- [ ] `npm run lint` passes.
</verification>

<success_criteria>
- CTRL-02 and CTRL-03 have global wiring.
- CTRL-01 remains direct-browser and safe.
</success_criteria>

<output>
After completion, create `.planning/phases/18-global-parz-site-control/18-02-SUMMARY.md`.
</output>
