---
phase: 24
plan: 01
title: Mobile Pass + Voice Stabilization (post-Phase-23)
type: execute
wave: 3
depends_on: [23]
files_modified:
  - src/lib/voice-controller.ts
  - src/components/ask-parz-button.tsx
  - src/components/portfolio-button.tsx
  - src/components/mobile-navbar.tsx
  - src/components/voice-overlay.tsx
  - src/components/voice-panel.tsx
  - src/components/voice-wave.tsx
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/api/chat/route.ts
  - src/app/api/tts/route.ts
  - src/app/api/stt-token/route.ts
autonomous: true
requirements: []
---

<objective>
Land three follow-ons after Phase 23 deployed: (1) finish unsticking TTS so Parz actually speaks instead of hanging at "thinking" / "speaking", (2) make the mobile site feel right (navbar overlap, iOS safe-area, compact voice panel), (3) remove the temporary diagnostic API logging that supported this diagnosis. No new requirements; this is stabilization + polish.
</objective>

<tasks>

<task id="1" type="execute">
<read_first>
- src/lib/voice-controller.ts (`open`, `streamTTS`, the speak/catch tail of `handleUserTurn`)
</read_first>
<action>
Voice TTS unstick. Three layered fixes:
- Pre-warm `window.VoiceBus._getCtx()` synchronously inside `open()` so the AudioContext is created (or resumed) while the click gesture is still alive.
- In `streamTTS` `.then`, after `_getCtx()` and before `decodeAudioData`, `await ctx.resume()` if `ctx.state === 'suspended'`.
- Replace `else if (toolCalls.length === 0) { setState('idle'); setCaption(''); }` with a plain `else` so any path with no `clean` text exits 'thinking', not just no-tool paths.
</action>
<acceptance_criteria>
- `open()` body (inside the 480ms timer wrapper) calls `_getCtx()` synchronously before scheduling `handleUserTurn(trigger, { kind: 'greet' })`.
- `streamTTS` `.then` chain has `if (ctx.state === 'suspended') { try { await ctx.resume(); } catch {} }` immediately after `_getCtx()` and before `decodeAudioData`.
- The empty-text branch of `handleUserTurn` is `else`, not `else if (toolCalls.length === 0)`.
</acceptance_criteria>
</task>

<task id="2" type="execute">
<read_first>
- src/components/ask-parz-button.tsx
- src/components/portfolio-button.tsx
- src/components/mobile-navbar.tsx
- src/components/voice-overlay.tsx
- src/components/voice-panel.tsx
- src/components/voice-wave.tsx
</read_first>
<action>
Mobile navbar pass.
- AskParzButton: add `variant: 'desktop' | 'mobile'` (default 'desktop'). Mobile uses `position: relative` (no `right: 132 top-1/2`), hides the "Parz" label, and uses `padding: 0 10px`. Desktop unchanged.
- PortfolioButton mobile variant: `<Image width={64} height={16}>` (was 112×28) and `overflow-hidden` on the inner button so `scale-[1.4]` can't bleed past the slot.
- Mobile-navbar passes `variant="mobile"` to `<AskParzButton>` and `compact` to `<VoicePanel>`.
- Voice-overlay mobile branch passes `compact` to `<VoicePanel>`.
- VoicePanel: new `compact` prop. Tighter padding (`0 10px 0 14px` vs `0 14px 0 22px`), smaller gap (`10px` vs `16px`). Forwards `compact` to `<VoiceWave>`.
- VoiceWave: new `compact` prop. 60×32 with 6 px bars (vs 88×40 with 8 px bars).
</action>
<acceptance_criteria>
- AskParzButton has `variant` in props, mobile branch renders without `absolute right: 132 top-1/2`, label hidden, padding `0 10px`.
- PortfolioButton mobile renders Image at 64×16 inside an `overflow-hidden` button.
- VoicePanel + VoiceWave both accept `compact` and adjust dimensions.
- mobile-navbar.tsx and voice-overlay.tsx (mobile branch only) pass `compact` to `<VoicePanel>`.
</acceptance_criteria>
</task>

<task id="3" type="execute">
<read_first>
- src/app/layout.tsx
- src/app/page.tsx
- src/components/mobile-navbar.tsx
- src/components/voice-overlay.tsx
</read_first>
<action>
iOS safe-area inset support.
- Add `export const viewport: Viewport = { width: 'device-width', initialScale: 1, viewportFit: 'cover' }` to `src/app/layout.tsx`. This is required for iOS to expose `env(safe-area-inset-*)`.
- Mobile-navbar: `bottom: max(20px, env(safe-area-inset-bottom))`.
- Voice-overlay mobile branch: same `bottom` formula.
- Mobile AuthorName + ThemeToggle wrappers on `app/page.tsx`: `top: calc(env(safe-area-inset-top) + 20px)`. Desktop wrappers unchanged.
</action>
<acceptance_criteria>
- `app/layout.tsx` exports `viewport` with `viewportFit: 'cover'`.
- `mobile-navbar.tsx` `<nav>` and SSR placeholder both use `style={{ bottom: 'max(20px, env(safe-area-inset-bottom))' }}` instead of `bottom-[20px]`.
- `voice-overlay.tsx` mobile branch wrapper uses the same `bottom` formula.
- `app/page.tsx` mobile theme-toggle and author-name wrappers use `style={{ top: 'calc(env(safe-area-inset-top) + 20px)' }}` instead of `top-5`.
</acceptance_criteria>
</task>

<task id="4" type="execute">
<read_first>
- src/app/api/chat/route.ts
- src/app/api/tts/route.ts
- src/app/api/stt-token/route.ts
</read_first>
<action>
Remove the temporary diagnostic `console.warn` logging that was added to the three voice API routes during the TTS-unstick diagnosis. Diagnostic served its purpose (confirmed server-side was healthy and the bug was client-side AudioContext); now reverted so the routes match the pre-diagnosis shape.
</action>
<acceptance_criteria>
- `grep -n 'console\.warn' src/app/api/{chat,tts,stt-token}/route.ts` returns no matches.
- `grep -n 't0 = Date\.now' src/app/api/` returns no matches.
- Empty-catch block returns to `} catch {` (no `(err)` parameter).
- Routes still return the same 503 / 400 / 500 / 200 responses for the same inputs as before logging was added.
</acceptance_criteria>
</task>

<task id="5" type="verify">
<action>
Typecheck, vitest, lint, Next build. Manual mobile smoke on the deployed test portfolio after the next `fly deploy --remote-only`.
</action>
<acceptance_criteria>
- `npx tsc --noEmit` clean.
- `npx vitest run` 12/12 passing.
- `npx next build` succeeds.
- `npx next lint --dir src` shows no new warnings on touched files.
- Live test on `https://portfolio-v4-test.fly.dev/` confirms: Parz speaks on greet, Parz speaks on user turn, navbar slots don't overlap on phones, AuthorName + ThemeToggle clear the iOS notch.
</acceptance_criteria>
</task>

</tasks>

<deviations>
- Filed retroactively. The work was performed across five commits across multiple deploy iterations as the user reported each follow-on issue. This phase document describes the work as it actually happened.
- No automated tests added. None of the changes touch persona / safety / project-resolution covered by Phase 20's contract suite. The TTS-unstick + autoplay-policy interaction is timing- and browser-dependent and would require mocking AudioContext, fetch, and event timing simultaneously to reproduce in a unit test — high cost, low value.
- Diagnostic logging cleanup is folded into this phase (rather than a separate phase) because it's a direct revert of code added in the same investigation window.
</deviations>
