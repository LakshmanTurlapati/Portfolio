---
phase: 01-app-shell-and-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - postcss.config.mjs
  - src/app/globals.css
  - src/app/layout.tsx
  - src/providers/theme-provider.tsx
  - src/lib/cn.ts
  - src/hooks/use-media-query.ts
  - src/hooks/use-mounted.ts
  - src/types/index.ts
  - public/icons/portfolio.png
  - public/icons/portfolio_light.png
autonomous: true
requirements:
  - FOUN-01
  - FOUN-02
  - FOUN-05

must_haves:
  truths:
    - "Next.js dev server starts without errors"
    - "Lato font loads with no layout shift and all 4 weights available"
    - "System dark/light preference is detected on first page load"
    - "Theme class toggles on html element between dark and light"
    - "Tailwind CSS v4 custom breakpoint at 600px is active"
  artifacts:
    - path: "package.json"
      provides: "Project configuration with all Phase 1 dependencies"
      contains: "next-themes"
    - path: "src/app/globals.css"
      provides: "Tailwind v4 CSS-first config with design tokens"
      contains: "@custom-variant dark"
    - path: "src/app/layout.tsx"
      provides: "Root layout with font, theme provider, metadata"
      contains: "ThemeProvider"
    - path: "src/providers/theme-provider.tsx"
      provides: "Client-side theme provider wrapping next-themes"
      contains: "use client"
    - path: "src/lib/cn.ts"
      provides: "Utility for conditional Tailwind class merging"
      exports: ["cn"]
    - path: "postcss.config.mjs"
      provides: "PostCSS config for Tailwind v4"
      contains: "@tailwindcss/postcss"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/providers/theme-provider.tsx"
      via: "import ThemeProvider"
      pattern: "import.*ThemeProvider.*from.*providers/theme-provider"
    - from: "src/app/layout.tsx"
      to: "next/font/google"
      via: "Lato font import"
      pattern: "import.*Lato.*from.*next/font/google"
    - from: "src/app/globals.css"
      to: "tailwindcss"
      via: "@import directive"
      pattern: "@import.*tailwindcss"
---

<objective>
Initialize the Next.js 15 project with all Phase 1 dependencies and configure the foundation: Tailwind CSS v4 with CSS-first configuration, next-themes for dark/light mode with system detection, Lato font via next/font, and shared utility files.

Purpose: Establish the bootable app shell that all subsequent plans build upon. Without this foundation, no components can be created.
Output: A running Next.js dev server with correct theme detection, font loading, and Tailwind v4 configuration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-app-shell-and-navigation/01-CONTEXT.md
@.planning/phases/01-app-shell-and-navigation/01-RESEARCH.md
@.planning/phases/01-app-shell-and-navigation/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project and install all dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, postcss.config.mjs</files>
  <read_first>
    .planning/phases/01-app-shell-and-navigation/01-RESEARCH.md
  </read_first>
  <action>
Step 1: Create Next.js 15 project WITHOUT the --tailwind flag (it installs Tailwind v3, not v4).
Run from the project root directory:
```bash
npx create-next-app@15 . --typescript --eslint --app --src-dir --no-tailwind
```
If prompted about overwriting existing files, accept. The project root is the current directory.

Step 2: Install Tailwind CSS v4 manually:
```bash
npm install tailwindcss @tailwindcss/postcss postcss
```

Step 3: Install theme, icons, animation, and utility libraries:
```bash
npm install next-themes react-icons gsap @gsap/react clsx tailwind-merge
```

Step 4: Create PostCSS configuration. Write `postcss.config.mjs` at project root:
```javascript
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
```

Step 5: Verify package.json contains ALL required dependencies:
- next: 15.x
- tailwindcss: 4.x
- @tailwindcss/postcss
- next-themes
- react-icons
- gsap
- @gsap/react
- clsx
- tailwind-merge

Step 6: Copy portfolio button image assets from Flutter source to Next.js public directory:
```bash
mkdir -p public/icons
cp web/icons/portfolio.png public/icons/portfolio.png
cp web/icons/portfolio_light.png public/icons/portfolio_light.png
```

Step 7: If create-next-app generated a tailwind.config.js or tailwind.config.ts, DELETE it. Tailwind v4 uses CSS-first configuration only.

Step 8: Ensure next.config.ts exists and is minimal (no special config needed for Phase 1).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Projekt\ A/Portfolio\ V2/Code/portfolio_v2 && node -e "const pkg = require('./package.json'); const deps = {...pkg.dependencies, ...pkg.devDependencies}; const required = ['next', 'tailwindcss', '@tailwindcss/postcss', 'next-themes', 'react-icons', 'gsap', '@gsap/react', 'clsx', 'tailwind-merge']; const missing = required.filter(d => !deps[d]); if (missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All deps present'); const fs = require('fs'); if (fs.existsSync('tailwind.config.js') || fs.existsSync('tailwind.config.ts')) { console.error('ERROR: tailwind.config file exists -- must delete for v4'); process.exit(1); } if (!fs.existsSync('postcss.config.mjs')) { console.error('ERROR: postcss.config.mjs missing'); process.exit(1); } if (!fs.existsSync('public/icons/portfolio.png')) { console.error('ERROR: portfolio.png not copied'); process.exit(1); } console.log('All checks passed')"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "next-themes" in dependencies
    - package.json contains "tailwindcss" with version starting with "4" or "^4"
    - package.json contains "@tailwindcss/postcss" in dependencies
    - package.json contains "react-icons" in dependencies
    - package.json contains "gsap" in dependencies
    - package.json contains "clsx" in dependencies
    - package.json contains "tailwind-merge" in dependencies
    - postcss.config.mjs exists and contains "@tailwindcss/postcss"
    - No tailwind.config.js or tailwind.config.ts file exists
    - public/icons/portfolio.png exists
    - public/icons/portfolio_light.png exists
    - src/ directory exists with app/ subdirectory
  </acceptance_criteria>
  <done>Next.js 15 project initialized with all Phase 1 dependencies, Tailwind v4 PostCSS config, and portfolio button assets migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Configure root layout, theme provider, globals.css, and utility files</name>
  <files>src/app/globals.css, src/app/layout.tsx, src/providers/theme-provider.tsx, src/lib/cn.ts, src/hooks/use-media-query.ts, src/hooks/use-mounted.ts, src/types/index.ts</files>
  <read_first>
    .planning/phases/01-app-shell-and-navigation/01-UI-SPEC.md
    .planning/phases/01-app-shell-and-navigation/01-RESEARCH.md
    src/app/layout.tsx
    src/app/globals.css
  </read_first>
  <action>
Step 1: Create `src/providers/theme-provider.tsx` -- client component wrapping next-themes:
```typescript
'use client';

import { ThemeProvider as NextThemesProvider } from 'next-themes';
import type { ReactNode } from 'react';

export function ThemeProvider({ children }: { children: ReactNode }) {
  return (
    <NextThemesProvider
      attribute="class"
      defaultTheme="system"
      enableSystem
      disableTransitionOnChange={false}
    >
      {children}
    </NextThemesProvider>
  );
}
```

Step 2: Write `src/app/globals.css` with Tailwind v4 CSS-first configuration. Must contain:
- `@import "tailwindcss";`
- `@custom-variant dark (&:where(.dark, .dark *));`
- `@theme` block with `--breakpoint-sm: 37.5rem;` (600px / 16 = 37.5rem)
- `:root` block with all light mode CSS custom properties from UI-SPEC:
  - `--color-bg: #FFFFFF`
  - `--color-text: #000000`
  - `--color-navbar-bg: rgba(0, 0, 0, 0.8)`
  - `--color-navbar-text: #9E9E9E`
  - `--color-social-icon: #808080`
  - `--color-sun: #000000`
  - `--color-moon: rgba(0, 0, 0, 0.26)`
  - `--color-separator: rgba(0, 0, 0, 0.26)`
  - `--color-portfolio-btn-bg: #FFFFFF`
  - `--color-portfolio-btn-text: #000000`
  - `--color-author-text-shadow: rgba(0, 0, 0, 0.38)` (light mode: black38)
- `.dark` block with all dark mode CSS custom properties:
  - `--color-bg: #000000`
  - `--color-text: #FFFFFF`
  - `--color-navbar-bg: rgba(255, 255, 255, 0.8)`
  - `--color-navbar-text: #424242`
  - `--color-social-icon: #424242`
  - `--color-sun: #757575`
  - `--color-moon: #E0E0E0`
  - `--color-separator: #9E9E9E`
  - `--color-portfolio-btn-bg: #000000`
  - `--color-portfolio-btn-text: #FFFFFF`
  - `--color-author-text-shadow: rgba(0, 0, 0, 0.54)` (dark mode: black54)
- Background gradient utility classes:
  ```css
  .bg-gradient-main {
    background: linear-gradient(to right, #FFFFFF 5%, #D0D0D0 40%, #D0D0D0 60%, #FFFFFF 95%);
    transition: background 300ms ease;
  }
  .dark .bg-gradient-main {
    background: conic-gradient(from 0deg, #000000 0%, rgba(16,16,16,0.9) 20%, rgba(26,26,26,0.8) 40%, rgba(32,32,32,0.7) 60%, rgba(16,16,16,0.9) 80%, #000000 100%);
  }
  ```
- Body base styles: `font-family: var(--font-lato), sans-serif; background-color: var(--color-bg); color: var(--color-text);`

Step 3: Rewrite `src/app/layout.tsx` -- root layout with Lato font, theme provider, and metadata:
```typescript
import type { Metadata } from 'next';
import { Lato } from 'next/font/google';
import { ThemeProvider } from '@/providers/theme-provider';
import './globals.css';

const lato = Lato({
  subsets: ['latin'],
  weight: ['400', '500', '600', '700'],
  variable: '--font-lato',
  display: 'swap',
});

export const metadata: Metadata = {
  title: 'Portfolio v2',
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" className={lato.variable} suppressHydrationWarning>
      <body className="font-[family-name:var(--font-lato)] bg-[var(--color-bg)] text-[var(--color-text)]">
        <ThemeProvider>
          {children}
        </ThemeProvider>
      </body>
    </html>
  );
}
```

Step 4: Create `src/lib/cn.ts`:
```typescript
import { clsx, type ClassValue } from 'clsx';
import { twMerge } from 'tailwind-merge';

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs));
}
```

Step 5: Create `src/hooks/use-mounted.ts` -- SSR-safe mounted state guard:
```typescript
'use client';

import { useState, useEffect } from 'react';

export function useMounted() {
  const [mounted, setMounted] = useState(false);
  useEffect(() => setMounted(true), []);
  return mounted;
}
```

Step 6: Create `src/hooks/use-media-query.ts` -- custom hook for responsive breakpoint detection (used only when CSS-based responsive is insufficient):
```typescript
'use client';

import { useState, useEffect } from 'react';

export function useMediaQuery(query: string): boolean {
  const [matches, setMatches] = useState(false);

  useEffect(() => {
    const media = window.matchMedia(query);
    setMatches(media.matches);

    const listener = (event: MediaQueryListEvent) => setMatches(event.matches);
    media.addEventListener('change', listener);
    return () => media.removeEventListener('change', listener);
  }, [query]);

  return matches;
}
```

Step 7: Create `src/types/index.ts` -- shared type exports (barrel file, initially minimal):
```typescript
export interface NavLink {
  label: string;
  href: string;
}

export interface SocialLink {
  icon: string;
  url: string;
  label: string;
}
```

Step 8: Update `src/app/page.tsx` to use the bg-gradient-main class as a placeholder home page:
```typescript
export default function Home() {
  return (
    <main className="bg-gradient-main min-h-screen relative overflow-hidden">
      <div className="flex items-center justify-center min-h-screen">
        <p className="text-xl">Home</p>
      </div>
    </main>
  );
}
```

Step 9: Verify the dev server starts without errors by running `npm run build` (build validates TypeScript, Tailwind config, and module resolution).
  </action>
  <verify>
    <automated>cd /Users/lakshmanturlapati/Documents/Codes/Projekt\ A/Portfolio\ V2/Code/portfolio_v2 && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - src/app/globals.css contains "@import" and "tailwindcss"
    - src/app/globals.css contains "@custom-variant dark"
    - src/app/globals.css contains "--breakpoint-sm: 37.5rem"
    - src/app/globals.css contains "--color-bg: #FFFFFF"
    - src/app/globals.css contains "--color-bg: #000000" inside .dark block
    - src/app/globals.css contains "bg-gradient-main" class
    - src/app/globals.css contains "conic-gradient" for dark mode background
    - src/app/globals.css contains "linear-gradient" for light mode background
    - src/app/layout.tsx contains "import { Lato } from 'next/font/google'"
    - src/app/layout.tsx contains "weight: ['400', '500', '600', '700']"
    - src/app/layout.tsx contains "suppressHydrationWarning"
    - src/app/layout.tsx contains "ThemeProvider" import and usage
    - src/providers/theme-provider.tsx contains "'use client'"
    - src/providers/theme-provider.tsx contains 'attribute="class"'
    - src/providers/theme-provider.tsx contains 'defaultTheme="system"'
    - src/providers/theme-provider.tsx contains "enableSystem"
    - src/lib/cn.ts contains "twMerge(clsx(inputs))"
    - src/hooks/use-mounted.ts exists
    - src/hooks/use-media-query.ts exists
    - src/types/index.ts contains "export interface NavLink"
    - src/types/index.ts contains "export interface SocialLink"
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Root layout renders with Lato font (4 weights), ThemeProvider detects system preference, globals.css has all design tokens and gradient classes, utility files are in place. Build succeeds.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. globals.css contains `@import "tailwindcss"` and `@custom-variant dark`
3. globals.css contains `--breakpoint-sm: 37.5rem`
4. layout.tsx imports Lato with weights 400, 500, 600, 700
5. layout.tsx wraps children in ThemeProvider
6. ThemeProvider uses `attribute="class"`, `defaultTheme="system"`, `enableSystem`
7. No tailwind.config.js or tailwind.config.ts exists
8. postcss.config.mjs uses `@tailwindcss/postcss`
9. public/icons/portfolio.png and portfolio_light.png exist
</verification>

<success_criteria>
- Next.js 15 project builds successfully with zero errors
- All 9 required npm packages are installed (next, tailwindcss, @tailwindcss/postcss, next-themes, react-icons, gsap, @gsap/react, clsx, tailwind-merge)
- Tailwind CSS v4 is configured via CSS-first approach (no JS config file)
- Theme system detects system preference on load
- Lato font loads with 4 weights and no layout shift
- Design token CSS variables are defined for both light and dark modes
- Background gradient classes exist for both themes
</success_criteria>

<output>
After completion, create `.planning/phases/01-app-shell-and-navigation/01-01-SUMMARY.md`
</output>
