# Plan 30-01: Website Audit Remediation

**Phase:** 30 — Website Audit Remediation
**Status:** Complete
**Created:** 2026-06-11

## Goal

Fix the quick-audit findings except the explicitly deferred paid-API rate-limit redesign.

## Requirements Covered

- AUDIT-01
- DEP-01
- DEP-02
- LINK-01
- SEC-01
- UX-01
- LINT-01
- VERIFY-01

## Tasks

1. Make portfolio project ordering deterministic across SSR/client hydration.
2. Update dependency versions and lockfile so normal `npm ci` works and package audit advisories are addressed where safe.
3. Harden programmatic external `window.open` calls with `noopener,noreferrer`.
4. Repair or remove visible project links known to return 404/private responses.
5. Resize small controls flagged by the audit while preserving the existing visual direction.
6. Resolve existing lint warnings.
7. Run verification: `npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm audit --audit-level=moderate`, `npm run test:e2e`, plus rendered route smoke checks for hydration/page errors.

## Out of Scope

- Durable/shared paid-API rate-limit redesign.
- New portfolio projects or design-direction changes.

## Success Criteria

1. `/portfolio` emits no hydration mismatch page errors on desktop or mobile.
2. `npm ci` succeeds without `--legacy-peer-deps`.
3. Known dependency audit advisories are cleared or explicitly documented if upstream-gated.
4. External programmatic opens use `noopener,noreferrer`.
5. Known broken/private project links are no longer exposed in visible project actions.
6. Lint, unit tests, production build, audit, and e2e pass.

## Result

Complete on 2026-06-11.

- Deterministic project ordering and dot-matrix values removed hydration drift risk.
- Dependency versions/lockfile now support normal `npm ci` and `npm audit --audit-level=moderate` reports 0 vulnerabilities.
- Programmatic external opens are hardened.
- Known broken/private visible links from the audit were removed.
- Small sampled controls now meet practical clickable/tappable dimensions.
- Verification passed: `npm ci`, `npm run lint`, `npm test`, `npm run build`, `npm audit --audit-level=moderate`, `npm run test:e2e`, and rendered route smoke checks.
