---
phase: 08-voice-mode
plan: 05
type: execute
wave: 3
depends_on:
  - "08-03"
  - "08-04"
files_modified: []
autonomous: false
requirements:
  - VOIC-01
  - VOIC-02
  - VOIC-03
  - VOIC-04
  - VOIC-05

must_haves:
  truths:
    - "User sees the navbar visually morph into a voice capsule when Ask Parz is clicked"
    - "VoiceWave bars animate continuously and respond to voice activity"
    - "State chip dot color changes correctly across idle/listening/thinking/speaking"
    - "Particle mesh breathing is visible during voice activity"
    - "User can say a page name and navigate successfully"
    - "User can say 'text' or click the chat button to open ChatPopup"
    - "User can say 'stop' or click close to exit voice mode and restore the navbar"
  artifacts:
    - path: "src/app/api/tts/route.ts"
      provides: "Working TTS endpoint (verified by curl)"
    - path: "src/components/desktop-navbar.tsx"
      provides: "Morphing navbar (verified visually)"
---

<objective>
Human visual verification of the complete voice mode feature before Phase 8 is marked complete.

Purpose: Voice mode has complex visual states (morph animation, waveform, particle breathing) that require eyes-on verification. No automated test can confirm the GSAP Flip animation runs at 60fps or the waveform bars look natural.

Output: Developer confirmation that all 5 VOIC requirements pass the testing checklist from VOICE_HANDOFF.md.
</objective>

<execution_context>
@/Users/lakshman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lakshman/Documents/Misc/P V3/.planning/ROADMAP.md
@/Users/lakshman/Documents/Misc/P V3/.planning/phases/08-voice-mode/08-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated pre-flight checks</name>
  <files></files>

  <read_first>
    - /Users/lakshman/Documents/Misc/P V3/.planning/phases/08-voice-mode/08-03-SUMMARY.md
    - /Users/lakshman/Documents/Misc/P V3/.planning/phases/08-voice-mode/08-04-SUMMARY.md
  </read_first>

  <action>
Run these checks and report results before asking for human verification:

1. TypeScript clean:
   ```bash
   cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit 2>&1
   ```

2. Build succeeds:
   ```bash
   cd "/Users/lakshman/Documents/Misc/P V3" && npm run build 2>&1 | tail -30
   ```

3. All required voice files present:
   ```bash
   ls src/types/voice-bus.d.ts src/lib/voice-bus-init.ts src/providers/voice-bus-provider.tsx src/hooks/use-voice-bus.ts src/lib/voice-controller.ts src/lib/voice-commands.ts src/components/voice-wave.tsx src/components/voice-panel.tsx src/app/api/tts/route.ts
   ```

4. Key wiring verified:
   ```bash
   grep -n "VoiceBusProvider" src/app/layout.tsx
   grep -n "useVoiceController" src/app/page.tsx
   grep -n "waitForInst" src/components/particle-background.tsx
   grep -n "Flip.from" src/components/desktop-navbar.tsx
   ```

5. API key safety:
   ```bash
   grep -rn "ELEVENLABS_API_KEY" src/ | grep -v "api/tts/route.ts"
   ```
   (should return no matches — key only in the server route)

6. Start dev server:
   ```bash
   cd "/Users/lakshman/Documents/Misc/P V3" && npm run dev 2>&1 &
   ```

Report all results. If any check fails, fix before presenting the human verification task.
  </action>

  <verify>
    <automated>cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit && npm run build 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0 with "Route (app)" table showing /api/tts as a valid route
    - All 9 voice files exist (ls command exits 0)
    - VoiceBusProvider appears in layout.tsx
    - useVoiceController appears in page.tsx
    - waitForInst appears in particle-background.tsx
    - Flip.from appears in desktop-navbar.tsx
    - ELEVENLABS_API_KEY grep outside api/tts/route.ts returns no matches
  </acceptance_criteria>

  <done>All automated checks pass. Dev server running. Ready for human verification.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Full voice mode:
- Navbar morphs into 760×72px voice capsule (desktop GSAP Flip) / grows to 140px (mobile CSS) when Ask Parz clicked
- VoiceWave 5-bar waveform animates based on VoiceBus.level
- State chip dot pulses blue (idle), red (listening), yellow (thinking), green (speaking)
- STT via Web Speech API (Chrome/Edge) with live mic amplitude
- ElevenLabs streaming TTS via /api/tts proxy (requires ELEVENLABS_API_KEY in .env.local)
- Particle mesh breathes during voice activity (two-sine 1.6/4.2 Hz)
- Voice commands: "open portfolio", "about page", "take me home"
- "text" / chat button opens ChatPopup fallback
- "stop" / close button exits voice mode, navbar reverts
  </what-built>

  <how-to-verify>
Open http://localhost:3000 in Chrome (required for Web Speech API).

**Checkpoint A — Morph animation (VOIC-01)**
1. Click the Ask Parz button
2. Verify: Navbar expands smoothly from pill to wider capsule (760px wide on desktop)
3. Verify: VoiceWave bars appear, state chip shows blue dot and "Ready"
4. Click close (X button) — verify navbar shrinks back to original dimensions

**Checkpoint B — Listening state (VOIC-02)**
1. Open voice mode again
2. Click the mic button (or speak)
3. Verify: State chip turns red, label changes to "Listening"
4. Verify: Caption shows live interim transcript as you speak
5. Verify: VoiceWave bars animate more actively during speech

**Checkpoint C — TTS (VOIC-03)** (requires ELEVENLABS_API_KEY in .env.local)
1. Say "what are your hobbies?" or any question
2. Verify: State chip turns yellow ("Thinking") while waiting
3. Verify: State chip turns green ("Speaking") when audio plays
4. Verify: VoiceWave bars animate in sync with the voice audio
5. Verify: Caption shows the response text while speaking

**Checkpoint D — Particle breathing (VOIC-04)**
1. While voice mode is active, observe the particle mesh background
2. Verify: Particles visibly "breathe" (opacity + connection lines pulse) during speaking
3. Verify: Thinking state causes more connections to appear between particles
4. Verify: Particles settle back to normal when voice mode closes

**Checkpoint E — Voice commands (VOIC-05)**
1. Open voice mode and say "open portfolio" — verify navigation to /portfolio occurs
2. Return to home, open voice mode, say "about page" — verify navigation to /about
3. Return to home, open voice mode, click the chat bubble button — verify ChatPopup opens
4. Return to home, open voice mode, say "stop" — verify voice mode closes

**Checkpoint F — Mobile (visual check)**
1. Resize browser to <600px or use Chrome DevTools mobile emulation
2. Click Ask Parz — verify bottom bar grows in height (not width) to show VoicePanel

If ELEVENLABS_API_KEY is not configured: TTS checkpoint (C) can be skipped — verify that the loading state at least shows "Thinking" and then returns to idle after the API call fails gracefully (no crash, no white screen).
  </how-to-verify>

  <resume-signal>Type "approved" if all checkpoints pass, or describe which specific checkpoint failed and what you observed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer browser → localhost:3000 | Local dev verification; no production trust boundaries active |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-18 | Information Disclosure | Dev server logs may show mic audio debug output | accept | print() statements are dev-only; removed in production build |
</threat_model>

<verification>
Pre-flight automated checks (Task 1) must all pass before human verification begins.
</verification>

<success_criteria>
- All 5 VOIC requirements verified visually by the developer
- Navbar morph animation runs smoothly (no jank)
- VoiceWave bars animate naturally
- Voice commands navigate correctly
- Text fallback opens ChatPopup
- Stop closes voice mode and navbar reverts
- Particle mesh responds to voice activity
</success_criteria>

<output>
After completion, create `.planning/phases/08-voice-mode/08-05-SUMMARY.md` using the template at @/Users/lakshman/.claude/get-shit-done/templates/summary.md
</output>
