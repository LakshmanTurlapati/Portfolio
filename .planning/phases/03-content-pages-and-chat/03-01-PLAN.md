---
phase: 03-content-pages-and-chat
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/assets/ (25 image files copied from Flutter)
  - src/data/projects.ts
  - src/components/portfolio-card.tsx
  - src/app/portfolio/page.tsx
  - src/app/globals.css
autonomous: true
requirements: [PAGE-02, PAGE-03]

must_haves:
  truths:
    - "Portfolio page displays 23 projects in a masonry grid with 4 columns on desktop"
    - "Portfolio page displays projects in single column on mobile (<600px)"
    - "First 2 projects (Software 3.0, Review Gate) are always pinned first; rest are shuffled"
    - "Each project card shows image, name, and clickable link icons (GitHub, Website, Design)"
    - "Portfolio page uses inverted background colors (dark bg in light mode, light bg in dark mode)"
    - "Snowfall effect overlays the portfolio page"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Hardcoded array of 23 projects with Project interface"
      exports: ["Project", "projects"]
    - path: "src/components/portfolio-card.tsx"
      provides: "Individual project card component"
      exports: ["PortfolioCard"]
    - path: "src/app/portfolio/page.tsx"
      provides: "Portfolio page with masonry grid, back button, snow overlay"
    - path: "public/assets/"
      provides: "25 project image files"
  key_links:
    - from: "src/app/portfolio/page.tsx"
      to: "src/data/projects.ts"
      via: "import projects array"
      pattern: "import.*projects.*from.*data/projects"
    - from: "src/app/portfolio/page.tsx"
      to: "src/components/portfolio-card.tsx"
      via: "renders PortfolioCard for each project"
      pattern: "PortfolioCard"
    - from: "src/app/portfolio/page.tsx"
      to: "src/components/snowfall.tsx"
      via: "wraps content with Snowfall overlay"
      pattern: "Snowfall"
---

<objective>
Build the Portfolio page with a CSS-columns masonry grid displaying all 23 projects from the Flutter version, including asset images, project data, and the PortfolioCard component.

Purpose: Replaces the placeholder portfolio page with the full masonry grid of projects matching the Flutter version's visual layout and content.
Output: Working portfolio page at /portfolio with all projects, images, links, and snowfall overlay.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/globals.css
@src/components/snowfall.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Copy assets, create project data, add inverted color tokens</name>
  <files>public/assets/ (25 files), src/data/projects.ts, src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (existing CSS custom properties)
  </read_first>
  <action>
1. Copy all 25 image files from the Flutter assets directory to public/assets/. The Flutter assets are at `.claude/worktrees/agent-a7a91758/assets/`. IMPORTANT: rename `CHD.png` to `chd.png` (lowercase) during copy. All other filenames are already lowercase.

Files to copy: s3.png, review_gate.webp, estsight.png, blockchain.jpg, sfuit.jpg, portfolio.jpg, fi.png, linkedin.png, CHD.png->chd.png, heartline.png, lucent.png, parz_ai.png, hackathon.png, t2s_cli.png, startrail.jpg, awsxutd.png, open_api.png, artscii.jpg, fsb.png, asteroids.png, prokeys.png, smollm_flutter.png, clickhere.gif, clickherelight.gif, movie.png

Use bash: `mkdir -p public/assets && cp .claude/worktrees/agent-a7a91758/assets/* public/assets/ && mv public/assets/CHD.png public/assets/chd.png`

2. Create `src/data/projects.ts` with the Project interface and all 23 projects. The data must be EXACTLY:

```typescript
export interface Project {
  name: string;
  image: string;
  links: Record<string, string>;
  useIframe?: boolean;
}

// First 2 are pinned (always shown first), rest are shuffled on each render
export const pinnedProjects: Project[] = [
  { name: "Software 3.0", image: "/assets/s3.png", links: { Website: "https://www.software-3.com", GitHub: "https://github.com/LakshmanTurlapati/Software-3.0" } },
  { name: "Review Gate", image: "/assets/review_gate.webp", links: { GitHub: "https://github.com/LakshmanTurlapati/Review-Gate" } },
];

export const shuffleableProjects: Project[] = [
  { name: "EatSight", image: "/assets/estsight.png", links: { Website: "https://eatsight.fly.dev", GitHub: "https://github.com/LakshmanTurlapati/EatSight" } },
  { name: "Blockchain Smartcontracts", image: "/assets/blockchain.jpg", links: { GitHub: "https://github.com/LakshmanTurlapati/Blockchain" } },
  { name: "Smart Fabric using IOT", image: "/assets/sfuit.jpg", links: { Website: "https://www.youtube.com/watch?v=AkKRSgQnT_c", GitHub: "https://github.com/prateek10201/sfuit-esp8266" } },
  { name: "Portfolio", image: "/assets/portfolio.jpg", links: { Website: "https://audienclature.com", GitHub: "https://github.com/LakshmanTurlapati/Portfolio", Design: "https://www.figma.com/design/UeixAHUPLTSKiwHR9HVfT2/Portfolio?node-id=0-1&t=QkxcB16bQkJ96mpv-1" } },
  { name: "Financial Inclusion", image: "/assets/fi.png", links: { Website: "https://docs.google.com/document/d/1cq1xeUpl-lst5bj4376_QhCSo7HIc1EvPecKw0cmQGc/edit?usp=sharing", GitHub: "https://github.com/LakshmanTurlapati/Financial-Inclusion-v2", Design: "https://www.figma.com/design/5kNlAtt2Hh6NTx2YAPLhIu/Financial-Inclusion?node-id=0-1&t=jnAr4kRmQeHKDdOg-1" } },
  { name: "LinkedIn Auto Connect", image: "/assets/linkedin.png", links: { Website: "https://chromewebstore.google.com/detail/linkedin-auto-connect/jomecnphbmfpkcajfhkoebgmbcbakjoa?authuser=1&hl=en&pli=1", GitHub: "https://github.com/LakshmanTurlapati/linkedin-autoconnect-extension/tree/main" } },
  { name: "Service Portal", image: "/assets/chd.png", links: { GitHub: "https://github.com/LakshmanTurlapati/Church-Dwight-Solution-Center", Design: "https://www.figma.com/design/Lj0O8tBvyuGSx3LePBCuO1/C%26D?node-id=0-1&t=QO284B9qHF3brzcH-1" } },
  { name: "X-Read", image: "", links: { GitHub: "https://github.com/LakshmanTurlapati/DCTE-Script" }, useIframe: true },
  { name: "Heartline", image: "/assets/heartline.png", links: { GitHub: "https://github.com/LakshmanTurlapati/Heartline" } },
  { name: "Lucent", image: "/assets/lucent.png", links: { Website: "https://monumental-granita-08d2f5.netlify.app", GitHub: "https://github.com/LakshmanTurlapati/Lucent" } },
  { name: "Parz-AI", image: "/assets/parz_ai.png", links: { GitHub: "https://github.com/LakshmanTurlapati/Parz-AI" } },
  { name: "awsxUTD-Hackathon", image: "/assets/hackathon.png", links: { GitHub: "https://github.com/LakshmanTurlapati/awsxUTD-Hackathon" } },
  { name: "T2S CLI", image: "/assets/t2s_cli.png", links: { GitHub: "https://github.com/LakshmanTurlapati/t2s-cli" } },
  { name: "Star-Trail-Flutter", image: "/assets/startrail.jpg", links: { GitHub: "https://github.com/LakshmanTurlapati/Star-Trail-Flutter" } },
  { name: "awsxutd", image: "/assets/awsxutd.png", links: { Website: "https://marvelous-sopapillas-cf2910.netlify.app", GitHub: "https://github.com/LakshmanTurlapati/awsxutd" } },
  { name: "Open-API", image: "/assets/open_api.png", links: { GitHub: "https://github.com/LakshmanTurlapati/open-api" } },
  { name: "ArtScii", image: "/assets/artscii.jpg", links: { GitHub: "https://github.com/LakshmanTurlapati/ArtScii" } },
  { name: "FSB", image: "/assets/fsb.png", links: { GitHub: "https://github.com/LakshmanTurlapati/FSB" } },
  { name: "Asteroids Game", image: "/assets/asteroids.png", links: { Website: "https://harmonious-caramel-3c3627.netlify.app", GitHub: "https://github.com/LakshmanTurlapati/Atari-Astroids-Multiplayer" } },
  { name: "ProKeys", image: "/assets/prokeys.png", links: { GitHub: "https://github.com/LakshmanTurlapati/ProKeys" } },
  { name: "SmolLM Flutter", image: "/assets/smollm_flutter.png", links: { GitHub: "https://github.com/LakshmanTurlapati/SmolLm-Flutter" } },
];
```

3. Add inverted background color CSS custom properties to globals.css. Add these inside the existing `:root` block:
```css
  --color-page-inverted-bg: #2A2A2A;
  --color-page-inverted-text: #FFFFFF;
```
And inside the existing `.dark` block:
```css
  --color-page-inverted-bg: #DBDBDB;
  --color-page-inverted-text: #000000;
```

Also add a vertical fade mask class for portfolio/about pages:
```css
/* Vertical fade mask for portfolio/about pages */
.page-vertical-fade-mask {
  mask-image: linear-gradient(to bottom, transparent 2%, black 8%, black 92%, transparent 98%);
  -webkit-mask-image: linear-gradient(to bottom, transparent 2%, black 8%, black 92%, transparent 98%);
}
```
  </action>
  <verify>
    <automated>ls public/assets/s3.png public/assets/chd.png public/assets/review_gate.webp && test -f src/data/projects.ts && grep "page-inverted-bg" src/app/globals.css && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - public/assets/ contains all 25 image files (including chd.png lowercase)
    - src/data/projects.ts exports Project interface, pinnedProjects (2), and shuffleableProjects (21)
    - globals.css contains --color-page-inverted-bg and --color-page-inverted-text for both :root and .dark
    - globals.css contains .page-vertical-fade-mask class
  </acceptance_criteria>
  <done>All 25 assets copied, project data file created with correct types and all 23 projects, inverted color tokens and fade mask CSS added</done>
</task>

<task type="auto">
  <name>Task 2: Build PortfolioCard component and Portfolio page</name>
  <files>src/components/portfolio-card.tsx, src/app/portfolio/page.tsx</files>
  <read_first>
    - src/data/projects.ts (created in Task 1)
    - src/components/snowfall.tsx (reuse for overlay)
    - src/app/globals.css (for CSS custom properties)
  </read_first>
  <action>
1. Create `src/components/portfolio-card.tsx` -- a client component for individual project cards:
   - Import `FaLink`, `FaCodeBranch`, `FaFigma` from `react-icons/fa6` for link icons
   - Import `Project` type from `@/data/projects`
   - Map link types to icons: Website->FaLink, GitHub->FaCodeBranch, Design->FaFigma
   - Card structure: rounded-xl overflow-hidden container
   - If project.image exists: render `<img>` with `src={project.image}`, `alt={project.name}`, `loading="lazy"`, `className="w-full rounded-lg object-cover"` wrapped in `p-2 pb-0` div
   - If project.image is empty (X-Read case): render a placeholder div with `h-[180px]` and a code icon or `<>` text centered, using `var(--color-page-inverted-text)` for color
   - Below image: name + links row in `p-3 flex items-center justify-between`
   - Name: `font-bold text-sm truncate` with `color: var(--color-page-inverted-text)`
   - Links: flex gap-2, each link is `<a>` with `target="_blank" rel="noopener noreferrer"`, `onClick={(e) => e.stopPropagation()}`, icon `w-4 h-4` styled with `color: var(--color-page-inverted-text)`
   - Card onClick opens the first link URL in a new tab (primary link is first entry in links object)

2. Create `src/app/portfolio/page.tsx` -- a client component replacing the placeholder:
   - `'use client'` directive at top
   - Import `pinnedProjects`, `shuffleableProjects` from `@/data/projects`
   - Import `PortfolioCard` from `@/components/portfolio-card`
   - Import `Snowfall` from `@/components/snowfall`
   - Import `useRouter` from `next/navigation` for back button
   - Import `useState`, `useMemo` from `react`
   - Import `FaArrowLeft` from `react-icons/fa6` for back button
   - Use `useMemo` to create display projects: `[...pinnedProjects, ...shuffledRest]` where shuffledRest is a Fisher-Yates shuffle of shuffleableProjects (shuffle once on mount, not on every render)
   - Page background: `min-h-screen` with `backgroundColor: var(--color-page-inverted-bg)`
   - Back button: fixed top-left (top-6 left-6), 48x48px, rounded-xl, inverted colors (`bg-[var(--color-page-inverted-text)]` with icon color `var(--color-page-inverted-bg)`), `cursor-pointer`, onClick calls `router.back()`
   - Snowfall component overlay: render `<Snowfall />` as a sibling positioned absolute, full width/height, pointer-events-none, z-10 above grid
   - Main grid area: `page-vertical-fade-mask` class, overflow-y-auto, full viewport height
   - CSS columns grid: `columns-1 sm:columns-4 gap-4` with `px-6 sm:px-[58px]` padding, `pt-20 pb-24` for spacing around back button
   - Each project wrapped in `break-inside-avoid mb-4` div
   - Footer at bottom: centered text "Some of my **finest works** are missing, Thanks to the **'NDA'**!" with `text-sm`, color matching inverted text with reduced opacity. Bold spans use `<strong>`. Placed below the grid.
   - Mobile (<600px / below sm breakpoint): columns-1 renders single column, `px-6` padding. No active-project highlighting needed for MVP (Flutter does scroll-based highlighting but CSS columns handle the single-column layout naturally).

DO NOT import or render DesktopNavbar or MobileNavbar -- portfolio page has NO navbar, only a back button.
  </action>
  <verify>
    <automated>npx tsc --noEmit src/components/portfolio-card.tsx src/app/portfolio/page.tsx 2>&1 | head -20; echo "---"; grep -c "PortfolioCard" src/app/portfolio/page.tsx && grep -c "Snowfall" src/app/portfolio/page.tsx && grep -c "columns" src/app/portfolio/page.tsx && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/portfolio-card.tsx exports PortfolioCard with image, name, and link icons
    - src/app/portfolio/page.tsx is a client component with 'use client' directive
    - Portfolio page uses CSS columns-1 sm:columns-4 for masonry layout
    - Portfolio page renders Snowfall overlay
    - Portfolio page has back button (no navbar)
    - Portfolio page uses inverted background colors via CSS custom properties
    - Footer text "Some of my finest works are missing" is rendered
    - No TypeScript errors
  </acceptance_criteria>
  <done>Portfolio page renders all 23 projects in masonry grid with snowfall overlay, back button, inverted colors, and footer text</done>
</task>

</tasks>

<verification>
- `ls public/assets/ | wc -l` returns 25
- `npx tsc --noEmit` passes with no errors
- Portfolio page file contains: 'use client', columns, Snowfall, PortfolioCard, pinnedProjects, shuffleableProjects
- No navbar imports in portfolio page
</verification>

<success_criteria>
Portfolio page at /portfolio displays all 23 projects in a masonry grid (4 columns desktop, 1 column mobile) with images, names, clickable link icons, inverted background colors, snowfall overlay, and back button navigation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-content-pages-and-chat/03-01-SUMMARY.md`
</output>
