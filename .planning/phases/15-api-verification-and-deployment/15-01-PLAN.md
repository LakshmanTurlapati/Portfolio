---
phase: 15-api-verification-and-deployment
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - amplify.yml
autonomous: true
requirements: [API-01, API-02]

must_haves:
  truths:
    - "amplify.yml preBuild injects both XAI_API_KEY and ELEVENLABS_API_KEY into .env.production"
    - "A build triggered after env var setup will bundle ELEVENLABS_API_KEY into the Lambda runtime"
    - "No 503 from /api/tts or /api/stt-token due to missing env var injection"
  artifacts:
    - path: "amplify.yml"
      provides: "Build spec that injects both API keys at build time"
      contains: "ELEVENLABS_API_KEY=$ELEVENLABS_API_KEY"
  key_links:
    - from: "amplify.yml preBuild"
      to: ".env.production"
      via: "echo ELEVENLABS_API_KEY=$ELEVENLABS_API_KEY >> .env.production"
      pattern: "ELEVENLABS_API_KEY"
---

<objective>
Fix amplify.yml so ELEVENLABS_API_KEY is injected into .env.production at build time alongside XAI_API_KEY.

Purpose: The current amplify.yml only writes XAI_API_KEY to .env.production. The two ElevenLabs routes (/api/tts and /api/stt-token) both call hasEnvVar('ELEVENLABS_API_KEY') and return 503 when it is absent. Because Amplify SSR lambdas do not inherit Amplify Console env vars at runtime, the key must be baked into .env.production at build time via the preBuild echo pattern already used for XAI_API_KEY.

Output: Updated amplify.yml with a second echo line for ELEVENLABS_API_KEY. No other files change.
</objective>

<execution_context>
@/Users/lakshman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lakshman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ELEVENLABS_API_KEY injection to amplify.yml preBuild</name>
  <files>amplify.yml</files>
  <action>
Read amplify.yml. In the preBuild commands block, immediately after the existing line:
  - echo "XAI_API_KEY=$XAI_API_KEY" >> .env.production
add a new line:
  - echo "ELEVENLABS_API_KEY=$ELEVENLABS_API_KEY" >> .env.production

The indentation must match the existing echo line exactly (4 spaces + "- ").

Do NOT touch the build commands, artifacts, or cache sections. Do NOT add any other keys.
  </action>
  <verify>
    <automated>grep "ELEVENLABS_API_KEY" amplify.yml</automated>
  </verify>
  <done>amplify.yml preBuild section contains both echo lines — XAI_API_KEY and ELEVENLABS_API_KEY — writing to .env.production</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build environment → Lambda runtime | API keys flow from Amplify Console env vars through build-time echo into .env.production, bundled with server code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Information Disclosure | amplify.yml echo pattern | accept | Keys are written to .env.production which is a server-only artifact; not exposed in client bundle; this is the standard Amplify SSR pattern for Next.js |
| T-15-02 | Information Disclosure | .env.local in repo | mitigate | Confirm .env.local is in .gitignore before committing — keys must not appear in git history |
</threat_model>

<verification>
1. grep "ELEVENLABS_API_KEY" amplify.yml -- returns the echo line
2. grep "XAI_API_KEY" amplify.yml -- still present (regression check)
3. Both lines appear inside preBuild.commands, not in build or other sections
</verification>

<success_criteria>
amplify.yml preBuild commands contain:
  - echo "XAI_API_KEY=$XAI_API_KEY" >> .env.production
  - echo "ELEVENLABS_API_KEY=$ELEVENLABS_API_KEY" >> .env.production

Both lines are present and in the correct indentation. No other changes.
</success_criteria>

<output>
After completion, create `.planning/phases/15-api-verification-and-deployment/15-01-SUMMARY.md`
</output>
