---
phase: 28-chat-ui-redesign
plan: 03
type: execute
wave: 3
depends_on: [28-02]
files_modified:
  - src/components/chat-popup.tsx
autonomous: false
requirements:
  - CHAT-UI-01
must_haves:
  truths:
    - "User opening the popup sees a 200ms scale-and-fade entry (scale 0.96 -> 1.0, opacity 0 -> 1) using cubic-bezier(0.2, 0.9, 0.2, 1)."
    - "User on prefers-reduced-motion sees the popup appear instantly with no scale, no slide, and no fade — and the dot-wave loading animation degrades to three static dots at opacity 0.6."
    - "User pressing Escape anywhere while the popup is open closes the popup."
    - "User using a screen reader hears the persona name as the dialog label, hears new assistant messages announced via aria-live polite, hears the loading state announced as 'Parz is typing', and hears errors announced via role=alert."
    - "User Tab-cycles through input -> send button -> close button -> [chips when visible] in document order; Tab/Shift+Tab does not escape the popup unexpectedly."
    - "User closing the popup has focus restored to the element that opened it (when possible)."
    - "User sees a visible focus ring (2px outline at var(--color-text) 0.40 alpha, 2px offset) on input, send button, close button, and suggestion chips on :focus-visible."
    - "User keeps every existing send/receive/streaming/suggestion/loading/error/tool-call behavior byte-identical."
    - "User on iOS keeps the Phase 26 inputMode/enterKeyHint/autoComplete attrs and the 300ms scrollIntoView focus handler and the safe-area paddingBottom."
  artifacts:
    - path: "src/components/chat-popup.tsx"
      provides: "Motion: popupIn (200ms), backdropFadeIn (200ms), messageAppear (180ms), reduced-motion neutralizer"
      contains: "@media (prefers-reduced-motion: reduce)"
    - path: "src/components/chat-popup.tsx"
      provides: "A11y: role=dialog + aria-modal + aria-labelledby; role=log + aria-live=polite on messages; role=status on loading; role=alert on error; aria-label='Send message' / 'Close chat' / 'Message Parz' / 'Suggested questions'"
      contains: "aria-modal=\"true\""
    - path: "src/components/chat-popup.tsx"
      provides: "Escape key handler closing the popup; focus capture+restore"
      contains: "e.key === 'Escape'"
    - path: "src/components/chat-popup.tsx"
      provides: "Visible focus ring on focus-visible for input, send, close, chips"
      contains: ":focus-visible"
  key_links:
    - from: "window keydown listener"
      to: "onClose"
      via: "Escape key handler attached in useEffect cleanup pair"
      pattern: "window\\.addEventListener\\('keydown'"
    - from: "previouslyFocused ref captured on mount"
      to: "previouslyFocused.focus() on unmount"
      via: "useEffect cleanup function"
      pattern: "previouslyFocused"
---

**Post-v4.2 correction (2026-04-28):** This plan predates the user's final DART chat decision. Keep its accessibility and reduced-motion requirements, but treat any old popup geometry or entry timing as historical unless it matches the DART-refined shell. Future polish belongs to CHAT-ANIM-01.

<objective>
Land UI-SPEC §7 (Motion) and §8 (Accessibility) on top of Plans 01 + 02. This is the polish closer for Phase 28: motion keyframes (entry, exit, message-appear, send-success-pulse), a `prefers-reduced-motion: reduce` neutralizer, ARIA semantics (dialog / log / status / alert), Escape-key handler, focus capture + restore, visible focus rings on the four focusable elements.

Purpose: Bring the popup to UI-SPEC compliance without disturbing any of the preserved behavior contracts. After this plan the phase is shippable to a UI checker.

Output: src/components/chat-popup.tsx finalized — animation feels smooth, screen readers receive correct semantics, keyboard users can close with Escape and Tab through the popup, motion gracefully degrades for reduced-motion users.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/28-chat-ui-redesign/28-CONTEXT.md
@.planning/phases/28-chat-ui-redesign/28-UI-SPEC.md
@.planning/phases/28-chat-ui-redesign/28-01-SUMMARY.md
@.planning/phases/28-chat-ui-redesign/28-02-SUMMARY.md

@src/components/chat-popup.tsx
@src/app/globals.css

<interfaces>
<!-- The scoped <style>{`...`}</style> block already inside chat-popup.tsx contains: -->
<!--   - @keyframes popupIn (currently translate(-50%, 30px) -> translate(-50%, 0) — STALE after Plan 01 removed translateX(-50%); rewrite this plan) -->
<!--   - @keyframes fadeIn (used on backdrop) -->
<!--   - @keyframes dot-wave-popup (used on loading dots — preserve verbatim) -->
<!--   - @keyframes popup-shimmer + .popup-shimmer-text class (used on loading status text — preserve verbatim) -->

<!-- Tokens to use for focus ring: -->
<!--   color-mix(in srgb, var(--color-text) 40%, transparent) — gives 0.40 alpha against either theme -->

<!-- Phase 26 attrs that MUST NOT change in this plan: -->
<!--   inputMode="text" enterKeyHint="send" autoComplete="off" onFocus 300ms scrollIntoView -->
<!--   paddingBottom: max(16px, env(safe-area-inset-bottom)) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Motion — rewrite popupIn keyframe, add messageAppear + sendSuccessPulse keyframes, wire prefers-reduced-motion neutralizer</name>
  <files>src/components/chat-popup.tsx</files>
  <read_first>
    - src/components/chat-popup.tsx (state after Plan 02)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §7 (Motion Spec table — full)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §13 (Implementation notes about scoped style block)
    - src/app/globals.css (confirm `askParzPulse` keyframe exists at the documented location for optional reuse)
  </read_first>
  <behavior>
    - Test 1: Scoped `<style>{`...`}</style>` block contains a rewritten `@keyframes popupIn` that animates `opacity 0 -> 1` and `transform: scale(0.96) -> scale(1.0)` with NO `translate(-50%, ...)` (the new geometry has no centering transform).
    - Test 2: Scoped style block contains `@keyframes messageAppear` animating `opacity 0 -> 1; transform: translateY(4px) -> translateY(0)`.
    - Test 3: Scoped style block contains `@keyframes sendSuccessPulse` animating `box-shadow: 0 0 0 0 rgba(169,227,75,0.55) -> 0 0 0 6px rgba(169,227,75,0)` (reuses the green from `askParzPulse` per UI-SPEC §13).
    - Test 4: Popup card root uses `animation: 'popupIn 200ms cubic-bezier(0.2, 0.9, 0.2, 1)'`.
    - Test 5: Each rendered message bubble wrapper `<div>` (the one with `justifyContent: isUser ? 'flex-end' : 'flex-start'`) uses `animation: 'messageAppear 180ms ease-out'`. Apply via inline style on the wrapper, not the inner bubble, so the slide-up is on the wrapper.
    - Test 6: Backdrop animation switches from `'fadeIn 0.2s ease'` to `'fadeIn 200ms ease-out'` (cosmetic but UI-SPEC §7 says 200ms ease-out).
    - Test 7: Scoped style block ends with a `@media (prefers-reduced-motion: reduce)` block that disables animations on:
        - The popup card root (no animation)
        - The backdrop (no animation)
        - Message wrappers (no animation)
        - The dot-wave-popup loop (set animation: none, opacity 0.6 — three static dots)
        - The popup-shimmer-text gradient (set animation: none — solid color)
        - Send success pulse (set animation: none)
    - Test 8: NO behavior change to `dot-wave-popup` keyframe in the non-reduced-motion default — preserve verbatim.
  </behavior>
  <action>
Edit `src/components/chat-popup.tsx`. Update the scoped `<style>` block.

1. **Rewrite the `popupIn` keyframe** (currently uses `translate(-50%, ...)` which is wrong post-Plan-01). Replace with:
   ```css
   @keyframes popupIn {
     from { opacity: 0; transform: scale(0.96); }
     to { opacity: 1; transform: scale(1); }
   }
   ```

2. **Add new keyframes** to the same scoped style block (append after the existing keyframes, before the `.popup-shimmer-text` class):
   ```css
   @keyframes messageAppear {
     from { opacity: 0; transform: translateY(4px); }
     to { opacity: 1; transform: translateY(0); }
   }
   @keyframes sendSuccessPulse {
     from { box-shadow: 0 0 0 0 rgba(169, 227, 75, 0.55); }
     to { box-shadow: 0 0 0 6px rgba(169, 227, 75, 0); }
   }
   ```

3. **Append a reduced-motion block** at the end of the scoped style block:
   ```css
   @media (prefers-reduced-motion: reduce) {
     [data-chat-popup-card],
     [data-chat-popup-backdrop],
     [data-chat-message-wrapper] {
       animation: none !important;
     }
     [data-chat-loading-dot] {
       animation: none !important;
       opacity: 0.6 !important;
     }
     .popup-shimmer-text {
       animation: none !important;
       background: var(--color-text) !important;
       -webkit-text-fill-color: var(--color-text) !important;
     }
     [data-chat-send-pulse] {
       animation: none !important;
     }
   }
   ```

4. **Tag elements with the data attributes used above:**
   - Popup card root `<div>`: add `data-chat-popup-card="true"`.
   - Backdrop `<div>`: add `data-chat-popup-backdrop="true"`.
   - Message wrapper `<div>` (the `<div style={{ display: 'flex', justifyContent: ... }}>` produced inside `messages.map`): add `data-chat-message-wrapper="true"` and add `animation: 'messageAppear 180ms ease-out'` to its inline style.
   - Loading dot `<div>` (each of the three dots): add `data-chat-loading-dot="true"`.

5. **Re-add the popup card entry animation** (Plan 01 deliberately removed it). Add to the popup card style object:
   ```ts
   animation: 'popupIn 200ms cubic-bezier(0.2, 0.9, 0.2, 1)',
   ```

6. **Backdrop animation:** change the existing `animation: 'fadeIn 0.2s ease'` to `animation: 'fadeIn 200ms ease-out'`.

7. **(Optional polish — do this if time permits, otherwise skip)** Wire a 250ms `sendSuccessPulse` on the send button after a successful sendMessage. Implementation: track a transient piece of state `const [pulseSuccess, setPulseSuccess] = useState(false)` set to `true` from inside `handleSend` after `sendMessage({ text: trimmed })` runs, with a `setTimeout(() => setPulseSuccess(false), 250)`. On the send button: conditionally apply `animation: 'sendSuccessPulse 250ms ease-out'` and add `data-chat-send-pulse="true"`. **Skip if any test infra friction; this is the only OPTIONAL item in the plan.**

8. **DO NOT TOUCH:**
   - `dot-wave-popup` keyframe definition
   - `popup-shimmer-text` className definition
   - `popupIn` animation duration was previously `0.4s` — UI-SPEC §7 says `200ms`. Use 200ms.
   - Any handler, useEffect, useState (except the optional pulseSuccess add), useRef.
   - Phase 26 iOS attrs and onFocus handler.
   - useChat destructure, siteControlChatTransport, useSiteControl, sanitizeText, RenderLinkedText.

References:
- UI-SPEC §7 motion table
- UI-SPEC §13 ("reuse the existing askParzPulse keyframe in globals.css... by adding a class and toggling it on... a one-shot setTimeout clearing 250ms after sendMessage returns")
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "@keyframes popupIn" src/components/chat-popup.tsx && grep -q "scale(0.96)" src/components/chat-popup.tsx && grep -q "@keyframes messageAppear" src/components/chat-popup.tsx && grep -q "translateY(4px)" src/components/chat-popup.tsx && grep -q "@keyframes sendSuccessPulse" src/components/chat-popup.tsx && grep -q "169, 227, 75" src/components/chat-popup.tsx && grep -q "prefers-reduced-motion: reduce" src/components/chat-popup.tsx && grep -q "data-chat-popup-card" src/components/chat-popup.tsx && grep -q "data-chat-popup-backdrop" src/components/chat-popup.tsx && grep -q "data-chat-message-wrapper" src/components/chat-popup.tsx && grep -q "data-chat-loading-dot" src/components/chat-popup.tsx && grep -q "popupIn 200ms cubic-bezier(0.2, 0.9, 0.2, 1)" src/components/chat-popup.tsx && grep -q "messageAppear 180ms ease-out" src/components/chat-popup.tsx && grep -q "fadeIn 200ms ease-out" src/components/chat-popup.tsx && grep -q "dot-wave-popup 1.4s ease-in-out" src/components/chat-popup.tsx && grep -q "inputMode=\"text\"" src/components/chat-popup.tsx && grep -q "max(16px, env(safe-area-inset-bottom))" src/components/chat-popup.tsx && grep -q "siteControlChatTransport" src/components/chat-popup.tsx && grep -q "handledToolCallsRef" src/components/chat-popup.tsx && ! grep -q "translate(-50%, 30px)" src/components/chat-popup.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - Scoped style has rewritten popupIn (no translate-50%), new messageAppear, new sendSuccessPulse, and a prefers-reduced-motion neutralizer.
    - Card, backdrop, message wrappers, and loading dots are tagged with data attributes.
    - Popup card and message wrappers play their entry animations.
    - Reduced-motion users get instant swaps + static dots + solid (non-shimmer) status text.
    - Phase 26 iOS attrs intact; useChat / handler logic intact.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Accessibility — role=dialog + aria-modal + aria-labelledby; role=log on messages; role=status on loading; role=alert on error; aria-label on input + suggested-questions group; Escape key handler; focus capture + restore; visible focus rings via :focus-visible</name>
  <files>src/components/chat-popup.tsx</files>
  <read_first>
    - src/components/chat-popup.tsx (state after Task 1)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §8 (Accessibility Spec — full)
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §11.8 (Acceptance criteria for a11y)
  </read_first>
  <behavior>
    - Test 1: Popup card root `<div>` has `role="dialog"`, `aria-modal="true"`, `aria-labelledby="chat-popup-heading"`.
    - Test 2: Messages area `<div>` has `role="log"`, `aria-live="polite"`, `aria-relevant="additions"`.
    - Test 3: Loading bubble outer wrapper has `role="status"`, `aria-label="Parz is typing"`. The three dots have `aria-hidden="true"`.
    - Test 4: Error bubble outer wrapper has `role="alert"`.
    - Test 5: Suggestion strip container has `role="group"`, `aria-label="Suggested questions"`.
    - Test 6: Input field has `aria-label="Message Parz"`.
    - Test 7: Component captures `document.activeElement` to a ref on mount; restores focus to it on unmount via the existing mount useEffect's cleanup function.
    - Test 8: Component installs a `keydown` listener on `window` for `e.key === 'Escape'` that calls `onClose()`. Listener is added on mount and removed on unmount (useEffect cleanup).
    - Test 9: Scoped `<style>` block contains a `:focus-visible` rule that applies `outline: 2px solid color-mix(in srgb, var(--color-text) 40%, transparent); outline-offset: 2px` to:
        - Input field via a class or `data-chat-input`
        - Send button via `data-chat-send`
        - Close button via `data-chat-close`
        - Suggestion chip buttons via `data-chat-chip`
    - Test 10: All Phase 26 iOS attrs and the onFocus 300ms scrollIntoView handler unchanged.
    - Test 11: All useChat / useEffect / handler logic unchanged.
  </behavior>
  <action>
Continue editing `src/components/chat-popup.tsx`.

1. **Add focus-capture state and Escape handler** (place these after the other useState calls, before the existing useEffect blocks):
   ```ts
   const previouslyFocusedRef = useRef<HTMLElement | null>(null);

   // Capture opener focus on mount; restore on unmount.
   useEffect(() => {
     previouslyFocusedRef.current = document.activeElement as HTMLElement | null;
     inputRef.current?.focus();
     return () => {
       previouslyFocusedRef.current?.focus?.();
     };
   }, []);
   ```
   **REPLACE** the existing `// Focus input on open` useEffect with the block above. The single combined effect now both focuses the input AND captures + restores opener focus, while honoring the prior responsibility.

   Add a separate Escape useEffect:
   ```ts
   useEffect(() => {
     const onKeyDown = (e: KeyboardEvent) => {
       if (e.key === 'Escape') onClose();
     };
     window.addEventListener('keydown', onKeyDown);
     return () => window.removeEventListener('keydown', onKeyDown);
   }, [onClose]);
   ```

2. **Popup card root `<div>`:** add ARIA attrs:
   ```tsx
   role="dialog"
   aria-modal="true"
   aria-labelledby="chat-popup-heading"
   data-chat-popup-card="true"
   ```
   (`role="dialog"` was already added in Plan 01 — confirm and add the rest.)

3. **Messages area `<div>`:** add:
   ```tsx
   role="log"
   aria-live="polite"
   aria-relevant="additions"
   ```

4. **Loading bubble outer wrapper `<div>`:** the current outer wrapper is `<div style={{ display: 'flex', justifyContent: 'flex-start', marginBottom: '12px' }}>`. Add:
   ```tsx
   role="status"
   aria-label="Parz is typing"
   ```
   On the inner three-dot container `<div>`, add `aria-hidden="true"`.

5. **Error bubble outer wrapper `<div>`:** add:
   ```tsx
   role="alert"
   ```

6. **Suggestion strip container `<div>`:** add:
   ```tsx
   role="group"
   aria-label="Suggested questions"
   ```

7. **Input field `<input>`:** add:
   ```tsx
   aria-label="Message Parz"
   data-chat-input="true"
   ```
   (Send + close + chip buttons already have aria-labels — keep them. Add data attrs.)

8. **Send button:** add `data-chat-send="true"`.
9. **Close button:** add `data-chat-close="true"`.
10. **Suggestion chip buttons (both):** add `data-chat-chip="true"`.

11. **Scoped `<style>` block — append focus ring rules** (place before the prefers-reduced-motion block):
    ```css
    [data-chat-input]:focus-visible,
    [data-chat-send]:focus-visible,
    [data-chat-close]:focus-visible,
    [data-chat-chip]:focus-visible {
      outline: 2px solid color-mix(in srgb, var(--color-text) 40%, transparent);
      outline-offset: 2px;
    }
    ```

12. **DO NOT TOUCH:**
    - `useChat`, `siteControlChatTransport`, `useSiteControl`.
    - All five existing useEffect bodies (auto-scroll, tool dispatch, loading cycle, error pick — only the focus useEffect was rewritten and an Escape useEffect added).
    - All three handler bodies (handleSend, handleSuggestionClick, handleKeyDown).
    - Phase 26 iOS attrs (`type="text" inputMode="text" enterKeyHint="send" autoComplete="off"`).
    - The onFocus 300ms scrollIntoView handler on the input.
    - The input row container's `paddingBottom: max(16px, env(safe-area-inset-bottom))`.
    - The popupIn / messageAppear / sendSuccessPulse / dot-wave-popup / popup-shimmer-text definitions added in Task 1.

13. **Final pass:** run `npm run build` (or `tsc --noEmit` if available) to confirm the file typechecks. Fix any TypeScript errors introduced by the new ARIA attrs (none expected — all attrs are valid HTML and React supports them natively).

References:
- UI-SPEC §8.1 semantics
- UI-SPEC §8.2 focus management (capture + restore)
- UI-SPEC §8.3 keyboard (Escape)
- UI-SPEC §11.8 acceptance items 39-45
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && grep -q "role=\"dialog\"" src/components/chat-popup.tsx && grep -q "aria-modal=\"true\"" src/components/chat-popup.tsx && grep -q "aria-labelledby=\"chat-popup-heading\"" src/components/chat-popup.tsx && grep -q "role=\"log\"" src/components/chat-popup.tsx && grep -q "aria-live=\"polite\"" src/components/chat-popup.tsx && grep -q "aria-relevant=\"additions\"" src/components/chat-popup.tsx && grep -q "role=\"status\"" src/components/chat-popup.tsx && grep -q "aria-label=\"Parz is typing\"" src/components/chat-popup.tsx && grep -q "role=\"alert\"" src/components/chat-popup.tsx && grep -q "role=\"group\"" src/components/chat-popup.tsx && grep -q "aria-label=\"Suggested questions\"" src/components/chat-popup.tsx && grep -q "aria-label=\"Message Parz\"" src/components/chat-popup.tsx && grep -q "previouslyFocusedRef" src/components/chat-popup.tsx && grep -q "e.key === 'Escape'" src/components/chat-popup.tsx && grep -q "window.addEventListener('keydown'" src/components/chat-popup.tsx && grep -q "data-chat-input" src/components/chat-popup.tsx && grep -q "data-chat-send" src/components/chat-popup.tsx && grep -q "data-chat-close" src/components/chat-popup.tsx && grep -q "data-chat-chip" src/components/chat-popup.tsx && grep -q ":focus-visible" src/components/chat-popup.tsx && grep -q "color-mix(in srgb, var(--color-text) 40%, transparent)" src/components/chat-popup.tsx && grep -q "inputMode=\"text\"" src/components/chat-popup.tsx && grep -q "enterKeyHint=\"send\"" src/components/chat-popup.tsx && grep -q "max(16px, env(safe-area-inset-bottom))" src/components/chat-popup.tsx && grep -q "scrollIntoView({ block: 'center', behavior: 'smooth' })" src/components/chat-popup.tsx && grep -q "siteControlChatTransport" src/components/chat-popup.tsx && grep -q "handledToolCallsRef" src/components/chat-popup.tsx && echo "PASS"</automated>
  </verify>
  <done>
    - Dialog, log, status, alert, group ARIA semantics applied per UI-SPEC §8.1.
    - aria-label="Message Parz" on the input.
    - Escape key handler added via window keydown listener with cleanup.
    - Focus captured to previouslyFocusedRef on mount, restored on unmount.
    - Visible focus rings on input, send, close, chips via :focus-visible.
    - Phase 26 inheritance points all preserved verbatim.
    - Existing useChat / handler logic untouched.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Manual cross-device + a11y smoke verification</name>
  <files>src/components/chat-popup.tsx</files>
  <read_first>
    - .planning/phases/28-chat-ui-redesign/28-UI-SPEC.md §11 (Acceptance Criteria — full)
    - src/components/chat-popup.tsx (final state after Tasks 1 and 2)
  </read_first>
  <what-built>
    Phase 28 fully landed: redesigned chat popup card with new geometry (Plan 01), full visual treatment (Plan 02), motion + a11y (Plan 03). All Phase 25 voice handoff and Phase 26 iOS keyboard handling preserved.
  </what-built>
  <action>
Run automated typecheck first, then perform the manual verification checklist below. Do not skip steps. If any item fails, document the failure in detail and pause for user input before proceeding.

Step 0: Automated typecheck and build sanity:
  npm run build

If the build fails, fix the TypeScript / lint errors and re-run before proceeding to manual checks.

Step 1: Start dev server (user runs this manually per CLAUDE.md "never run applications automatically"):
  npm run dev

Step 2: Walk the user through the 19-point checklist in <how-to-verify>. The user is responsible for actually opening the browser, the iPhone simulator, and toggling reduce-motion.
  </action>
  <how-to-verify>
    Desktop verification (Chrome or Safari, viewport >= 768px):
    1. Open the chat popup — confirm it appears in the bottom-right corner at 420px wide, 24px from the edges, with a 200ms scale-and-fade entry.
    2. Confirm header reads "Parz" in italic serif at 22px, with a smaller "Lakshman's digital twin" subtitle below at 0.55 opacity.
    3. Confirm the empty greeting copy uses an em-dash (`—`).
    4. Confirm the two suggestion chips render as full-pill shapes, hover at scale 1.04.
    5. Click a suggestion chip — message sends, suggestion strip disappears.
    6. Type a message — send button transitions from outlined (0.30 opacity) to filled accent.
    7. Send the message — assistant bubble appears with subtle border, transparent tint, tail bottom-left. Each new message slides up 4px on appear.
    8. Force an error (turn off network, send a message) — error bubble appears in red tint; input wrapper gains a red border.
    9. Type a character — red border fades back to default within 200ms.
    10. Press Escape — popup closes; focus returns to opener (Ask Parz button).
    11. Press Tab repeatedly inside the popup — confirm input → send → close cycle (chips when present).

    Mobile verification (iPhone Safari, real device or simulator):
    12. Open the popup — confirm it fills the viewport minus 8px insets, height respects safe-area at top and bottom.
    13. Tap the input — keyboard pops, field scrolls into view above keyboard within 300ms (Phase 26 behavior).
    14. Confirm `paddingBottom: max(16px, env(safe-area-inset-bottom))` keeps the input clear of the home indicator.
    15. Scroll the suggestion strip horizontally — confirm pill chips scroll smoothly, no wrap.

    Accessibility verification:
    16. macOS VoiceOver (or NVDA on Windows): tab into the popup — VoiceOver announces "Parz, dialog". New assistant messages are announced via aria-live polite. Loading state announces "Parz is typing".
    17. Toggle System Preferences → Accessibility → Display → Reduce Motion. Open the popup — appears instantly with no animation; loading bubble shows three static dots; status text is solid (no shimmer).

    Voice integration verification (regression smoke):
    18. From `/portfolio` say "switch to text chat" with voice mode active — popup mounts, input focuses (Phase 25 / VOICE-05 inheritance). No regression.

    Behavior preservation (regression smoke):
    19. Assistant tool-call still works: ask Parz "open the InfiniteChoice project" — siteControl.openProject fires. Six-branch tool-call dispatch preserved.

    Resume the workflow if all 19 checks pass.
  </how-to-verify>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - npm run build succeeds with zero TypeScript errors and zero lint errors related to chat-popup.tsx.
    - User has confirmed all 19 manual verification items pass.
    - User has typed "approved" or described any issues that require a Plan 04 (gap closure).
  </done>
  <resume-signal>Type "approved" if all 19 checks pass, or describe issues for revision.</resume-signal>
</task>

</tasks>

<verification>
After all three tasks land, the popup matches UI-SPEC §7 and §8 in addition to §3-§6 from Plan 02 and §5 from Plan 01. Behavior preservation across §11.6 and §11.7 is verified by both automated grep tests in each task and the manual checkpoint smoke.

Run a final automated typecheck after Task 2 lands to catch any regression: `npx tsc --noEmit` (or `npm run build` if the project uses Next.js's bundled tsc).
</verification>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User keyboard → window | Escape key listener installed on window |
| ARIA attrs → assistive technology | role / aria-live / aria-label values surfaced to screen readers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-07 | Denial of service | window keydown listener leaks across mounts | mitigate | useEffect returns a cleanup that calls removeEventListener('keydown', onKeyDown). Verified by including in grep checks. |
| T-28-08 | Tampering | previouslyFocusedRef points at a removed DOM node on unmount | accept | Optional chaining `.focus?.()` is defensive. Worst case the focus call no-ops; popup still closes correctly. |
| T-28-09 | Information disclosure | aria-live="polite" announces every assistant chunk | accept | aria-relevant="additions" prevents edits being announced. Polite (not assertive) avoids interrupting other AT speech. UI-SPEC §8.1 explicitly mandates this combo. |
| T-28-10 | Information disclosure | role="alert" reads error bubble loudly to screen readers | accept | Error copy is from PARZ_ERRORS — Parz-voice strings, never raw error.message. No PII or sensitive details. |
</threat_model>

<success_criteria>
- Popup entry runs 200ms scale 0.96→1.0 + opacity 0→1 via popupIn keyframe with cubic-bezier(0.2, 0.9, 0.2, 1).
- Each new message bubble wrapper plays messageAppear 180ms ease-out.
- prefers-reduced-motion: reduce neutralizes all entry/message/dot-wave/shimmer animations.
- Popup root has role=dialog + aria-modal=true + aria-labelledby="chat-popup-heading".
- Messages area has role=log + aria-live=polite + aria-relevant=additions.
- Loading bubble has role=status + aria-label="Parz is typing"; dots are aria-hidden.
- Error bubble has role=alert.
- Suggestion strip has role=group + aria-label="Suggested questions".
- Input has aria-label="Message Parz".
- Escape key on window closes the popup.
- Opener focus is captured on mount and restored on unmount via previouslyFocusedRef.
- Visible focus ring (color-mix 40% var(--color-text), 2px outline, 2px offset) appears on input, send, close, chips :focus-visible.
- All Phase 26 iOS attrs verbatim.
- All useChat + handler + useEffect logic verbatim (except the mount focus useEffect, which is replaced with the focus-capture variant that still calls inputRef.current?.focus()).
- File typechecks.
- Manual checkpoint smoke covers desktop + mobile + reduce-motion + voice handoff + tool-call regression.
</success_criteria>

<output>
After completion, create `.planning/phases/28-chat-ui-redesign/28-03-SUMMARY.md` documenting:
- Motion grep evidence (popupIn rewritten, messageAppear added, sendSuccessPulse added, prefers-reduced-motion neutralizer)
- A11y grep evidence (dialog, log, status, alert, group, label, escape, focus-capture)
- Behavior preservation grep evidence (Phase 25 + Phase 26 + useChat + handlers)
- Manual smoke verification result (all 19 checks)
- Hand-off note: Phase 28 ready for UI-checker review against UI-SPEC §15 sign-off matrix.
</output>
