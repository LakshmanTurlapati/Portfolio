version: 1
frontend:
  phases:
    preBuild:
      commands:
        # Write environment variables to .env.production for API route access
        - echo "XAI_API_KEY=$XAI_API_KEY" >> .env.production
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
