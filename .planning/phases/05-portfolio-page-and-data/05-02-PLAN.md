---
phase: 05-portfolio-page-and-data
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/project-detail.tsx
  - src/components/portfolio-card.tsx
  - src/app/globals.css
  - src/app/layout.tsx
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - DATA-02
must_haves:
  truths:
    - "Project detail overlay title renders in Instrument Serif font matching the prototype"
    - "Overview text renders in Instrument Serif at 22px with 1.45 line-height"
    - "Stats section uses CSS grid (auto-fit, minmax 140px) not flex"
    - "Highlights list uses dash-style bullets (::before dash line) not standard disc bullets"
    - "Stat values render in Instrument Serif at 28px"
    - "Portfolio cards show a subtle hover background brightening (rgba(255,255,255,0.05) light / rgba(0,0,0,0.05) dark)"
    - "Stack tags use monospace font (JetBrains Mono fallback) matching prototype .pd-tag"
    - "Section headings (pd-h3) use monospace font at 11px with 0.14em tracking"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Instrument Serif font loaded via next/font/google"
      contains: "Instrument_Serif"
    - path: "src/app/globals.css"
      provides: "pd-* and ghx-* keyframe/animation classes"
      contains: "pd-fade"
    - path: "src/components/project-detail.tsx"
      provides: "Visual-audit-corrected project detail overlay"
      contains: "font-serif"
    - path: "src/components/portfolio-card.tsx"
      provides: "Card with hover background brightening"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "CSS variable --font-instrument-serif"
      pattern: "instrumentSerif"
    - from: "src/components/project-detail.tsx"
      to: "src/app/layout.tsx"
      via: "font-[family-name:var(--font-instrument-serif)]"
      pattern: "instrument-serif"
---

<objective>
Pixel-perfect audit and polish of the project detail overlay, portfolio card, and related global styles to match the v3 prototype exactly.

Purpose: The existing implementations are functionally complete but visually diverge from the prototype in font choices (Instrument Serif), layout (stats grid vs flex), bullet style (dash vs disc), hover states, and monospace font for metadata elements.

Output: Updated project-detail.tsx, portfolio-card.tsx, layout.tsx (adds Instrument Serif), globals.css (cleans up existing pd-* animations to match prototype spec).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/lakshman/Documents/Misc/P V3/.planning/PROJECT.md
@/Users/lakshman/Documents/Misc/P V3/.planning/ROADMAP.md
@/Users/lakshman/Documents/Misc/P V3/.planning/STATE.md
</context>

<interfaces>
<!-- Key styles from the prototype that differ from existing implementation -->

Prototype .pd-title (project-detail.tsx h1):
```css
font-family: 'Instrument Serif', 'Cormorant Garamond', serif;
font-size: 56px; line-height: 1.02; font-weight: 400; letter-spacing: -0.02em;
```

Prototype .pd-tagline:
```css
font-size: 18px; line-height: 1.5; opacity: 0.72; max-width: 62ch;
```

Prototype .pd-overview:
```css
font-family: 'Instrument Serif', 'Cormorant Garamond', serif;
font-size: 22px; line-height: 1.45; margin: 0 0 32px; max-width: 62ch; opacity: 0.92;
```

Prototype .pd-stats:
```css
display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr));
gap: 16px; padding: 20px 0; margin: 0 0 32px;
border-top: 1px solid rgba(255,255,255,0.08); border-bottom: 1px solid rgba(255,255,255,0.08);
```

Prototype .pd-stat-val:
```css
font-family: 'Instrument Serif', serif; font-size: 28px; line-height: 1;
letter-spacing: -0.01em; margin-bottom: 6px;
```

Prototype .pd-stat-lbl:
```css
font-family: 'Space Mono', 'JetBrains Mono', monospace;
font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase; opacity: 0.55;
```

Prototype .pd-tag (stack chip):
```css
font-family: 'Space Mono', 'JetBrains Mono', monospace;
font-size: 11px; padding: 4px 10px; border-radius: 6px;
background: rgba(255,255,255,0.06); opacity: 0.82;
```
Light mode: background: rgba(0,0,0,0.05)

Prototype .pd-h3 (section heading):
```css
font-family: 'Space Mono', 'JetBrains Mono', monospace;
font-size: 11px; letter-spacing: 0.14em; text-transform: uppercase;
opacity: 0.55; margin: 0 0 14px; font-weight: 500;
```

Prototype .pd-list li (highlights bullet):
```css
/* NO standard disc bullet — uses ::before pseudo-element dash */
position: relative; padding: 0 0 12px 22px;
font-size: 15px; line-height: 1.55; opacity: 0.88;
```
```css
.pd-list li::before {
  content: ""; position: absolute; left: 0; top: 10px;
  width: 10px; height: 1px; background: currentColor; opacity: 0.5;
}
```

Prototype .pd-p (section body text):
```css
font-size: 15px; line-height: 1.65; margin: 0; opacity: 0.88;
```

Prototype .pd-chip (year/role badge):
```css
font-family: 'Space Mono', 'JetBrains Mono', monospace;
font-size: 10px; letter-spacing: 0.12em; text-transform: uppercase;
padding: 5px 10px; border-radius: 999px;
background: rgba(255,255,255,0.08); border: 1px solid rgba(255,255,255,0.10);
```
Light mode: background: rgba(0,0,0,0.05); border-color: rgba(0,0,0,0.08)

Prototype .pd-chip-subtle (role chip):
```css
background: transparent; opacity: 0.7;
```

Prototype .pd-hero padding: 56px 56px 24px (current is px-14/pt-14 = 56px — OK)
Prototype .pd-cover margin: 16px 56px 32px (current is mx-14/mb-8 = 56px/32px — OK; mt is 16px → `mt-4`)
Prototype .pd-body padding: 0 56px 40px (current is px-14 = 56px — OK, pb missing: add `pb-10`)
Prototype .pd-foot padding: 24px 56px 48px (current is px-14/py-8 — close but use exact values)

Prototype .pf-card hover state:
```css
.pf-card:hover { background: rgba(255,255,255,0.05); }
.portfolio.dark .pf-card:hover { background: rgba(0,0,0,0.05); }
```
Current card has no explicit hover background style change.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add Instrument Serif font and fix project-detail.tsx</name>
  <files>src/app/layout.tsx, src/components/project-detail.tsx, src/app/globals.css</files>
  <read_first>
    src/app/layout.tsx (full file — already read in planning)
    src/components/project-detail.tsx (full file — already read in planning)
    src/app/globals.css (full file — already read in planning)
    /tmp/design-extract/portfolio-v3/project/styles.css (lines 830–1157 — all pd-* CSS)
  </read_first>
  <action>
**Step A — Add Instrument Serif to layout.tsx:**

Import `Instrument_Serif` from `next/font/google`:
```typescript
import { Lato, Instrument_Serif } from 'next/font/google';

const instrumentSerif = Instrument_Serif({
  subsets: ['latin'],
  weight: ['400'],
  style: ['normal', 'italic'],
  variable: '--font-instrument-serif',
  display: 'swap',
});
```
Apply both font variables to the `html` element:
```typescript
<html lang="en" className={`${lato.variable} ${instrumentSerif.variable}`} suppressHydrationWarning>
```

**Step B — Update globals.css:**

Add `.pd-list` and `.pd-list li::before` CSS rules for the dash-bullet style. These cannot be done purely with Tailwind (pseudo-element with content ""):
```css
/* Project detail highlights list — dash bullets (per prototype) */
.pd-highlights-list {
  list-style: none;
  padding: 0;
  margin: 0;
}
.pd-highlights-list li {
  position: relative;
  padding: 0 0 12px 22px;
  font-size: 15px;
  line-height: 1.55;
  opacity: 0.88;
}
.pd-highlights-list li::before {
  content: "";
  position: absolute;
  left: 0;
  top: 10px;
  width: 10px;
  height: 1px;
  background: currentColor;
  opacity: 0.5;
}
```

**Step C — Update project-detail.tsx:**

Make these targeted changes (do NOT change the component's logic, props, or structure — only the visual/styling properties):

1. **h1 title:** Change class from `text-[56px] leading-[1.02] font-normal tracking-[-0.02em] m-0 mb-3 font-serif` to `text-[56px] leading-[1.02] font-normal tracking-[-0.02em] m-0 mb-3 font-[family-name:var(--font-instrument-serif)]`

2. **tagline `<p>`:** Change `text-lg` to `text-[18px]` and `opacity-70` to `opacity-[0.72]` and add `max-w-[62ch]`

3. **Year chip `<span>`:** Change its className to use monospace font and exact prototype sizes:
   - Remove `font-mono` (uses Lato/system mono)
   - Add: `style={{ fontFamily: "'JetBrains Mono', 'Space Mono', monospace" }}` OR add a CSS custom class
   - Change `text-[10px]` to `text-[10px]` (already correct)
   - Change `px-2.5 py-1` to `px-[10px] py-[5px]`
   - Change `rounded-full` to `rounded-full` (already correct)
   - Keep the border/background inline styles already there

4. **Role chip `<span>`:** Same monospace font treatment, and keep `opacity-70` as `opacity-[0.70]`

5. **overview `<p>`:** Add `font-[family-name:var(--font-instrument-serif)]` class, change `text-base` to `text-[22px]`, change `leading-relaxed` to `leading-[1.45]`, change `opacity-80` to `opacity-[0.92]`, add `max-w-[62ch]`, change `mb-8` to `mb-8` (keep as is — 32px = mb-8 ✓)

6. **stats section:** Change the flex wrapper `div className="flex gap-8 mb-8 flex-wrap"` to:
   ```
   className="grid gap-4 mb-8 py-5"
   style={{ gridTemplateColumns: 'repeat(auto-fit, minmax(140px, 1fr))', borderTop: `1px solid ${isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.08)'}`, borderBottom: `1px solid ${isDark ? 'rgba(255,255,255,0.08)' : 'rgba(0,0,0,0.08)'}` }}
   ```
   - Stat value `<div>`: Change `text-2xl font-bold tracking-tight` to `text-[28px] leading-none tracking-[-0.01em] mb-1.5 font-[family-name:var(--font-instrument-serif)] font-normal`
   - Stat label `<div>`: Change `text-xs uppercase tracking-[0.08em] opacity-50 mt-1` to `text-[10px] uppercase tracking-[0.12em] opacity-55 mt-0` and add `style={{ fontFamily: "'JetBrains Mono', 'Space Mono', monospace" }}`

7. **stack tags `<span>`:** Change className to include monospace font and correct sizing:
   - Change `text-xs px-3 py-1.5 rounded-full` to `text-[11px] px-[10px] py-1 rounded-[6px]`
   - Add `style={{ fontFamily: "'JetBrains Mono', 'Space Mono', monospace", opacity: 0.82 }}`
   - Remove `opacity: 0.82` from the inline border style — move it to the style object
   - Background already correct in inline style

8. **Highlights section h3:** Change className from `text-sm font-bold uppercase tracking-[0.06em] opacity-60 mb-3` to `text-[11px] font-medium uppercase tracking-[0.14em] opacity-55 mb-[14px]` and add `style={{ fontFamily: "'JetBrains Mono', 'Space Mono', monospace" }}`

9. **Highlights `<ul>`:** Change className from `list-disc pl-5 space-y-2` to `pd-highlights-list` (uses the CSS class added in Step B above)

10. **Highlights `<li>`:** Remove the className entirely (handled by `pd-highlights-list li` CSS)

11. **Sections h3:** Same monospace treatment as highlights h3 (Step 8 above)

12. **Section body `<p>`:** Change `text-sm leading-relaxed opacity-80` to `text-[15px] leading-[1.65] opacity-[0.88]`

13. **Cover div margin:** Change `mx-14 mb-8` to `mt-4 mx-14 mb-8` (adds top margin to match `margin: 16px 56px 32px`)

14. **Body padding:** Change `px-14` wrapper to `px-14 pb-10` (adds pb 40px)

15. **Footer padding:** Change `px-14 py-8 mt-8` to `px-14 py-6 mt-8` with `style={{ paddingTop: '24px', paddingBottom: '48px' }}`

**Keep unchanged:** All logic, props, event handlers, close button, cover image, link buttons, background/border inline styles, z-index, animation classes.
  </action>
  <verify>
    <automated>cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit 2>&1 | head -30 && grep -n "instrument-serif\|Instrument_Serif" src/app/layout.tsx src/components/project-detail.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/layout.tsx` imports `Instrument_Serif` and applies `instrumentSerif.variable` to `html` element
    - `src/components/project-detail.tsx` h1 uses `font-[family-name:var(--font-instrument-serif)]` class
    - `src/components/project-detail.tsx` overview p uses `font-[family-name:var(--font-instrument-serif)] text-[22px]`
    - `src/components/project-detail.tsx` stat values use `font-[family-name:var(--font-instrument-serif)] text-[28px]`
    - `src/components/project-detail.tsx` highlights ul has class `pd-highlights-list` (not `list-disc`)
    - `src/app/globals.css` contains `.pd-highlights-list li::before` rule
    - Stats section uses CSS grid: grep for `gridTemplateColumns` or `grid gap-4` in project-detail.tsx
    - TypeScript compiles without errors
  </acceptance_criteria>
  <done>Project detail overlay matches prototype typography: Instrument Serif for title/overview/stat values, monospace for chips/tags/headings, grid stats layout, dash bullets for highlights.</done>
</task>

<task type="auto">
  <name>Task 2: Polish portfolio card hover state</name>
  <files>src/components/portfolio-card.tsx</files>
  <read_first>
    src/components/portfolio-card.tsx (full file — already read in planning)
    /tmp/design-extract/portfolio-v3/project/styles.css (lines 522–555 — .pf-card styles)
  </read_first>
  <action>
Update `src/components/portfolio-card.tsx` to add hover background brightening per the prototype:

**Prototype spec:**
```css
.pf-card { background: rgba(255,255,255,0.03); transition: background 0.3s; }
.portfolio.dark .pf-card { background: rgba(0,0,0,0.03); }
.pf-card:hover { background: rgba(255,255,255,0.05); }
.portfolio.dark .pf-card:hover { background: rgba(0,0,0,0.05); }
```

**Current code:**
```typescript
style={{
  background: isDark ? 'rgba(0,0,0,0.03)' : 'rgba(255,255,255,0.03)',
}}
```
(No hover state.)

**Change:** Add a React hover state to the card div using `onMouseEnter`/`onMouseLeave` — but `onEnter`/`onLeave` are already used for DataGrid effects. Add a separate `isHovered` state for background only:

```typescript
const [isHovered, setIsHovered] = useState(false);
```

In `onEnter`, also call `setIsHovered(true)`. In `onLeave`, also call `setIsHovered(false)`.

Update the card div style:
```typescript
style={{
  background: isHovered
    ? (isDark ? 'rgba(0,0,0,0.05)' : 'rgba(255,255,255,0.05)')
    : (isDark ? 'rgba(0,0,0,0.03)' : 'rgba(255,255,255,0.03)'),
  transition: 'background 0.3s',
}}
```

Also check the card's `mb-8` class: prototype uses `margin-bottom: 32px` → Tailwind `mb-8` = 2rem = 32px — this is correct, no change needed.

Card `border-radius: 12px` → `rounded-xl` = 12px — correct.

Import `useState` if not already imported (it is already: `useRef` is imported from react, but check if `useState` is — it's not in current imports).

Add `useState` to the existing `import { useRef } from 'react'` → `import { useRef, useState } from 'react'`.
  </action>
  <verify>
    <automated>cd "/Users/lakshman/Documents/Misc/P V3" && npx tsc --noEmit 2>&1 | head -30 && grep -n "isHovered\|setIsHovered" src/components/portfolio-card.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/portfolio-card.tsx` imports `useState`
    - `isHovered` state variable is present
    - Card div background style changes on hover: `rgba(0,0,0,0.05)` (dark) or `rgba(255,255,255,0.05)` (light) when hovered
    - `transition: 'background 0.3s'` is set on card div
    - TypeScript compiles without errors
    - DataGrid hover effect (onEnter/onLeave) still works — `setCardHover` calls are preserved
  </acceptance_criteria>
  <done>Portfolio cards show a subtle hover background brightening matching the prototype spec, without affecting the DataGrid hover effect logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| layout.tsx → Google Fonts CDN | Font loading from external CDN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | dangerouslySetInnerHTML in project-detail.tsx | accept | Content is static, developer-authored text — no user input rendered as HTML |
| T-05-05 | Spoofing | Google Fonts CDN | accept | Next.js font optimization self-hosts the font files — no runtime CDN dependency after build |
</threat_model>

<verification>
1. Open portfolio page and hover a project card — background should subtly brighten
2. Click a project card to open the detail overlay
3. Verify project name renders in Instrument Serif (elegant serif, not sans-serif)
4. Verify overview text renders in Instrument Serif at larger size
5. Verify stat values (e.g., "~5x") render in Instrument Serif
6. Verify stat labels use monospace font
7. Verify highlights use dash bullets (horizontal line, not disc)
8. Verify stack tags use monospace font, rounded corners (not fully rounded)
9. Verify section headings are monospace, uppercase, faded
</verification>

<success_criteria>
PORT-01 through PORT-05, DATA-02: All visual elements match prototype spec. Instrument Serif loaded and applied to title/overview/stat values. Monospace applied to metadata chips/tags/headings. Stats grid layout correct. Dash bullets on highlights. Card hover background brightening active.
</success_criteria>

<output>
After completion, create `.planning/phases/05-portfolio-page-and-data/05-02-SUMMARY.md` using the summary template.
</output>
