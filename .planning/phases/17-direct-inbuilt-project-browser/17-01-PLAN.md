---
phase: 17
plan: 01
title: Canonical project resolver and approved browser targets
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
autonomous: true
requirements:
  - BROW-03
  - BROW-04
  - BROW-05
  - SAFE-06
must_haves:
  - Natural aliases resolve to canonical project records
  - Project targets come only from approved local project records
  - GitFly opens only https://gitfly.ai
---

<objective>
Add a canonical project resolver and approved browser-target selector so manual and AI-triggered project opens can share safe local records instead of model-generated destinations.
</objective>

<tasks>
<task id="01" type="execute">
  <title>Extend project records with aliases and target preference</title>
  <read_first>
    - .planning/phases/17-direct-inbuilt-project-browser/17-CONTEXT.md
    - .planning/phases/17-direct-inbuilt-project-browser/17-UI-SPEC.md
    - src/data/projects.ts
  </read_first>
  <files>
    - src/data/projects.ts
  </files>
  <action>
    Extend the `Project` interface with optional `aliases?: string[]` and `preferredTarget?: keyof Project['links']` fields.

    Add aliases for at least:
    - `FSB / Full Self Browsing`: `FSB`, `Full Self Browsing`, `full-selfbrowsing`, `full self browsing`
    - `GitFly`: `git fly`, `gitfly.ai`
    - `Review Gate`: `ReviewGate`, `review-gate`
    - `T2S CLI`: `T2S`, `text to sql`, `text-to-sql`
    - `Parz-AI`: `Parz AI`, `Parz`, `parz ai`

    Set GitFly's preferred target to `Website`. Do not add any GitFly GitHub/source link.
  </action>
  <acceptance_criteria>
    - `src/data/projects.ts` defines `aliases?: string[]` on `Project`.
    - `src/data/projects.ts` defines `preferredTarget?: keyof Project['links']` on `Project`.
    - `src/data/projects.ts` contains aliases for `FSB`, `Full Self Browsing`, `GitFly`, `Review Gate`, `T2S`, and `Parz-AI`.
    - GitFly links contain `https://gitfly.ai` and no GitHub/source URL.
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Add resolver and safe browser target helpers</title>
  <read_first>
    - src/data/projects.ts
    - src/components/iframe-viewer.tsx
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - src/data/projects.ts
  </files>
  <action>
    Add exported helpers in `src/data/projects.ts`:
    - `allProjects` or equivalent combined canonical project list.
    - `resolveProject(input: string): Project | null` that matches name or aliases case-insensitively after punctuation/spacing normalization.
    - `getProjectBrowserTarget(project: Project): { url: string; label: string } | null` that prefers `preferredTarget`, then Website, Design, GitHub.
    - `isApprovedProjectUrl(url: string): boolean` that returns true only for URLs present in local project `links` records.

    Keep the helpers pure and framework-free so Phase 18 and Phase 20 can reuse them.
  </action>
  <acceptance_criteria>
    - `resolveProject('FSB')` can resolve to `FSB / Full Self Browsing`.
    - `resolveProject('Full Self Browsing')` can resolve to `FSB / Full Self Browsing`.
    - `resolveProject('GitFly')` can resolve to `GitFly`.
    - `resolveProject('Review Gate')` can resolve to `Review Gate`.
    - `resolveProject('T2S')` can resolve to `T2S CLI`.
    - `resolveProject('Parz-AI')` can resolve to `Parz-AI`.
    - `getProjectBrowserTarget` returns only URLs from the project `links` object.
    - `isApprovedProjectUrl` rejects URLs that are not present in project records.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run lint` after implementation.
- Inspect `src/data/projects.ts` for the exported resolver helpers and alias records.
- Confirm GitFly target selection returns `https://gitfly.ai`.
</verification>

<success_criteria>
- Natural project aliases resolve to canonical local project records.
- Approved browser targets are selected from local project data only.
- Unknown/projectless input has no target and cannot open arbitrary model-generated URLs.
</success_criteria>

<must_haves>
- Covers BROW-03, BROW-04, BROW-05, and SAFE-06.
- Keeps resolver code reusable for Phase 18 global site control.
</must_haves>
