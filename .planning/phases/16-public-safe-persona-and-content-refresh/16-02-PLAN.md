---
phase: 16
plan: 02
title: Visible About Experience and flagship project content refresh
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/data/bio.ts
  - src/data/experience.ts
  - src/data/projects.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - PERS-04
must_haves:
  - Covers CONT-01 through CONT-05
  - No GitFly private source exposure
  - No non-public InfiniteChoice/Voyza details
---

<objective>
Update visible portfolio content so About, Experience, FSB, GitFly, and existing flagship project copy match the same approved public facts used by Parz.
</objective>

<tasks>
<task id="01" type="execute">
  <title>Refresh About narrative</title>
  <read_first>
    - src/data/bio.ts
    - src/data/public-profile.ts
    - .planning/phases/16-public-safe-persona-and-content-refresh/16-CONTEXT.md
  </read_first>
  <files>
    - src/data/bio.ts
  </files>
  <action>
    Rewrite `bioSegments` to reflect Lakshman as an AI builder, open-source builder, full-stack engineer turned AI engineer, and creative technologist.

    The copy must include these exact or near-exact facts:
    - Current work at `InfiniteChoice`
    - Current role `AI Enablement Engineer`
    - Building `Voyza, an AI-first hotel booking platform`
    - Current flagship projects include `FSB / Full Self Browsing` and `GitFly`
    - GitFly is public at `https://gitfly.ai` and source/private implementation details are not part of the public story
    - Builder psychology uses the alignment/gap-radar framing from the context

    Remove stale wording that says current work is at Rocket Mortgage. Keep the tone personal, high-energy, grateful, and direct without making it sound like a recruiter bio.
  </action>
  <acceptance_criteria>
    - `src/data/bio.ts` contains `InfiniteChoice`.
    - `src/data/bio.ts` contains `AI Enablement Engineer`.
    - `src/data/bio.ts` contains `Voyza`.
    - `src/data/bio.ts` contains `FSB`.
    - `src/data/bio.ts` contains `GitFly`.
    - `src/data/bio.ts` does not contain `current role working on production AI systems at Rocket Mortgage`.
  </acceptance_criteria>
</task>

<task id="02" type="execute">
  <title>Refresh Experience entry</title>
  <read_first>
    - src/data/experience.ts
    - src/data/public-profile.ts
    - .planning/REQUIREMENTS.md
  </read_first>
  <files>
    - src/data/experience.ts
  </files>
  <action>
    Add or update the top `experienceData` entry for InfiniteChoice.

    Required fields:
    - `title`: `AI Enablement Engineer`
    - `company`: `InfiniteChoice`
    - description includes `Voyza, an AI-first hotel booking platform`
    - description stays public-safe and does not include internal implementation details, metrics, private architecture, client/employer details, or secrets

    Preserve prior roles as historical entries below current work unless there is a clear duplicate/stale conflict.
  </action>
  <acceptance_criteria>
    - `src/data/experience.ts` contains `AI Enablement Engineer`.
    - `src/data/experience.ts` contains `InfiniteChoice`.
    - `src/data/experience.ts` contains `Voyza, an AI-first hotel booking platform`.
    - `src/data/experience.ts` still contains `Rocket Mortgage` or `Mr.Cooper` as historical experience if retained.
  </acceptance_criteria>
</task>

<task id="03" type="execute">
  <title>Refresh FSB and GitFly project content</title>
  <read_first>
    - src/data/projects.ts
    - src/data/public-profile.ts
    - .planning/phases/16-public-safe-persona-and-content-refresh/16-CONTEXT.md
  </read_first>
  <files>
    - src/data/projects.ts
  </files>
  <action>
    Update `src/data/projects.ts` so FSB / Full Self Browsing and GitFly are current flagship projects.

    Required project content:
    - FSB / Full Self Browsing must use the public URL `https://www.full-selfbrowsing.com`.
    - FSB must be described as a public/open-source browser automation assistant or AI browsing/control project, not as a game-development project.
    - GitFly must be present as a flagship project.
    - GitFly must link only to `https://gitfly.ai`.
    - GitFly copy must avoid private source-code or private implementation details.
    - Review Gate should remain available and accurate as a flagship/open-source story.

    Prefer pinning or clearly promoting current flagships without removing useful historical projects unless necessary. If image assets for GitFly/FSB are missing, reuse existing safe placeholder behavior or existing assets; do not invent external private asset URLs.
  </action>
  <acceptance_criteria>
    - `src/data/projects.ts` contains `GitFly`.
    - `src/data/projects.ts` contains `https://gitfly.ai`.
    - `src/data/projects.ts` contains `Full Self Browsing`.
    - `src/data/projects.ts` contains `https://www.full-selfbrowsing.com`.
    - `src/data/projects.ts` does not contain `Game development project exploring interactive gameplay mechanics`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
- Run `npm run lint`.
- Inspect `src/data/bio.ts`, `src/data/experience.ts`, and `src/data/projects.ts` for the required strings in the acceptance criteria.
</verification>

<success_criteria>
- About content reflects current AI-builder/open-source-builder identity.
- Experience content includes InfiniteChoice / AI Enablement Engineer / Voyza public-safe wording.
- FSB and GitFly appear as current flagship projects with approved public links.
- Prompt facts and visible content use the same public-safe source facts.
</success_criteria>

<must_haves>
- Covers CONT-01 through CONT-05.
- No GitFly private source exposure.
- No non-public InfiniteChoice/Voyza details.
</must_haves>
